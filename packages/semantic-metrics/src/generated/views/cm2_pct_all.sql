-- SPEC:D.2 — compiled metric view: cm2_pct (v1) @ grain=all · entity=semantic_order
-- GENERATED by @brain/semantic-metrics compiler — DO NOT EDIT. Change the YAML + recompile.
-- Tenancy: the BRAND_PREDICATE sentinel (below) is replaced with `brand_id = ?` by the serving seam.
CREATE OR REPLACE VIEW iceberg.brain_serving.mv_metric_cm2_pct_all AS
WITH base AS (
  SELECT
    brand_id,
    currency_code,
    channel,
    SUM(cm2_minor) AS cm2_minor,
    SUM(net_revenue_minor) AS net_revenue_minor
  FROM iceberg.brain_serving.semantic_order
  WHERE ${BRAND_PREDICATE}
  GROUP BY 1, 2, 3
)
SELECT
  brand_id,
  currency_code,
  channel,
  cm2_minor,
  net_revenue_minor,
  (CAST(cm2_minor AS double) / NULLIF(net_revenue_minor, 0)) AS cm2_pct
FROM base;
