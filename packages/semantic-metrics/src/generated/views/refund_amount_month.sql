-- SPEC:D.2 / §1.11.1 — compiled INTERACTIVE metric view: refund_amount (v1) @ grain=month.
-- Reads the pre-agg iceberg.brain_serving.preagg_refund_amount_month (covered dims). Long-tail/uncovered dims → the
-- base-entity fallback view brain_serving.mv_metric_refund_amount_month_slow. Tenancy: BRAND_PREDICATE sentinel → `brand_id = ?`.
-- GENERATED by @brain/semantic-metrics compiler — DO NOT EDIT. Change the YAML + recompile.
CREATE OR REPLACE VIEW brain_serving.mv_metric_refund_amount_month AS
SELECT
  brand_id,
  period,
  currency_code,
  channel,
  gross_revenue_minor,
  net_revenue_minor,
  (gross_revenue_minor - net_revenue_minor) AS refund_amount
FROM iceberg.brain_serving.preagg_refund_amount_month
WHERE ${BRAND_PREDICATE};
