# Prod overrides — set the real upstream Postgres host + the admin-creds secret.
# Deployed by the ArgoCD app infra/argocd/envs/prod/pgbouncer.yaml (AUD-COST-019).
# CORE routes here: its DATABASE_URL / BRAIN_APP_DATABASE_URL host:port must be
# pgbouncer.pgbouncer.svc.cluster.local:6432 (the Service is named by CHART name
# `pgbouncer` in ns `pgbouncer`; set in the core-env secret). The STREAM-WORKER stays DIRECT
# on the Postgres primary (session advisory locks — transaction pooling would break the leader lock).
replicaCount: 2
upstream:
  host: brain-prod-postgres.cluster-cjy6iicow625.ap-south-1.rds.amazonaws.com      # e.g. brain-prod.xxxx.rds.amazonaws.com
envSecretName: pgbouncer-env
pool:
  defaultPoolSize: 25
  maxClientConn: 5000
