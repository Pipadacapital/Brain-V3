{
  "name": "@brain/metric-engine",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "test:unit": "vitest run --exclude '**/*.live.test.ts' --exclude '**/*.e2e.test.ts'",
    "test:live": "vitest run .live"
  },
  "dependencies": {
    "@brain/money": "workspace:*",
    "@brain/observability": "workspace:*"
  },
  "devDependencies": {
    "@types/pg": "^8.11.0",
    "pg": "^8.21.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
