# Pending Stakeholder Attention

> Items here require Stakeholder review. Agents add lines; the Stakeholder acts and strikes them through.
> Each line names the issue, the artifact path, and the slash command to act.

- ~~[feat-realized-revenue-ledger / Stage-6 PASS] **F-SEC-01 (HIGH) conscious-accept** — `revenue-finalization` Argo job no-ops under `brain_app` FORCE-RLS brand enumeration ... Act: accept at the Stage-7 gate or bounce to fix.~~ **[RESOLVED 2026-06-17 — fixed via 0019 `list_active_brand_ids()`; codified by durable rule `system-job-force-rls-enumeration`.]**
- ~~[RULE-PROPOSAL recommended — recurring pattern, 2nd occurrence] **Cross-tenant system/Argo jobs MUST enumerate tenants via a SECURITY DEFINER function ...** Act: `/adopt-rule` to codify.~~ **[SUPERSEDED → adopted 2026-06-17 as durable rule `system-job-force-rls-enumeration`.]**
- ~~[RULE-PROPOSAL — **3rd occurrence, auto-candidate bar CROSSED**] **System/cron/worker jobs MUST enumerate tenants via a SECURITY DEFINER fn ...** Act: `/adopt-rule system-job-force-rls-enumeration`.~~ **[ADOPTED 2026-06-17 by stakeholder → `.engineering-os/durable-rules/2026-06-17T09-59-08Z__system-job-force-rls-enumeration.md`.]**
- ~~[feat-connector-backfill / Stage-6 PASS] **APPROVE → Stakeholder gate, 0 blocking.** ... Act: accept + commit + advance at the Stage-7 gate.~~ **[SHIPPED 2026-06-17 (PR #31). Residual tracked debt remains open: SEC-BF-M2 (dual LedgerWriter, post-M1), SEC-BF-L1 (dual repo), DEV-TOKEN-REACH (live worker token reachability).]**

- ~~[feat-shopify-live-connector / Stage-6 PASS] **APPROVE → Stakeholder gate, 0 blocking.** Deep Shopify live connector (webhooks + 35-day re-pull + live recognition). ORCH-LV-H1 (wired-to-nothing) fixed + re-proven LIVE (ledger 19,488→20,285, 49 rto_reversal). Act: accept + commit + advance at the Stage-7 gate.~~ **[SHIPPED 2026-06-20 — accepted at the Stage-7 gate; branch `feat/shopify-live-connector` merged to master. Tracked non-blocking debt remains open: SEC-LV-M1 (MED, re-pull lock-window → double API calls, M1+), SEC-LV-L1 (LOW, NaN-date guard).]**
- [WATCH — wired-to-nothing pattern, **2nd occurrence**, NOT yet at the /adopt-rule bar] A new Kafka consumer / recognition-writer built + unit-tested in isolation but NOT subscribed/`.start()`-ed in the deployable (`main.ts`) — order/recognition effect doesn't run; caught only by live verification, not the unit-tested reviews. #1 = ADR-BF-9 (backfill), #2 = ORCH-LV-H1 (this run). Proposed rule (END-TO-END wiring test: real produce → real subscribe → observed sink effect) is drafted in `11-final-review.md` + `lessons-learned.md`. Act: NO action now; the final reviewer recommends raising `/adopt-rule` at occurrence #3 (matches the system-job-force-rls 3-occurrence precedent).

(no other open items)

- ~~[eos-oob-reconciliation / #68 feat-capi-conversion-feedback `17215034ecb6`] **Currency-exponent latent money bug** — `customData.value = Number(valueMinor)/100` hardcodes a 2-decimal exponent (off by 100×/10× for JPY/KWD/BHD)~~ **[CODE RESOLVED 2026-06-20 — exponent is currency-aware via `@brain/money` MINOR_UNITS; the silent `/100` fallback is removed and an unmodeled currency is now BLOCKED fail-closed (status `blocked_unsupported_currency`, migration 0052). PR `fix/68-capi-currency-failclosed`.]** — STILL OPEN (non-code): the advertising path skips the 9–9 IST send-window as a compliance call — confirm legal signed off on the send-window carve-out. (ref `docs/eos-reconciliation-2026-06.md`)
- ~~[eos-oob-reconciliation / #74 feat-decision-intelligence-inputs `2b5397e61175`] **Cost-observability hole** — the only model call (NLQ resolver) ships without a machine-readable `@effort` declaration. Act: add `@effort(...)` in `packages/ai-gateway-client/src/client.ts`.~~ **[RESOLVED 2026-06-20 — `@effort("large_model", token_budget=256)` is present (client.ts:9,86); the hard caps are code constants `RESOLVER_MAX_OUTPUT_TOKENS=256` / `RESOLVER_TEMPERATURE=0` AND unit-tested (client.test.ts "exactly ONE call at temperature 0 with the output-token cap"). Per-tenant spend-cap is enforced at the litellm gateway (virtual-key budget) — ops confirmation, not in-repo.]**
- ~~[eos-oob-reconciliation / #67 fix-analytics-contract-audit `b3a75b7c90a1`] **Previously-fabricated analytics number** — journey-mix screen now shows honest distinct-journey counts. Act: consider a web↔core contract codegen test to stop drift recurring.~~ **[RESOLVED 2026-06-20 — the web↔core BFF read-contract gate was widened to the drift-prone analytics/identity/billing/recommendation DTOs (PR `feat/67-widen-contract-gate`, 61 gate cases). Stakeholder heads-up: the journey-mix number changed (informational, already merged).]**
- ~~[eos-oob-reconciliation / #69 fix-session-brand-context `6f2665cb5ba7`] **Auth/multi-tenancy change merged with no security review** — brand-context resolution mints `brand_id` in the JWT; shipped with only a DB-gated test. Act: backfill a non-DB-gated unit/contract test for the brand-resolution path.~~ **[RESOLVED — `apps/core/src/modules/workspace-access/tests/brand-resolution.test.ts` backfills pure (no-DB) coverage of `selectActiveContext`, incl. the safety property (never mints `brand_id=null` when a brand exists); 6/6 green. Process-gap noted.]**
- ~~[eos-oob-reconciliation / #75 fix-worker-secrets-aws-import `0dcf973`] **Prod Shopify secret loading was previously broken** — the old cross-package `require()` path resolved outside the repo (MODULE_NOT_FOUND). Act: confirm bundle layout matches the relative path.~~ **[RESOLVED 2026-06-20 — the fragile cross-package `require()` is ELIMINATED: the secrets abstraction moved to a real workspace package `@brain/connector-secrets` (with `@aws-sdk` declared as its own dep); both deployables import it normally, so there is no source-tree-layout dependency to break. PR `fix/75-connector-secrets-shared-package`. KMS/NN-2 proofs moved with the impl (14 tests green).]**
- ~~[eos-oob-reconciliation / #65 fix-starrocks-analytics-dev-password `9678ac3`] **Weak-default prod footgun** — core defaults the StarRocks analytics password to `brain_analytics_dev`. Act: prefer fail-loud in production over the dev default.~~ **[RESOLVED — all 4 `STARROCKS_ANALYTICS_PASSWORD` call sites use `requireEnvInProd(...)`, which THROWS in production when unset (config/index.ts:91-96) and only returns the dev default in dev/test — the exact fail-loud-in-prod pattern (strictly better than a blanket `getEnvOrThrow` that would break dev). Prod injects the value via the opaque `envSecretName` blob (External Secrets ← AWS Secrets Manager), whose `values.yaml` documents `STARROCKS_*` as required keys; the throw is the enforcement.]**
