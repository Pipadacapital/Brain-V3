# Deployment-Spec Ratifications — Stage-B Deploy Audit (Wave 4)

Status: **RATIFIED** (user decision, 2026-07-02). Source:
`audit/DEPLOY-AUDIT-REPORT.md` §Wave-4 — the 20 CONFLICT-ESCALATION findings
where the repo deliberately deviates from the deployment spec with in-repo,
evidence-backed justification. **The repo state stands; the spec/doc text is
updated to match.** None of these items is queued for code change — a future
audit that flags any row below as a deviation should treat this document as
the ratification record and close the finding as conformant.

Rules preserved by every ratification: tenant isolation, measured memory
bounds, durability (no event loss), and the SLA path are never weakened.

| AUD ID | Spec said | Repo does | Why ratified (evidence) | Date |
| --- | --- | --- | --- | --- |
| AUD-LOCAL-005 | LocalStack `SERVICES` = `secretsmanager,s3` only | `SERVICES: "s3,secretsmanager,kms,ses,events"` (`docker-compose.yml:226`) | `kms` is load-bearing for the PII-vault DEK wrap (`packages/pii-vault`, `@aws-sdk/client-kms`; commit 5d5d1bee re-wraps brand keyrings via KMS) and `ses` for the notification adapter (`apps/core/.../ses-adapter.ts`) — the spec predates the prod-on-local KMS work (merged 2026-06-21). Follow-up (non-blocking): trim the unused `events` entry. | 2026-07-02 |
| AUD-LOCAL-006 | Apicurio lives in a separate `schema` compose profile | Apicurio is in the `core` profile (`docker-compose.yml:384-410`); `dev-down.sh:25` still passes a harmless no-op `--profile schema` | Apicurio sits on the collector/stream-worker ingest-path schema validation (strict-SLA path) — a schema-profile-off run would break ingest. Deliberate fold-in documented at `docker-compose.yml:385-387`. | 2026-07-02 |
| AUD-LOCAL-007 | Trino container 1G limit / 768m heap | 7g `mem_limit` + `MaxRAMPercentage=70` (~4.9g heap) — `docker-compose.yml:516-521`, `db/trino/jvm.config` | Measured outage: Trino is the SOLE serving engine and was OOM-killed under refresh at the unbounded/stock config (trino-oom-serving-outage-fix, branch `fix/trino-bounded-heap-autorestart`); the compose CONFLICT-REFUSED note records that 1g reproduces the outage. Live steady 2.7GiB/7GiB. Heap = 70% of limit (within the ≤75% JVM rule). | 2026-07-02 |
| AUD-LOCAL-008 | Kafka 1G limit / 768m heap | 2500m limit / pinned 1G heap (`docker-compose.yml:262-271`, commit 55b5b118 AUD-INFRA-007) | Live measurement: broker steady RSS 1.42GiB **exceeds** the spec's 1G limit — a 1G cap OOM-kills the event backbone in normal operation (`docs/ops/local-memory-budget.md:36`). 1G/2.5g = 40% heap ratio. | 2026-07-02 |
| AUD-LOCAL-009 | Neo4j 512M container / 256+256 heap+pagecache | 1500m container / 512m heap + 256m pagecache (`docker-compose.yml:100-108`) | The spec conflated JVM heap with container limit: live RSS is 870MiB > the spec's 512M total, so 512m hard-caps OOM-kill neo4j on boot (compose CONFLICT-REFUSED note; `docs/ops/local-memory-budget.md:37`). | 2026-07-02 |
| AUD-LOCAL-010 | Two Bronze sinks @ 2GB each / 1g driver | ONE unified sink (`bronze_landing.py`) @ 7g container / 4g driver (`tools/dev/dev-bronze-streaming.sh:17-26,83,108`) | The unified single sink supersedes the two-sink model (unified-Bronze-landing, MERGED 2026-07-01); the 1g driver died with `java.lang.OutOfMemoryError` mid-drain after ~73k tasks (reproduced; script header). Measured steady 2.145GiB — BELOW the old 2×2GB aggregate; heap+offheap/7g ≈ 64%. Budget doc offers 2g-steady/4g-drain as an optional claw-back. | 2026-07-02 |
| AUD-LOCAL-011 | Spark transform jobs: 2G containers, 1g driver+executor | 7g container / 4g driver on all 35 run scripts, strictly sequential (commit 1eb6917d, PR #342; `docs/ops/local-memory-budget.md:48-73`) | Measured heap OOM at the spec's 1g driver on real data (silver-collector-event on the 9,916-order Shopify backfill). The refresh loop runs jobs strictly sequentially, so only ONE job's ~5GB is live at a time — the 7g cap does not multiply. Both values stay env-tunable (`SPARK_CONTAINER_MEMORY`/`SPARK_DRIVER_MEMORY`). | 2026-07-02 |
| AUD-LOCAL-012 | MinIO 256m / iceberg-rest 128m | MinIO 5g (`GOMEMLIMIT` 4500MiB), iceberg-rest 512m (`docker-compose.yml:148-156,440-444`) | Blueprint caps are physically un-runnable: live steady usage is minio 1.77GiB and iceberg-rest 222MiB (a Jetty JVM) — both already above the blueprint numbers. MinIO serves the entire Iceberg warehouse for Trino+Spark. Compose CONFLICT-REFUSED notes + `docs/ops/local-memory-budget.md:35,41`. | 2026-07-02 |
| AUD-LOCAL-013 | `ai` profile runs LiteLLM | LiteLLM block commented out — `--profile ai` is a no-op (`docker-compose.yml:188-218`, commit 3cddd2a1) | No active feature consumes the gateway (Ask-Brain/MCP not live yet); running it only burns memory. Documented deliberate disable with an explicit re-enable path (uncomment; stays profile-tagged `ai`). Spec marks the profile **defined but dormant**; the app seam (`@brain/ai-gateway-client` → `LITELLM_BASE_URL`) is unchanged. | 2026-07-02 |
| AUD-LOCAL-014 | `pnpm dev:up` ≈ 30s "compose up + bootstrap" | Documented multi-minute 8-step bring-up: preflight → postgres → migrations → health poll → Bronze sink → bootstrap → ONESHOT medallion refresh → apps (`tools/dev/dev-up.sh`) | Every extra step carries a documented cause: migrate-before-sink prevents the sink's cold-start JDBC crash (`dev-up.sh:19-22`); bootstrap restores LocalStack secrets (community LocalStack can't persist); the one-shot refresh exists so dashboards render honest-empty, not 500s (`dev-up.sh:15-17` — product rule "No empty charts as a success state"). Spec readiness expectation updated. | 2026-07-02 |
| AUD-PROD-005 | EKS API endpoint fully private | Public-but-CIDR-pinned to one operator /32 (`envs/prod/terraform.tfvars:14`, `modules/eks/main.tf:110-111`) | AUD-COST-009 (`modules/eks/main.tf:81-92`): a private-only endpoint had NO access path for the one-time kubectl/helm/argocd bootstrap (no bastion/VPN/SSM; GitHub runners are outside the VPC). Ratified as the go-live bootstrap posture with a documented flip-back-to-`[]` instruction once an SSM bastion or Client VPN exists — that follow-up remains scheduled. | 2026-07-02 |
| AUD-PROD-009 | Three buckets `brain-prod-{bronze,silver,gold}` | ONE warehouse bucket `brain-bronze-prod-{acct}` with Bronze/Silver/Gold as Iceberg namespaces (`modules/s3-iceberg/main.tf:65-99`; `envs/prod/bootstrap.tf:167-188`) | AUD-COST-016: the single Iceberg REST/Jdbc catalog has ONE warehouse root — local compose runs exactly this shape, and per-layer buckets would need per-namespace location overrides local never exercises (breaks local/prod parity). IAM still separates layers via namespace-prefix policies. `modules/s3-iceberg-medallion/main.tf:16-21` records "PROD NO LONGER USES THIS MODULE". See `docs/infra/naming-and-tagging.md` §3. | 2026-07-02 |
| AUD-PROD-016 | System MNG = t4g.medium ×1 fixed | 2–6 nodes, desired 3 (`envs/prod/terraform.tfvars:17-19`); instance type/on-demand match spec | The system group hosts 2 HA Karpenter controller replicas ("an autoscaler must not run on the Spot capacity it manages" — `helm/karpenter/README.md:15-24`) + CoreDNS/ArgoCD/KEDA/ALB-controller; a single t4g.medium cannot host them with any node-failure tolerance. Spec table updated to t4g.medium 2–6 (desired 3). | 2026-07-02 |
| AUD-PROD-017 | streaming/batch/trino are fixed managed node groups (streaming floor 2–3) | Bounded Karpenter Spot NodePools; streaming warm floor enforced via `WhenEmpty` consolidation + workload replica baseline (`helm/karpenter/values.yaml:55-88`, README:26-47) | AUD-COST-010: pools deliver the same instance types/capacity classes with consolidation-based cost recovery fixed MNGs can't; the design (incl. the two-mechanism streaming no-scale-to-zero) is documented in the chart README. Ratified as the superseding topology; no hard 2-node MNG floor added. | 2026-07-02 |
| AUD-PROD-018 | Node-group table has exactly 4 groups | Fifth tainted `ondemand` NodePool (t4g.xlarge on-demand, 0–2, `brain.platform/pool=ondemand:NoSchedule`) — `helm/karpenter/values.yaml:89-109` | AUD-COST-018: Neo4j is the identity SYSTEM OF RECORD (ADR-0004) on Community edition (no HA) — a Spot reclaim bounces the only replica. The tainted pool scales to zero when unused (no idle cost); Neo4j pins there via `helm/neo4j/values-prod.yaml` nodeSelector+toleration. Spec node table gains the ondemand row; no code change. | 2026-07-02 |
| AUD-PROD-020 | Iceberg `expire_snapshots` TTL = 90 days | 7-day snapshot TTL (`db/iceberg/spark/medallion_maintenance.py:53`, `bronze_maintenance.py:48`; weekly + daily crons in `helm/cronworkflows/values.yaml:142-148`) | AUD-PERF-013 (documented in `medallion_maintenance.py:47-53`): snapshot TTL ≠ data retention — `expire_snapshots` never deletes current-state rows; a long cutoff delivered NO retention while retaining every micro-batch snapshot (unbounded metadata/storage = mart_size × refresh cycles). The 24-month DATA retention contract is enforced separately as row/partition DELETEs (`bronze_raw_retention.py`, D4 window), and erasure REQUIRES short/zero snapshot TTL so erased rows aren't time-travel-readable (`bronze_maintenance.py:89-91`, ttl_ms=0 purge). | 2026-07-02 |
| AUD-PROD-023 | CD builds all 4 services unconditionally per push | Affected-only matrix build, FAIL-CLOSED; unaffected services keep their pinned immutable `repository@digest` (`.github/workflows/deploy.yml` — affected-set at :60-78, gitops bump skip at :271-285) | The fail-closed affected computation was itself a deliberate audit fix (comment at deploy.yml:64-67 records the prior silent-skip bug); build-once/promote-same-artifact is the documented posture. Deployment correctness preserved; saves CI/ECR cost. Residual risk (base-image/CVE rebuilds for untouched apps) noted — a periodic scheduled full rebuild is the recorded follow-up option. | 2026-07-02 |
| AUD-NAME-003 | EKS cluster named `brain-prod-eks` (spec example) | Cluster `brain-prod` (`modules/eks/main.tf:98`) — the whole discovery graph keys on it (`kubernetes.io/cluster/brain-prod`, `karpenter.sh/discovery=brain-prod`, interruption queue == cluster name) | `docs/infra/naming-and-tagging.md` §1+§3 canonize `brain-{env}` ({layer} omitted where the resource type implies it); the prod control plane is ALREADY APPLIED (account 668848431102) and an EKS rename = destroy/recreate + retag every subnet/SG + all helm/ArgoCD values. Spec example was stale vs the repo's ratified standard. | 2026-07-02 |
| AUD-NAME-004 | Node group named `brain-prod-streaming-ng` | Only MNG is `brain-prod-system` (`modules/eks/main.tf:210`); workload capacity = Karpenter NodePool CRs `streaming`/`batch`/`trino` (no env prefix — cluster-scoped CRs, single-env cluster) | AUD-COST-010 (`envs/prod/bootstrap.tf:113-114,136-139`) deliberately replaces fixed workload node groups with Karpenter Spot — the spec's named streaming NG has no counterpart **by design**. Karpenter-launched instances DO carry the 4 mandatory tags via EC2NodeClass `spec.tags`. NodePool naming now documented in `naming-and-tagging.md` §3. | 2026-07-02 |
| AUD-NAME-005 | Medallion bucket named `brain-prod-bronze` | `brain-bronze-prod-{account_id}` (`modules/s3-iceberg/main.tf:89`) — layer-first + account-id suffix, and the unified warehouse keeps the historical `-bronze-` name | `naming-and-tagging.md` §1 lists S3 medallion ordering (`brain-{layer}-{env}-{account_id}`) as a baked-in exception (stable identifiers; rename forces replacement + data migration); `s3-iceberg/main.tf:11-12` keeps `-bronze-` explicitly for parity with the local warehouse root (`s3://brain-bronze/`). Bucket is in the already-APPLIED set; a rename = create-new + copy + repoint every catalog/IAM/checkpoint reference. | 2026-07-02 |

## Related same-day doc updates (not CE items, but part of the same audit close-out)

- **AUD-PROD-022** — CD workflow renamed `main.yml` → `deploy.yml` (name:
  `deploy`); branch trigger **stays `[master]`** (repo default branch — the
  spec's `[main]` is stale; a prior audit fixed the dormant-CD `[main]`≠master
  bug).
- **AUD-NAME-006 / AUD-NAME-007 / AUD-PROD-015** — `naming-and-tagging.md`
  refreshed (single warehouse bucket, no data-bucket Object Lock, Aurora
  Serverless v2 rows, KMS alias rows + §1 ordering exception, Karpenter
  NodePool rows, 3-AZ VPC rationale + PrivateLink tradeoff).
