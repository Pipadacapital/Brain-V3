# Engineering Advisor — Journal

> Append-only. See /Users/rishabhporwal/.claude/plugins/cache/engineering-os/engineering-os/2.3.1/docs/role-empowerment-model.md for entry shape.

## 2026-06-15T07:19:27Z — system — bootstrap
**Action:** Journal initialized by /eos-init on 2026-06-15T07:19:27Z.

## 2026-06-15T12:00:00Z — Engineering Advisor (cto-advisor) — context-sync/doc-08-v1.5
**Stage:** context-sync · **Action:** Foundation-amendment assessment (doc-03 §5 + doc-08 §36/§37) · **Decision:** PARTIALLY WARRANTED
**Rationale:** Absorb §37 field-complete dict + 5 reserved domains + connector-registry ext as context (no Canon change). Two targeted notes warranted: METRICS.md money-rule += reporting_currency_value_minor approved FX-normalization pattern (doc 08 §36 Delta 1); TRIGGER-SURFACES.md compliance row += tax_regime enum/region are live Phase-1 model fields requiring the compliance surface lane on any change. All other Canon files (STACK, HLD, INVARIANTS, COMPLIANCE) are NO-CHANGE. GCC GTM stays Phase 5; Iceberg+StarRocks ADR-002 confirmed. · **Next:** Stakeholder approves or rejects the two AMEND recommendations; if approved, a Foundation amendment edits METRICS.md + TRIGGER-SURFACES.md.

## 2026-06-17T02:35:00Z — Engineering Advisor (cto-advisor) — feat-analytics-api-dashboard
**Stage:** 1 (compressed, personas folded) · **Action:** Intake + adversarial stress pass · **Personas:** none spawned (folded) · **Decision:** ADVANCE
**Rationale:** All blockers shipped (feat-metric-engine-parity facacfe + full ledger spine). 4 HIGH findings: (1) `realized-revenue.ts:71` `?? '0'` creates a `0n` that is indistinguishable from "no finalized rows" — D-2 requires explicit `state:'no_data'` discriminant; (2) `analytics/index.ts` stub + BFF direct-query pattern risks ad-hoc SUM regression — D-3 sole-read-path must be architecturally enforced and tested; (3) 8 prior BFF envelope mismatches — D-1/D-5 binds exact `{request_id,data}` envelope and `const {data}=` unwrap in client; (4) dev superuser masks RLS — D-6 isolation test must use `brain_app` NOBYPASSRLS (already wired in CI from parity gate). 2 MEDIUM: provisional-separate (D-4), money formatting utility (D-7). 12 architect bindings declared (D-1..D-12). Tier-0 deterministic throughout. No new deployable. · **Next:** Architect (Stage 2) — bindings D-1..D-12 are the contract

## 2026-06-16T00:00:00Z — Engineering Advisor (cto-advisor) — feat-access-onboarding-flow
**Stage:** 1 · **Action:** Intake review + Canon-conflict reconciliation · **Personas:** Identity & Session Abuse Red-teamer:sonnet, Scope & Product-Realism Skeptic:sonnet · **Decision:** ADVANCE (scoped)
**Rationale:** Spec names Authentik OIDC, Google one-tap, MFA, Redis sessions, rotating refresh tokens — all Canon-conflicting with D0.1 (app-native M1 sealed). Scoped 10 acceptance criteria that fix the Stakeholder's tested gaps within the app-native stack and no new infrastructure: rotating refresh tokens (Postgres-only AC-1), membership-remove/suspend revocation (AC-2), rate limiting via Redis CacheAdapter (AC-3), brand schema additions currency/timezone/revenue_definition (AC-4), onboarding progress persistence column on organization (AC-5), 4-step wizard Step 3+4 (AC-6), invited-email sign-up guard (AC-7), multi-org selector (AC-8), session context improvements (AC-9), audit coverage gaps (AC-10). 3 Stakeholder decisions required (SD-1: confirm Authentik/Google/MFA deferred; SD-2: rotating refresh now; SD-3: revocation-on-role-change policy). 5 items deferred as child requirements. Lane high_stakes confirmed + schema_changes added. · **Next:** Orchestrator spawns 2 sonnet personas; synthesis pass → Architect (Stage 2) on ADVANCE

## 2026-06-15T21:37:25Z — Engineering Advisor (cto-advisor) — feat-access-onboarding-flow
**Stage:** 1 (synthesis pass) · **Action:** Persona synthesis (02a identity-abuse + 02b scope-realism) → 02c-intake-synthesis.md · **Decision:** ADVANCE
**Personas synthesized:** Identity & Session Abuse Red-teamer:sonnet (9 concerns, 2 CRITICAL, 4 HIGH, 2 MED, 1 LOW) + Scope & Product-Realism Skeptic:sonnet (7 concerns, 3 HIGH, 3 MED, 1 LOW). No concern dropped. All CRITICAL/HIGH folded into ranked must-address list (MA-01 through MA-16). 10 finalized ACs with persona modifications incorporated. Canon amendment: conditional only — none required if `revenue_definition` CHECK constraint ships without `placed` in M1; METRICS.md amendment needed only if `placed` is included. Lane high_stakes CONFIRMED. Build tracks: backend-developer + frontend-web-developer.
**Rationale:** Both CRITICALs are implementation-absent gaps (refresh endpoint does not exist; revocation not wired into service layer). 6 HIGHs cover harden-the-CRITICAL-path concerns plus design-level issues the Architect must resolve before migration schema is committed (MA-09 onboarding_status placement, MA-12 revenue_definition enum). 5 MEDs are correctness/maintainability issues within same sprint.
**Next:** Architect (Stage 2) — resolve MA-09 binding option decision + MA-12 before schema migration; then build tracks backend + frontend in parallel.

## 2026-06-15T11:09:57Z — Engineering Advisor (cto-advisor) — chore-platform-foundations-sprint0
**Stage:** 1 · **Action:** Intake review · **Personas:** Sprint-0 Over-Engineering Skeptic:sonnet, Isolation + Secrets Hardness Skeptic:sonnet · **Decision:** ADVANCE
**Rationale:** Requirement is sound — problem/user/success metric/constraints all Canon-aligned; no INVARIANT violated; no frozen ADR re-opened. Lane confirmed high_stakes with 6 trigger surfaces (scan's multi_tenancy + 5 added: schema_changes, system_of_record_audit, secrets_auth_iam, iac, shared_contract_parity). Five challenge findings raised as Architect directional input: Sprint-0 2-week cap risk (C1), StarRocks row-policy gap (C2), dbt over-scope (C3), DQ framework scope ambiguity (C4), pixel scope in CI (C5). Four "make it less dumb" findings: Authentik on EKS deferral candidate, LiteLLM gateway deferral candidate, staging/prod full-apply scope ambiguity, output-format framing clarification. No escalation triggers met.
**Next:** Persona synthesis (2 sonnet personas spawn in parallel via orchestrator), then ADVANCE to Architect.

## 2026-06-16T00:27:32Z — Engineering Advisor (final-reviewer) — feat-access-onboarding-flow
**Stage:** 6 · **Verdict:** PASS (GO) · **Paradigm audit:** clean (Tier-1, $0 model spend)
**Gates re-run (captured on this machine):** full suite 75/75 turbo + core 74/74 (family-wipe.live 3/3 NON-skipped, 53ms); validity_check exit 0; **family-wipe under brain_app: set_config=3 rows / no-GUC=0 rows (real negative control — the regressed path)**; Playwright 3/3 live (4-step + ghost-404 + resume); BFF rate-limit single-count 1-5=401/6=429 (Redis counter=6); brand CHECK rejects placed+USD; tenant-isolation no-GUC=0/wrong-WS=0/brain=16.
**Acceptance:** 11 MET / 0 scaffold / 0 gap · open CRITICAL/HIGH = 0 · Stakeholder complaint MET (4-step + currency/timezone + resume).
**Scope/Canon:** all 5 deferred items OUT; +1 sanctioned dep (ioredis); no new table/service; no hard-rule deviation. Honesty spot-check: r2 false-done /onboarding/advance now genuinely registered (bff.routes:363).
**Auto-rule:** first-occurrence root cause (inert test masked live-PG RLS defect) — below ≥3 threshold; watch-item only, no rule-proposal written.
**Next:** Stakeholder gate (Stage 7).

## 2026-06-16T04:38:00Z — Engineering Advisor (cto-advisor) — feat-multi-brand
**Stage:** 1 · **Action:** Intake review (design reconciliation + RLS gap analysis + scope definition) · **Personas:** Brand-Switch-Abuse Isolation Red-teamer:sonnet, Scope-and-Product-Realism Skeptic:sonnet · **Decision:** ADVANCE
**Rationale:** Two prerequisites shipped and verified. Three key design decisions resolved: (1) set-brand is a NEW BFF endpoint — not an extension of set-org — because the membership check is the 3-arg brand-level form and the audit event is distinct; (2) no schema change needed — the 3-GUC session already carries brand_id/workspace_id/role and re-mint is a read+JWT-sign operation; (3) brand_self_read RLS policy is MISSING (P0 blocker — brand_isolation only allows reads when GUC=brand_id, so list-my-brands returns zero rows under brain_app without it). Migration 0013_brand_self_read.sql is required in this slice. 7 findings surfaced; F-1 (missing RLS) and F-3 (missing endpoint) are P0. No Canon amendment. 2 Stakeholder decisions required (route URL shape, dashboard summary focus).
**Next:** Orchestrator spawns 2 sonnet personas in parallel; synthesis pass → Architect (Stage 2) on ADVANCE

## 2026-06-16T05:20:00Z — Engineering Advisor (cto-advisor) — feat-multi-brand
**Stage:** 1 (synthesis pass) · **Action:** Persona synthesis (02a brand-switch-abuse 10 concerns + 02b scope-realism 7 concerns) → 02c-intake-synthesis.md · **Decision:** ADVANCE
**Personas synthesized:** Brand-Switch-Abuse Isolation Red-teamer:sonnet (10 concerns: 2 CRITICAL, 4 HIGH, 3 MED, 1 LOW) + Scope-and-Product-Realism Skeptic:sonnet (7 concerns: 3 HIGH, 2 MED, 2 LOW). No concern dropped. All CRITICAL/HIGH folded into ranked must-address list (MA-01 through MA-08). Full list MA-01 through MA-15 (2 CRITICAL, 6 HIGH, 4 MED, 3 LOW).
**Rationale:** Both CRITICALs address the same root defect from different attack angles: (C1-abuse + C3-realism) both confirm that `refreshSession`/`resolveActiveContext` cannot be used for set-brand because there is no `preferredBrandId` parameter and the fallback `findActiveByUser` selects the wrong brand. Consolidated into MA-01: use `mintSessionToken` directly with explicitly resolved `ActiveContext`. (C2-abuse) cross-org workspace spoofing via body → MA-02: workspaceId from JWT only. 6 HIGHs address role carry-over (MA-03), RLS policy workspace scoping + soft-delete trap + NN-1 (MA-04), sessionPreHandler mandate (MA-05), brand-summary cache invalidation + adapter fix + BFF scoping (MA-06), M1 org-level invariant documentation (MA-07), create-brand routing isolation (MA-08). 7 finalized ACs with all persona modifications incorporated. Canon: no amendment. Lane high_stakes confirmed. Build tracks: backend-developer + frontend-web-developer in parallel after migration 0013 deploys.
**Next:** Architect (Stage 2) — resolve implementation order: 0013 migration first; then AC-1 set-brand endpoint; then AC-3/AC-4/AC-5 frontend tracks in parallel.

## 2026-06-16T10:56:00Z — Engineering Advisor (final-reviewer) — feat-multi-brand
**Stage:** 6 · **Verdict:** PASS · **Recommendation:** APPROVE · **Paradigm audit:** clean (Tier-0 deterministic, 0 model calls; Single-Primitive reuse; no over-engineering)
**ONE invariant (I-S01):** HOLDS — independently re-ran isolation-fuzz (11/11 PASS, NOSUPERUSER NOBYPASSRLS, cross-brand connector read=0, negative-control policy_off=1/policy_on=0 non-inert). SEC-MB-1 body-spoof closed (organizationId from JWT). 0013 live in pg_policies.
**Gates re-run (≥3, captured):** isolation-fuzz pg.test.ts 11/11 · switch-brand.live.test.ts 4/4 (incl 2 negative controls) · critical-paths.test.ts 22/22. Typecheck core+web EXIT 0.
**Both VETO gates:** FAIL→bcfee81→DELTA PASS legitimate, blocking:false on both; negative_control[] present + non-inert.
**Deferred:** QA-3 (audit correlation_id column) ship-as-techdebt; SEC-MB-4 (audit-after-mint, LOW) ship-as-techdebt. Neither touches isolation/auth.
**Scope creep:** none (no new migration/endpoint/secret beyond plan; STACK/ADR unchanged). **No AC unmet.**
**Commit-hygiene finding (action for gate):** spine UNCOMMITTED in working tree — 0013_brand_self_read.sql (untracked) + bff.routes.ts + auth.service.ts + brand.service.ts (modified). Mechanical commit command provided (11-final-review.md §7); not a code defect.
**Auto-candidate rule:** NOT fired — "tenant/workspace id from body not JWT" root-cause class recurs in 2 distinct runs (this + feat-access-onboarding-flow set-org); below ≥3 threshold; recorded as watch-item, no rule-proposal written (human runs /adopt-rule).
**Next:** Stakeholder gate (/approve). Intended state: stage 6 complete, awaiting-stakeholder, owner=stakeholder. Did NOT advance to Stage 7/deploy.

## 2026-06-16T20:40:00Z — Engineering Advisor (final-reviewer) — feat-members-team-management
**Stage:** 6 · **Verdict:** PASS · **Recommendation:** APPROVE · **Paradigm audit:** clean (Tier-0 deterministic, 0 model calls; Single-Primitive reuse — no second suspend mechanism; no over-engineering)
**Requirement:** DELIVERED — lifecycle Invited→Active→Suspended→Removed operable end-to-end (invite/accept/role/suspend/reactivate/remove + pending resend/revoke), hierarchy-enforced, e2e 4/4 standalone.
**Security spine (spot-verified by READING code, not trusting reports):** createInvite D-6 bound (invite.service.ts:97-100); suspendUser atomic single rawPgPool BEGIN/COMMIT + actor/target authority from DB + owner block + D-9 org assertion + post-COMMIT audit brand_id correct (auth.service.ts:766-868); reactivateUser distinct, no session revoke (875-953). LIVE brand_admin→brand_admin escalation CLOSED (403, wire-tested WIRE-2).
**Gates re-run (captured on this machine):** vitest workspace-access 69/69 PASS (6 files; 3 wire tests executed not skipped) — reproduced QA delta exactly. core typecheck EXIT 0; web typecheck EXIT 0. 43ea557 is-ancestor → true.
**Both VETO gates legitimate:** Security PASS (0 blocking; 5 CRITICAL + 3 HIGH closed). QA FAIL→fcbc221(test-only, 2 files)→DELTA PASS; WIRE-1 read at :366 confirmed non-inert (live session, 200-before control, real suspendUser, 401 SESSION_REVOKED after). Real fix, not a wave-through.
**Deferred dispositions:** SEC-V1 ship-as-techdebt (functional claim effectively resolved by WIRE-1; cross-org isolation independently proven NC-1/2/3 + WIRE-3 under brain_app — residual is two assertion queries' hygiene, not a runtime risk); SEC-V2 LOW ship-as-techdebt (dead path); F-QA-4 ship-as-techdebt (audit correlation_id column, pre-existing schema, needs migration); F-QA-5 ship-as-techdebt (rate-limiter test-infra, pre-existing). None touch the security spine.
**Scope/Canon:** no scope creep (diff confined to workspace-access + apps/web members surface + contracts + 0014 + run artifacts); no new ADR/stack/secret/dep; migration 0014 additive (I-E02); no Single-Primitive violation; no hard-rule deviation. No AC unmet.
**Auto-candidate rule:** "wire-level assertion required for auth/isolation claims (not DB-row/service-throw proof)" — root cause of r1 bounce; below ≥3-distinct-prior-runs threshold; watch-item only, NO rule-proposal auto-written (human runs /adopt-rule). Dev-superuser-masks-RLS durable rule honored end-to-end.
**Next:** Stakeholder gate (Stage 7). Intended state: stage 6, awaiting-stakeholder, owner=stakeholder. Did NOT advance to deploy / did NOT write state/active.json.

## 2026-06-16T21:20:00Z — Engineering Advisor (final-reviewer) — feat-data-plane-ingest-spine
**Stage:** 6 · **Verdict:** PASS · **Recommendation:** APPROVE · **Paradigm audit:** clean (deterministic-only, 0 model spend; Single-Primitive — one spool/one dedup/one Bronze table, brand_id at every layer; no over-engineering; M1 Zod-local validate is the sanctioned simplification 02 §133)
**Requirement:** DELIVERED — doc 05 §14 exit met: hello-world event flows collector→Redpanda→Bronze behind RLS, contracts regenerated. Full-wire e2e (pipeline-wire.e2e.test.ts, dcf2d55) proves it end-to-end (real subprocess, real TCP, real Redpanda, in-proc consumer, brain_app asserted) — non-inert.
**Spine spot-verified by READING code (not trusting reports):** accept-before-validate structural (collect.route + accept-event.usecase import zero kafka; ACK=spool INSERT commit pre-200); offset-commit-after-write (CollectorEventConsumer autoCommit:false, commit only on written/dedup/DLQ); RLS write path BEGIN→set_config(...,true)→INSERT ON CONFLICT→COMMIT as brain_app; dedup Redis NX + PK backstop.
**Gates re-run (captured on this machine):** stream-worker lint EXIT 0 (SR-01 closed); stream-worker typecheck EXIT 0; live RLS shape (bronze_events rls=t force=t, two-arg policy, INSERT+SELECT only); LIVE fail-closed under SET ROLE brain_app reproduced by me — no-GUC=0, wrong-brand=0, correct-brand=1, superuser=1 (false-pass trap proven); iceberg diff empty + 0015/0016 status A.
**Both VETO gates legitimate:** Security FULL BOUNCE SR-01 (real ESLint NN-7 CI failure, test-only) → 3567196 → DELTA PASS, migrations untouched. QA FULL FAIL F-QA-01 (real missing full-wire e2e) → dcf2d55 (additive) → DELTA PASS. Both bounces = gates working; neither fix weakened isolation/auth.
**D-4 amendment SOUND:** Postgres bronze_events is the pre-authorised D-4 fallback (02 §119), amendment on ADR-003, NOT a new stack layer/ADR. Iceberg target untouched. Header-marked dev/M1 staging mirror.
**Deferred dispositions:** SR-02 ship-as-techdebt (null-GUC test precision; production fail-closed re-proven live by me — test hygiene, not runtime risk); SR-03 ship-as-techdebt-CONDITIONAL (spool unvalidated body — acceptable ONLY under M1-internal no-external-traffic; must-fix before external exposure); SR-04/F-QA-05 rate-limit ship-as-techdebt-CONDITIONAL (same M1-internal caveat); F-QA-04 cosmetic. None touch the live-proven RLS spine.
**Scope/Canon:** no new ADR/STACK/docs; only 2 existing deployables touched; no hardcoded secret; migrations additive (I-E02); no Single-Primitive violation; no hard-rule deviation; no AC unmet.
**Auto-candidate rule:** none written — no root cause repeating ≥3 distinct prior runs (SR-01 lint-key + F-QA-01 wire-gap are run-local). Dev-superuser-masks-RLS durable rule honored end-to-end (every isolation probe under brain_app, superuser-trap explicitly proven).
**Next:** Stakeholder gate (Stage 7). Intended state: stage 6, awaiting-stakeholder, owner=stakeholder. Did NOT advance to deploy / did NOT write state/active.json.

## 2026-06-16T22:40:00Z — Engineering Advisor (final-reviewer) — feat-identity-graph
**Stage:** 6 · **Verdict:** PASS · **Recommendation:** APPROVE → Stakeholder gate · **Paradigm audit:** clean (Tier-1 deterministic, zero model calls, @effort present)
**Gates re-run (independently captured):** identity e2e 26/26 PASS (79ms); identity-core + stream-worker typecheck EXIT 0; RLS no-GUC identity_link=0 rows under SET ROLE brain_app; re-eval brand visibility superuser=171 vs brain_app=0 (SR-01 root cause confirmed at DB).
**Spine verified (code-read):** per-brand 32B salt hard-fail no-default (SaltProvider:88-123); no raw PII in identity_link (hash-only, raw→contact_pii); contact_pii dual-GUC gate (0017:239-244); RLS FORCE all 8 tables + NN-1 block; idempotent merge (deterministic merge_id + ON CONFLICT DO NOTHING). Negative controls real/non-tautological (superuser-sees vs brain_app-doesn't reproduced).
**Over-engineering/hard-rule:** CLEAN — only new dep @types/node; no new deployable/ADR/stack/argo; migration additive; merge_rule/merge_candidate/pii_vault_reference deferred. No hard-rule deviation; no Stakeholder-stop.
**Re-eval-gap call (SR-01/QA-04 MED):** phone-guard-reeval runs as brain_app and enumerates 0 brands under FORCE RLS → suppressions never expire, over-suppression accumulates. Fail-closed (no false-merge/leak), recoverable via fix+replay. ACCEPT as M1 techdebt; P1 must-fix before prod scale. Misleading comment (reeval.ts:41-42) claims an unwired superuser pool.
**Retro:** root cause = cross-tenant system job written against tenant-scoped brain_app under FORCE RLS (related to dev-db-superuser-masks-rls.md note, opposite direction). First occurrence in series — NOT ≥3-run recurrence → no rule-proposal; logged for watch.
**Next:** Stakeholder gate (Stage 7) — weighs the reeval-suppressions-never-expire gap as the sole residual risk.

## 2026-06-16T19:56:00Z — Engineering Advisor (final-reviewer) — feat-realized-revenue-ledger
**Stage:** 6 · **Verdict:** PASS · **Recommendation:** APPROVE → Stakeholder gate · **Paradigm audit:** clean (tier-0 deterministic, zero model calls — correct for a money substrate)
**Gates re-run (independently captured):** ledger live suite 30/30 PASS (306ms); append-only-by-GRANT — UPDATE+DELETE as brain_app (non-superuser, is_super=f) → permission denied, grants=SELECT,INSERT only; closed-sum NON-TAUTOLOGY live — realized_gmv_as_of=100000 vs naive SUM=200000 (DIFFER → fn load-bearing); closed-sum nets — +finalization/−refund → 0; single-currency trigger — AED into INR brand RAISEs. brand+ledger both FORCE RLS confirmed.
**Heart verified (code+live):** 4 load-bearing walls real — append-only by GRANT (not convention), no-double-count named fn excludes provisional, dual-date late-reversal posts new current-period row (June untouched), no-float BIGINT (Assertion-3 + grep clean). Negative controls non-tautological/non-inert (5 probes captured RED). Dedup index correctly hardened to timezone('UTC',occurred_at)::date (IMMUTABLE) vs plan's STABLE occurred_at::date.
**Over-engineering/scope/hard-rule:** CLEAN — additive 0018; no new deployable/ADR/stack/queue/Argo-type; only packages/money addition is roundToMinorBankers (1 fn); non-goals billing_run/fx_rate honored; doc-08 §7.1 date→CHAR(7) is an operational tightening (journaled, NOT a Canon amendment). No hard-rule deviation.
**F-SEC-01 call (HIGH, deferred):** revenue-finalization no-ops under brain_app FORCE-RLS brand enumeration → no provisional ever finalizes. FAIL-CLOSED (understate never overstate, no leak/double-count), recoverable (fix+replay; rebuildable from Bronze), no M1 consumer (metric engine is next slice). ACCEPT as M1 techdebt; P1 must-fix before prod (SECURITY DEFINER enumeration fn / superuser-scoped pool). Stakeholder owns the conscious accept.
**Retro / recurrence:** root cause = cross-tenant system job enumerating FORCE-RLS `brand` under brain_app with no GUC → 0 rows → silent no-op. This is the SAME pattern as identity phone-guard-reeval (run c9a1a0, SR-01/QA-04) — now **2nd distinct occurrence**. The identity final-review explicitly logged "first occurrence, no rule-proposal, logged for watch." Now at 2 of the ≥3 auto-write threshold → I do NOT adopt a rule; I RECOMMEND the Stakeholder file one and added it to pending-stakeholder-attention.md. A 3rd occurrence crosses the auto-candidate bar.
**Next:** Stakeholder gate (Stage 7) — weighs F-SEC-01 conscious-accept + the recommended rule-proposal; deploy is affected-only core+stream-worker + finalization manifest, NO canary.

## 2026-06-17T01:48Z — Engineering Advisor (final-reviewer) — feat-metric-engine-parity
**Stage:** 6 · **Verdict:** PASS (APPROVE) · **Paradigm audit:** clean (tier-0, zero model calls verified)
**Gates re-run:** typecheck (engine+oracle EXIT 0); test:parity 16/16 GREEN + live RED PROOF captured (TS=50001 vs 50000 delta=1 FAIL@tol0 -> revert PASS); test:unit 9/9; turbo --affected --dry-run (both pkgs in scope); validity_check clean (9 files). All replicated by reviewer.
**Heart spot-verified at source:** reference.ts imports ONLY `pg` type (the 2 metric-engine hits are comments), raw SQL `recognition_label='finalized' GROUP BY currency_code` — genuinely non-tautological. RED proof = hard assertions (toBe(false)/toBe(1n)). CI pr.yml now provisions postgres:16 + brain_app NOBYPASSRLS + migrate:up BEFORE test:parity (line 61<69), no continue-on-error — SEC-001 bounce was a REAL fix not a wave-through. Engine reads ONLY via realized_gmv_as_of inside withBrandTxn (correct-by-construction).
**Deferred:** F-SEC-02 (old GetRealizedGmvAsOf autocommit-GUC gap) — re-confirmed still unwrapped at source; NOT regressed (new path uses withBrandTxn); ship as carried tech-debt, must-fix-before-Phase-2. SEC-003/QA-F1/QA-F2 LOW -> ship, M2.
**Recurrence / auto-candidate:** root cause this run = none (clean money/parity path; the prior FORCE-RLS-enumeration pattern at 2/3 does NOT recur here — engine reads per-brand under GUC, no cross-tenant enumeration). No new rule-proposal from this run.
**Hard-rule check:** no dependency/Single-Primitive/compliance/paradigm/gate-skip deviation. Scope honored (only realized+provisional; no new deployable; 0020 additive; eslint fence = bug-fix).
**Next:** Stakeholder gate (Stage 7) — weighs F-SEC-02+SEC-003 carried tech-debt + QA M2 deferrals + the merge of feat/metric-engine-parity.

## 2026-06-17T08:00:00Z — Engineering Advisor (cto-advisor) — feat-connector-marketplace
**Stage:** 1 (high-stakes, personas folded) · **Action:** Intake + adversarial stress pass (4 personas inhabited) · **Personas:** none spawned (folded per compressed high-stakes lane) · **Decision:** ADVANCE
**Rationale:** All blockers shipped (feat-m1-app-foundation chain, feat-access-onboarding-flow, feat-analytics-api-dashboard). 12 binding decisions declared (D-1..D-12). CRITICAL: brand_id ONLY from signed state nonce, never from body (D-1) — the Shopify impl already has this correct; the risk is in the generalization. HIGH: ISecretsManager interface is Shopify-shaped and must be generalized before the generic connect command is written (D-3); UNIQUE constraint (brand_id, provider) must be consciously extended or accepted as one-per-provider for M1 (D-4 note). HIGH: 7-state health as TWO COLUMNS on connector_instance (not a new table) per simplicity-first (D-4). HIGH: Coming-Soon types rejected server-side by reading catalog before dispatch (D-5). Key clarification: requirement text uses "oauth_token_ciphertext" language — this CONTRADICTS NN-2/I-S09; tokens are in Secrets Manager, ARN only in Postgres; no ciphertext column in the DB. Audit log entries for connect/disconnect established now (D-11) even though sha256-chain debt is still open. Detector/backfill/live-sync deferred boundary is a hard line (D-12). Dev superuser masks RLS carry-in: isolation test must use brain_app. Paradigm: tier-0 deterministic, $0/month model spend.
**Next:** Architect (Stage 2) — resolve UNIQUE constraint shape, catalog entry TypeScript interface, generic ISecretsManager signature, dispatch table design, migration number, ConnectorType discriminated union in packages/contracts BEFORE any route handler is written. Tracks: backend-developer + frontend-web-developer in parallel after migration + contracts committed.

## 2026-06-17T07:12:00Z — Engineering Advisor (final-reviewer) — feat-analytics-api-dashboard
**Stage:** 6 · **Verdict:** PASS / APPROVE · **Paradigm audit:** clean (Tier-0 deterministic throughout, zero model calls verified)
**Gates re-run (≥3, captured at source):** @brain/core typecheck EXIT 0; @brain/web typecheck EXIT 0; revenue-metrics.live.test.ts 20/20 against REAL postgres:16 with BRAIN_APP_DATABASE_URL — pg_roles confirms brain_app NOSUPERUSER/NOBYPASSRLS; negative-control current_user='brain_app'+is_superuser=false replicated, cross-brand BRAND_B query returns no_data (BRAND_A value never leaks). e2e 4/4 inherited-green from QA DELTA (Playwright stack not stood up here; rides the same BFF→engine path the live suite exercises).
**Heart spot-verified at source:** sole-read-path — grep SUM/reduce across analytics module + BFF route + web card = ZERO executable hits (comments/assertions only); numbers come ONLY from computeRealized/ProvisionalRevenue, the lone extra SQL is EXISTS(finalized) (existence, not value). Honest-empty driven by EXISTS gate not the ??'0' value (get-revenue-metrics.ts:80). Isolation: brand from auth.brandId not body; rawPgPool threaded (main.ts:314→bff.routes.ts:66, raw pg.Pool not DbPool — F-SEC-02 not regressed, no double-GUC). Envelope: client unwraps .data (client.ts:757). Money: realized/provisional in separate blocks, never summed; formatMoneyDisplay bigint-split, no parseFloat/inline-/100.
**Over-engineering:** CLEAN — delivered set == plan §2/§5/§6 named artifacts; no new service/package/queue/ADR; the one new formatter wraps the ONE @brain/money model (not a duplicate).
**Deferred / risks remaining:** LOW-SEC-001 (semantic as_of month=13 → RangeError at get-revenue-metrics.ts:61 → 500 not 400; not exploitable, no leak — path verified in code) · QA-F-002 (Number(decimalString) display-only after bigint split, precision-safe ~₹90T, satisfies D-7) · F-SEC-02 carried (not regressed) · QA-3 carried. All ship as tracked tech-debt.
**Recurrence / auto-candidate:** root cause this run = QA-F-001 stale test-fixture table name (app_user_org_membership vs membership), fixed 709cb2c, DELTA 4/4. Zero prior occurrences in lessons-learned → below ≥3 threshold → NO rule-proposal written.
**Hard-rule check:** no dependency/Single-Primitive/compliance/paradigm/gate-skip deviation. Scope honored (only realized+provisional; no new deployable; zero migrations; read-only).
**Next:** Stakeholder gate (Stage 7) — M1 vertical spine complete end-to-end; weighs LOW-SEC-001 + QA-F-002 + carried F-SEC-02/QA-3 as tracked debt + the branch merge.

## 2026-06-17T10:30:00Z — Engineering Advisor (final-reviewer) — feat-connector-marketplace
**Stage:** 6 · **Verdict:** PASS / APPROVE · **Paradigm audit:** clean (Tier-1 deterministic, zero model calls in diff, $0/mo)
**Gates re-run (≥3, captured this session):** @brain/core typecheck EXIT 0; @brain/web typecheck EXIT 0; connector unit suite `vitest run src/modules/connector --exclude **/*.live.test.ts` 35/35 PASS EXIT 0 (incl. SecretRef KMS non-inert negative control "goes RED when KmsKeyId absent"). Backend 189/189 + marketplace e2e 6/6 + full-journey 1/1 carried green from QA DELTA. Live DB suite (isolation/forged-body/authz/audit) NOT re-executed (no psql/brain_app in sandbox) — verified at source for non-inertness instead.
**Spine spot-verified at source:** D-1 — `query['brand_id']` inline callback DELETED in main.ts (diff shows removed lines); generic `/api/v1/oauth/callback/:type` derives brand_id ONLY from `consumeAndGetBrandId(state)`; grep confirms NO surviving request-body/query brand_id read. Token secrecy — AwsSecretsManager binds `KmsKeyId` (CMK) on both CreateSecret calls (:84/:155), main.ts:363 + LocalSecretsManager prod-hard-fail; MarketplaceTile contract + marketplace GET response carry NO secret_ref/token (secret_ref lives only in internal ConnectorInstanceSchema, never crosses BFF). Isolation — 0021 additive (0006 untouched, RLS two-arg FORCE untouched, NN-1 assertion carried); live test asserts current_user==='brain_app' + cross-brand count===0 (non-inert, dual-pool). Authz — connect/disconnect manager+, backfill brand_admin+ 501 stub, analyst 403; marketplace read analyst+ (render≠connect). Envelope D-10 — connectorsApi.list() derives from getMarketplace() (no raw.data.shopify), no 9th mismatch. Deferred boundary D-12 — grep CLEAN (only 501 backfill gate references backfill). Honest marketplace — 3-layer coming-soon gate (HTML disabled + JS early-return + server 422 keyed on availability).
**Real-store smoke:** live Shopify connect vs Boddactive — connector_instance Healthy, has_secret=true (token as ref, none in response), audit_log connector.connected written, sync_status=waiting_for_data (honest).
**Over-engineering:** CLEAN — delivered set == plan (catalog 4 files, 0021, secrets seam, generic routes, contracts, web marketplace); no new deployable (core+web only); catalog static (9 tiles/7 cats); health = 2 columns not a table; no IConnector/BaseConnector/plugin-registry/connector_definition (the 2 grep hits are comments documenting avoidance).
**Deferred / risks remaining:** SEC-CM-RES-01 (MED, M2 — single shared CMK; per-brand isolation via IAM/key-policy not a per-brand key; adjudicated acceptable M1) · KNOWN-CM-01 (LOW — UNIQUE(brand_id,provider) one-per-provider) · Scale-C4 (LOW — InProcessOAuthStateStore single-instance, Redis path reserved) · Sec-C3 (non-goal — no provider-side revocation on disconnect). Minor non-blocking: health literals normalized to no-space form (consistent across migration/contract/entity/UI); QA verdict negative_control[0].path string off (file at connector/tests not shopify/tests — exists, non-inert).
**Recurrence / auto-candidate:** root cause this run = stale legacy client (connectorsApi.list()) reading old raw.data.shopify shape after backend endpoint swap (envelope-unwrap theme). Distinct prior runs sharing precise mechanism = 2 (members-team-management, analytics-api-dashboard) — below ≥3 threshold; theme already guarded procedurally (per-run "no Nth-mismatch" D-item + QA VETO). NO rule-proposal written. Watch item logged in retro: a 3rd occurrence → promote "endpoint-shape-change = enumerate-and-repoint-all-consumers".
**Hard-rule check:** no dependency/Single-Primitive/compliance/paradigm/gate-skip deviation. Single-Primitive clean (one secrets seam, one audit writer, one RBAC guard, one state store, one connector_instance +2 cols, one catalog SoR, one OAuth callback path).

## 2026-06-17T10:45:00Z — Engineering Advisor (cto-advisor) — feat-connector-backfill
**Stage:** 1 (high-stakes, personas folded/inhabited) · **Action:** Intake + adversarial stress pass (6 personas inhabited) · **Personas:** none spawned (folded per compressed high-stakes lane) · **Decision:** ADVANCE
**Rationale:** All 6 upstream blockers shipped and verified. 15 binding decisions declared (D-1..D-15). Critical findings: (D-5) event_id must be deterministic sha256(brand_id||shopify_order_id||schema_name) not randomUUID; (D-6) occurred_at in ledger = Shopify processed_at not NOW() — otherwise finalization horizon misfires for weeks; (D-4) worker emits directly to Redpanda, not through collector HTTP spool; (D-3) two-lane = separate topic not same-topic different consumer group; (D-10) PII strip at worker boundary (no raw email/phone in events or Bronze). High findings: (D-8) estimated_total from countOrders with date filter before first page; (D-9) overlap-lock DB-level not in-process; (D-11) gross-of-fees label mandatory on dashboard; (D-13) integer arithmetic for amount_minor conversion not parseFloat. Dev secret durability resolved: D-7 = reconnect-before-backfill protocol (check getSecret before accepting trigger; return 409 RECONNECT_REQUIRED if null). Worker placement bound: D-1 = stream-worker job (apps/stream-worker/src/jobs/shopify-backfill.ts), same pattern as revenue-finalization.ts. Trigger handoff: D-2 = backfill_job table poll (DB-mediated, no cross-process spawn). Historical orders finalize through existing revenue-finalization job — no special-case code needed (confirmed by reading the query at line 121 of revenue-finalization.ts). DPDP posture clear — no escalation. Paradigm: tier-0 deterministic, $0/month model spend.
**Next:** Architect (Stage 2) — resolve migration number for backfill_job table, event schema for order.backfill.v1, Shopify→Bronze mapping, and all D-1..D-15 bindings before any implementation begins. Tracks: @data-engineer (backfill worker + lane + Bronze) || @backend-developer (trigger API + job + progress + authz) || @frontend-web-developer (progress UX + gross-label + dashboard real number) in parallel after migration committed.
**Next:** Stakeholder gate (Stage 7) — 0 blocking; weighs SEC-CM-RES-01 [M2] + KNOWN-CM-01 + Scale-C4 carried debt + the feat/connector-marketplace merge. Orchestrator commits + advances the gate; I did not commit.

## 2026-06-17T16:10:00Z — Engineering Advisor (final-reviewer) — feat-connector-backfill
**Stage:** 6 · **Verdict:** PASS (APPROVE) · **Blocking:** 0 · **Paradigm audit:** clean (tier-0 deterministic, $0/mo model spend)
**Gates re-run (replicated at source, captured):** worker-runs (T11 + live-DB fn introspection: list_queued_backfill_jobs prosecdef=t/search_path=public; brain_app no-GUC negative control on backfill_job = 0 rows, rolbypassrls=f) · payoff-proven (T12 runRevenueFinalization → finalized=1 amount=250000 INR then idempotent=0, REAL fn executed) · isolation (T4 cross-brand under brain_app = 0 rows; FORCE RLS + no-DELETE confirmed live) · authz (backend B3 11/11, manager meetsMinimumRole non-inert). Backfill e2e file re-run 35/35.
**Source spot-checks:** 0023 SECURITY DEFINER + GUC-before-read at run.ts:265 (worker no longer inert) · order-mapper drops raw customer object, emits hashes-only (D-10) · LedgerWriter ON-CONFLICT key byte-identical to core PgLedgerRepository (SEC-BF-M2 aligned today) · topics.yml dedicated single-partition backfill topic (D-3) · no new deployable / no new external dep / Single-Primitive extend-only.
**Negative-control validity:** PASS (populated, RED-captured, non-tautological on tenancy/money/authz).
**/adopt-rule:** RECOMMENDED — 3rd distinct occurrence of system-job-under-FORCE-RLS inert-enumeration (phone-guard-reeval, revenue-finalization, now shopify-backfill). Wrote rule-proposals/system-job-force-rls-enumeration.md + appended pending-stakeholder-attention. Did NOT adopt (human runs /adopt-rule).
**Residual (non-blocking):** SEC-BF-M2 (MED, aligned) · SEC-BF-L1 (LOW) · dev-token reachability for live Boddactive backfill (tracked validation follow-up).
**Next:** Stakeholder gate (Stage 7) — orchestrator commits + advances; I did NOT commit, did NOT advance the gate.

## 2026-06-17T16:30:00Z — Engineering Advisor (final-reviewer) — fix-dev-token-reach
**Stage:** 6 · **Verdict:** PASS (APPROVE) · **Blocking:** 0 · **Paradigm audit:** clean (tier-0 deterministic, $0/mo model spend; this branch adds no model path)
**Mode:** retroactive review of an already-committed branch (live-debug fixes run through the gate before merge).
**Gates re-run (replicated at source, captured):** analytics vitest 21/21 exit 0 (the QA-bounced gate) · validity_check clean exit 0 (negative-control present) · core typecheck exit 0 · stream-worker tsc = 3 errors ALL pre-existing (backfill.e2e.test.ts NOT in diff → 2× TS2345 cannot be branch-introduced; worker-secrets.ts:41 require/import line byte-identical on master via `git show` → TS2307 pre-existing; stronger than QA's stash proof).
**Source spot-checks:** NIL-uuid GUC (run.ts BEGIN/set_config nil user+workspace/brand-scoped SELECT/COMMIT/ROLLBACK) does NOT widen access — brand_self_read membership subquery empty for nil user, brand_isolation sole grant, connector tables have no self-read policy · dev_secret 0024 dev-only (core LocalSecretsManager ctor throws in prod; main.ts selects Aws in prod; worker buildWorkerSecretsManager selects Aws in prod) token never in API/log/redirect · UPSERT brand_id in both conflict keys (0025 UNIQUE) no cross-brand overwrite · callback redirect HMAC-first + state unchanged, fixed appBaseUrl target (no open-redirect), only ?connected/?connect_error enum in URL · analytics EXISTS broadened to ANY ledger row inside withBrandTxn, realized honest {INR:'0'}, engine sole computation · live re-query: 10009 provisional + 9467 finalized = 19476 INR rows, sync_status=connected.
**Negative-control validity:** PASS — test body at revenue-metrics.live.test.ts:367-402 verified non-inert (appPool/brain_app, GUC=BRAND_B, BRAND_A count=0, asserts is_superuser=false; RED if RLS dropped). Not tautological.
**Over-engineering:** CLEAN — no new files/deps/abstractions/deployable beyond the 8-fix surface; WHY-comments only; surgical 1-commit-per-defect; force-dynamic is a real useSearchParams constraint.
**Hard-rule check:** no dependency/Single-Primitive/compliance/paradigm/gate-skip deviation. Auto-approvable under delegation.
**Residual (non-blocking, none on prod isolation/money path):** SEC-DTR-L1 (LOW NIL-uuid neg-control test) · SEC-DTR-M1 (MED dev_secret plaintext — add shared-DB warning to 0024) · lifecycle+real-pagination regression e2e (LOW) · stale revenue-snapshot.ts JSDoc (trivial) · WorkerLocalSecretsManager single-guard prod-safety (LOW — recommend ctor hard-fail for symmetry).
**Recurrence / auto-candidate:** root cause = all 8 fixes stem from fixture-only connector tests that never exercised connect→disconnect→reconnect lifecycle or real Shopify pagination/data. ≥3-distinct-prior-run threshold NOT met (first run where this is the dominant root cause; system-job-force-rls theme is distinct + already adopted). NO rule-proposal written. RECOMMEND (not filed): lessons-learned entry + a follow-up requirement for a connector lifecycle + real-data regression suite (subsumes SEC-DTR-L1 + the lifecycle/pagination debts).
**Next:** Stakeholder gate (Stage 7) — orchestrator commits explicit product-code paths + advances; I did NOT commit, did NOT advance the gate.

## 2026-06-17T12:50:00Z — Engineering Advisor (cto-advisor) — chore-connector-lifecycle-regression
**Stage:** 1 · **Action:** Intake review (test/regression suite, high-stakes lane, stress-test personas folded inline) · **Personas:** 0 spawned (inhabited inline: Test Realist, Isolation/RLS Skeptic, Non-Inert Skeptic, Data-Safety Skeptic, Scope/Cost Realist, Honesty Skeptic) · **Decision:** ADVANCE
**Rationale:** Requirement is sound — problem, user (internal/platform), success metric, and constraints all Canon-aligned; no product behavior changes; no new deployable; test-only per hard constraint. All 8 upstream fixes are on master; no blocked dependency. 9 binding decisions declared (D-1..D-9). Key bindings: (D-1) lifecycle split — e2e for UI state transitions (connect tile / disconnect tile / button state), integration test at repo/DB layer for reconnect-UPSERT same-id + single-sync-row; (D-2) OAuth callback contract via Fastify inject/supertest — 302 on success + 302 on error (never JSON), fixed appBaseUrl, HMAC-first forged-callback; (D-3) ALL isolation assertions under brain_app (NOSUPERUSER NOBYPASSRLS) with current_user + is_superuser non-inert assertions — durable rule honored; (D-4) mock Shopify = injected fake client (3 pages / 600 orders), since_id='0' revert-RED on first page; (D-5) never touch 60d543dc, all brands test-specific UUIDs with afterAll cleanup; (D-6) NIL-uuid positive + empty-string revert-RED both required; (D-7) sync-status reset + backfill-connected + currency-mismatch trigger; (D-8) dev_secret round-trip + prod-hard-fail; (D-9) discovered product bug = bounce not silent fix. Critical concern: the e2e CANNOT drive a real OAuth reconnect (Playwright cannot automate an external OAuth provider) — the split per D-1 is the resolution; honesty about coverage level is required. Pagination scope capped at 600 orders / 3 pages (not a perf test). · **Next:** Architect (Stage 2) — all D-1..D-9 are the contract

## 2026-06-17T18:15:00Z — Engineering Advisor (final-reviewer) — chore-connector-lifecycle-regression
**Stage:** 6 · **Verdict:** PASS / APPROVE · **Paradigm audit:** clean (tier-0 deterministic, $0, no model path)
**Non-inertness (the deliverable):** 8/8 non-inert. PROVEN by revert→RED→restore (QA, replicated): #6 pagination since_id=0, #2 reconnect-UPSERT no-23505, #1 disconnected-tile. ASSERTION-CONFIRMED at source (read the assertions): #3 single-sync-row count===1 + stale-error reset; #4a/#4b/#4c callback 302/Location/no-PII/forged-HMAC→auth_failed (real HandleOAuthCallbackCommand, genuine HMAC); #5 provisional REFERENCE-only (cited, not duplicated); #7a NIL-uuid positive + empty-string 22P02 revert-leg + #7b cross-brand count===0 under brain_app; #8a sync→connected (exact run.ts UPDATE SQL), #8c currency P0001 + AED-into-AED positive control, #8b dev_secret cross-process round-trip + delete→null + core prod-hard-fail.
**Gates re-run (≥3):** stream-worker shopify-pagination + worker-guc + dev-secret → 3 files passed, 23 passed / 1 skipped (ADR-R3) / 0 failed; replicated QA PASS. assertBrainApp spot-confirmed live (current_user=brain_app, is_superuser=false).
**D-9 / honesty:** D-9 CONFIRMED — git diff vs master = 8 test/fixture/e2e files only, 0 product src, 0 migrations. Honesty CONFIRMED — e2e header states "REAL OAUTH NOT FAKED"; connect→disconnect→reconnect round-trip honestly split (e2e UI + Track B repo/DB); callback 302 honestly delegated to Track B inject, not claimed by e2e. ADR-R3 worker prod-guard gap correctly it.skip + surfaced (D-9), NOT fixed here.
**Over-engineering / Single-Primitive:** CLEAN — one assertBrainApp primitive, one fetch-stub, stubbed fetch over a fixture HTTP server, no new infra/migration/deployable. Plan length proportionate to high-stakes 8-defect surface.
**Hard-rule check:** none (no dependency/Single-Primitive/compliance/paradigm/gate-skip deviation). Verification-validity PASS — negative_control[] populated, no bypass-green/inert/tautological.
**QA-CLR-LOW-01 call:** MERGE-AS-TRACKED. The 8 new tsc errors are confined to test/fixture files (cross-rootDir LocalSecretsManager import + ADR-R3 cast + RequestInfo/Response.bytes DOM-type mismatch); vitest passes via esbuild transpile. A behavior-correct regression net should not be blocked on test-file tsc hygiene. Caveat surfaced: these test files are effectively outside the tsc gate until the tsconfig is fixed — tracked follow-up, not an auto-block. (Dev-report's "zero new errors" claim was false; QA caught it — Stage-5 worked.)
**Tracked debt / recommended follow-ups (recommend, not filed):** SEC-CLR-MED-01 (MED, latent — product PR: WorkerLocalSecretsManager NODE_ENV=production ctor guard; un-skip ADR-R3) · QA-CLR-LOW-01 (LOW — cleanup PR: stream-worker tsconfig cross-rootDir test imports + fetch-stub DOM types).
**Lessons-learned:** this suite CLOSES the connector-path root gap (lifecycle/real-data/worker-RLS-secret coverage absent → produced 8 live fixes). No rule-proposal — system-job-force-rls-enumeration already adopted + honored; recurrence threshold for a NEW rule not met.
**Next:** Stakeholder gate (Stage 7). I did NOT commit, did NOT advance the gate.

## 2026-06-17T20:00:00Z — Engineering Advisor (cto-advisor) — feat-shopify-live-connector
**Stage:** 1 · **Action:** Intake + adversarial stress pass (6 personas inhabited inline) · **Personas:** none spawned (inhabited inline: Security/Webhook Skeptic, Dev-Reality Skeptic, Dedup/Correctness Skeptic, Money/COD Skeptic, Isolation/System-Job Skeptic, Scale/Lane Skeptic) · **Decision:** ADVANCE
**Rationale:** All blockers shipped. 14 binding decisions declared (D-1..D-14). CRITICAL findings: (D-4) webhook handler resolves brand_id via DB lookup on connector_instance after HMAC validation — never from the X-Shopify-Shop-Domain header (attacker-controlled); (D-6) sha256(brand:order_id) as the SOLE event_id BREAKS live status-change sync — ON CONFLICT DO NOTHING silently drops delivered→RTO transitions; the Architect must choose a per-state composite key (recommended: uuidV5FromOrderLive(brand,order,updated_at_ms)) so each state change lands as a distinct Bronze row; (D-7) re-pull is a cross-tenant system job → SECURITY DEFINER enumeration fn required (adopted durable rule); (D-8) dev-honesty: Shopify cannot reach localhost — synthetic HMAC-signed POST is the only dev test path for webhooks; the 35-day re-pull is the dev freshness mechanism. HIGH findings: ShopifyHmac.validateWebhook() CONFIRMED correct (raw body bytes, base64 comparison, timing-safe — covers webhook case); overlap-lock per connector (D-9); mapper package extraction to unblock cross-app reuse (D-12); LedgerWriter RTO→reversal path confirmation (D-13). Lane: high_stakes + system_of_record_audit surface added. Paradigm: tier-0 deterministic, $0/month.
**Next:** Architect (Stage 2) — D-4, D-6, D-7 are the three CRITICAL pre-implementation gates; D-12 (mapper package) is the prerequisite that unblocks parallel tracks.

## 2026-06-17T19:05:00Z — Engineering Advisor (final-reviewer) — fix-connector-lifecycle-cleanup
**Stage:** 6 · **Verdict:** PASS / APPROVE · **Paradigm audit:** clean (tier-0, no model path, $0)
**Scope/lineage:** Closes the two tracked follow-ups SEC-CLR-MED-01 (worker prod-guard) + QA-CLR-LOW-01 (test-file tsc cleanup) filed at the end of chore-connector-lifecycle-regression. Diff = 4 files +125/-84, tests-and-one-guard only; no migration, no RLS/grant, 60d543dc untouched. No scope creep.
**Product spot-confirm:** WorkerLocalSecretsManager guard condition `NODE_ENV==='production'` is IDENTICAL to core LocalSecretsManager.ts:33, same `[ClassName] FATAL` throw-before-work. Prod path provably unaffected — factory returns AwsSecretsManager (line 46) before reaching `new WorkerLocalSecretsManager()` (line 50); guard defends a direct-instantiation bypass only. `export` adds no runtime surface (test-import only).
**Gates re-run (≥3, replicated):** stream-worker tsc → 3 errors (1×TS2307 factory require + 2×TS2345 backfill.e2e, pre-existing); dev-secret.integration → 4 passed / 0 skipped (A4-3 active+green); core LocalSecretsManager.test → 3 passed (moved coverage present). All replicate QA PASS.
**Negative-control (independently re-replicated, not trusted from QA):** removed worker guard → A4-3 RED (`expected [Function] to throw an error`) → restored → git diff empty. Test genuinely non-inert; QA negative_controls[] valid.
**Pre-existing proof:** TS2307 require line exists verbatim on origin/master; backfill.e2e.test.ts has zero branch diff. The 3 residual errors cannot be branch-introduced. 11→3 confirmed.
**3 residual tsc errors — my call:** ACCEPTABLE-AS-TRACKED (do not block). Recommend a tiny follow-up to bring the stream-worker prod secrets path back inside the tsc gate (TS2307 cross-package require = latent typing blind-spot on a secrets seam) + batch the 2 backfill.e2e fixture types. Recommend, not file — surface to Stakeholder.
**Over-engineering / Single-Primitive:** CLEAN — mirrors an existing primitive (no new one), surgical diff, no new deps/abstractions, comments are WHY not WHAT.
**Hard-rule check:** none (no dependency/Single-Primitive/compliance/paradigm/gate-skip deviation). Verification-validity PASS.
**Retro / rule-proposal:** no new rule (recurrence threshold not met; system-job-force-rls-enumeration already adopted). Process note: prior dev-report's false "zero new tsc errors" claim is what QA caught → produced QA-CLR-LOW-01; gate worked.
**Next:** Stakeholder gate (Stage 7). I did NOT commit, did NOT advance the gate.

## 2026-06-17T22:15:00Z — Engineering Advisor (final-reviewer) — feat-shopify-live-connector
**Stage:** 6 · **Verdict:** PASS / APPROVE · **Paradigm audit:** clean (Tier-0, $0/mo; over-engineering clean — all artifacts plan-sanctioned; no drive-by)
**Gates re-run (≥3, independently replicated under real brain_app, is_superuser=off):**
- live-ledger-wiring.e2e TW1-TW4 → 4/4 PASS (real produce → wired consumer → ledger row) — the ORCH-LV-H1 fix proof.
- live-connector.e2e T1-T8 → 16/16 PASS (D-6 per-state, RTO-reversal, no-GUC NC, isolation).
- shopifyWebhookHandler.integration B3 → 8/8 PASS (HMAC-first, anti-spoof).
- Direct DB: no-GUC connector_instance bare SELECT = 0 rows; list_connectors_for_repull() = 2 rows; ledger GRANT = INSERT,SELECT only; both 0026 fns prosecdef=true/search_path=public; live ledger total=20285 reversals=49 (matches orchestrator proof).
- stream-worker tsc: 1 error (worker-secrets AwsSecretsManager cross-rootDir) — confirmed pre-existing on master, 0 new, exit 0.
**D-6 (make-or-break):** live event_id = sha256(brand:order:updatedAtMs:order.live.v1), backfill = sha256(brand:order:order.backfill.v1) — distinct namespaces, per-state new Bronze row, retry dedups. Re-replicated non-inert.
**ORCH-LV-H1 fix:** LiveLedgerBridgeConsumer imported(28)/instantiated(102)/started(148)/stopped(113) in main.ts; real subscribe+run; filters order.live.v1; ledger-only (no double-Bronze). RESOLVED.
**Anti-spoof:** HMAC validateWebhook first op (401 on fail); brandId from resolve_connector_by_shop_domain DB row (158), never header/body. **Reversal:** writeReversal GUC-first, negative BigInt-string, ON CONFLICT DO NOTHING, sale untouched; live 49 rto_reversal.
**2 tracked findings — my call:** ACCEPT AS TRACKED. SEC-LV-M1 (MED lock-window → worst case double API calls, dedup-absorbed, M1+); SEC-LV-L1 (LOW NaN-date guard, Shopify always sends updated_at, one-line). Neither on a money/tenancy/auth correctness path.
**Wired-to-nothing pattern — my call:** WATCH + lessons-learned now; PROPOSE durable rule at occurrence #3. This is #2 (ADR-BF-9 backfill + ORCH-LV-H1 live), both caught by live verification not unit-tested reviews. Threshold (3, per system-job-force-rls precedent) not met → no rule-proposal file written; lessons-learned + attention watch filed. Proposed rule: new consumer/recognition-writer needs an END-TO-END wiring test (real produce→subscribe→sink effect) + reviewer verifies it's wired into main.ts.
**Hard-rule check:** clean (no dependency/Single-Primitive/compliance/paradigm/gate-skip deviation). Verification-validity PASS (non-inert negative controls on every tenancy/auth/money path).
**Next:** Stakeholder gate (Stage 7). I did NOT commit, did NOT advance the gate.

## 2026-06-17T22:32:08Z — Engineering Advisor (final-reviewer) — feat-collection-foundation
**Stage:** 6 · **Verdict:** BOUNCE · **Paradigm audit:** clean (deterministic tier-1, no model call)
**Gates re-run:** pixel-sdk 12/12 GREEN, tracking-status 9/9 GREEN, consent-propagation+no-pii 3/3 GREEN; consent-gate mutation → RED (non-inert), restored byte-identical. Infra-bound e2e not replicable here (brainv3 containers publish no host ports; brain-spec stack down — relied on QA captured run + negative_control[]).
**Keystone:** R2 isolation REAL + NON-INERT + WIRED (0028 fn → ProcessEventUseCase:123 → main.ts:74 default-on); wired-to-nothing near-miss noted, WATCH stays #2-of-3.
**Blocking:** FR-CF-01 MEDIUM — consent_flags write(payload.consent_flags)/read(payload.properties.consent_flags) path mismatch → Tracking Center consent KPIs always 0/false. Escaped: no writer→reader consent round-trip test.
**Next:** bounce → Stage 3 (Track C read seam) → re-verify Stage 5.
