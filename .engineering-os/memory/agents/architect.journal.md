# Architect — Journal

> Append-only. See /Users/rishabhporwal/.claude/plugins/cache/engineering-os/engineering-os/2.3.1/docs/role-empowerment-model.md for entry shape.

## 2026-06-15T07:19:27Z — system — bootstrap
**Action:** Journal initialized by /eos-init on 2026-06-15T07:19:27Z.

## 2026-06-16T20:10:00Z — Architect — feat-data-plane-ingest-spine
**Stage:** 2 · **Paradigm:** deterministic-only (zero model calls; HTTP edge + pg spool + KafkaJS + Zod + Redis SETNX + SQL INSERT) · **Tracks:** @backend-developer (collector accept-before-validate edge + spool + drainer) ∥ @data-engineer (stream-worker consume→dedup→Bronze sink + DLQ)
**D-4 RESOLVED → Postgres `bronze_events` staging-table fallback (M1 only).** Decisive: no production-grade TS Iceberg writer exists (repo grep clean: zero iceberg/parquet/pyiceberg deps); Nessie is catalog-metadata-only (does not accept row writes); mature writers = JVM/PyIceberg = Phase-3-deferred (STACK.md:46/60, locked-choice #7). The fallback is the one D-4 pre-authorised (02 §119). NO new stack layer (Postgres = PersistenceAdapter). ADR-002/003 amendment NOTE only, not a new ADR; target (Bronze=Iceberg) unchanged; Phase-3 flip = mechanical column-map. **bronze_table.sql/bronze_spec.json untouched** (Phase-3 target spec).
**Isolation RE-BOUND for Postgres sink:** D-8 = Postgres RLS (two-arg current_setting, FORCE RLS, brain_app INSERT+SELECT-only, NN-1 assertion inherited) instead of Iceberg partition predicate. All isolation tests under SET ROLE brain_app (superuser brain = false pass; MEMORY note).
**Contract fixes (Slice 1, before any consumer/Apicurio):** F-6/D-5 ingest_at→ingested_at (Zod); F-1/D-6 envelope occurred_at/ingested_at number→string ISO-8601; codegen regen .avsc committed same commit (I-E01).
**Single-Primitive:** clean — ONE spool, ONE dedup (Redis + PK backstop), ONE bronze_events (event_type as column), brand_id at every layer. Extends @brain/events + tenant-context + 0001 RLS pattern.
**Slices:** 1 spike-confirm+contract+migrations(0015 spool,0016 bronze) → 2 collector edge → 3 stream-worker sink → 4 e2e+durability+dedup+isolation tests → 5 deploy track. COMMIT PER SLICE (prior runs lost ~61min to infra timeout). **Artifact:** 03-architecture-plan.md. **Next:** @backend-developer + @data-engineer — Stage 3.

## 2026-06-15T11:34:04Z — Architect — chore-platform-foundations-sprint0
**Stage:** 2 · **Paradigm:** deterministic/infrastructure (zero model calls; managed-vs-self-host already bound in STACK.md) · **Tracks:** A(backend-developer) B(platform-devops) C(platform-devops) D(data-engineer) E(data-engineer+backend-developer)
**Single-Primitive:** clean — repo sealed with correct topology (apps/packages/tools/db/infra all stubbed); builders fill stubs, no new deployable/db/ledger/package. **Encoded:** NN-1..NN-7 + 9 scope rulings + EC10 operational definition (dev full-apply / staging structural-scaffold-compute-zero / prod plan-only) as per-track pass-1 acceptance. Critical path = Track C (Terraform); D/E develop code legs against local docker-compose day 1, gate live legs on C. Track B carries the deploy pipeline (affected-only build + ArgoCD staging-auto/prod-promote + auto-rollback + flag-off).
**Artifact:** 03-architecture-plan.md (run 2026-06-15T11-06-27Z__09ba6e). **Next:** A,B,C,D,E builders — Stage 3 (Group 1 day-1 parallel; A→D contracts-skeleton handoff EOD day 1).

## 2026-06-15T15:00:00Z — Architect — context-sync:datamodel-v1.5 (doc 08 §36/§37 + doc 03)
**Stage:** context-sync (no spawn) · **Paradigm:** deterministic/data-modeling (zero model calls) · **Frozen-architecture:** INTACT — no new primitive (5 reserved domains = Silver tables modeled-not-built; connector-registry + region cols = additive on existing Postgres/StarRocks tiers; only 2 ledgers remain). **Region seam:** additive-columns-now, RegionAdapter posture UNCHANGED (Phase-5 GTM trigger holds). **M1 delta:** field-complete §37 dict + new Silver dims (ad_account/campaign/set/creative, product_variant, inventory_level, order_line_item, order_status_history, refund, shipment_tracking_event) + region/tax cols (region, tax_regime, reporting_currency_value_minor). **§37.11 promote-via-contracts** = consistent with I-E01. **Artifact:** architect-assessment.md. **Next:** data-engineer — M1 canonical migration (contract-first, additive-only).

## 2026-06-16T01:50:00Z — Architect — feat-access-onboarding-flow
**Stage:** 2 · **Paradigm:** Tier-1 deterministic (zero model calls; $0/mo — every item is a DB txn / Redis counter / enum→URL table / CHECK constraint; a model call here = paradigm-bypass) · **Tracks:** B-MIG,B-1..B-8,DEPLOY(backend-developer) + F-1..F-4(frontend-web-developer)
**Single-Primitive:** CLEAN (extend-only) — extends user_session(rotation lineage), UserSessionRepository(+2 bulk-revoke), BrandRepository(+3 cols), organization(+onboarding cols), one RateLimiter, one AuditWriter; CSRF nets DOWN to one (removed BFF duplicate). No new service/table/queue/primitive.
**Decisions (binding):** MA-09 → onboarding_status on `organization`, Option A, first-brand-only (no brand_id exists at Step 1; Canon "brand=tenant" is isolation not UX; enum pending→org_created→brand_created→integration_selected→complete maps 1:1 to resume routes). MA-12 → revenue_definition CHECK=('realized','delivered'), 'placed' EXCLUDED, NO Canon amendment (grep-confirmed METRICS.md has no placed_revenue; MER is "never placed/gross"). CRITICAL 2/2 closed in design (B-1 rotation+FOR UPDATE+family-wipe; B-2 same-txn revoke-on-all).
**Migrations:** 0010_brand_locale, 0011_onboarding_state, 0012_session_rotation_lineage (each up+down, NN-1 negative-control note; next free int after 0009; renumber on collision). Branch from latest feat/onboarding-session-context (rebase risk flagged).
**New endpoints:** POST /auth/token/refresh, POST /bff/session/set-org, POST /bff/session/onboarding/advance. BFF contract: needs_onboarding boolean → onboarding_status enum across login/refresh/set-org.
**Artifact:** 03-architecture-plan.md (run 2026-06-15T21-18-00Z__a7a965). **Next:** backend-developer + frontend-web-developer — Stage 3 parallel against §6 contract.

## 2026-06-16T22:15:00Z — Architect — feat-identity-graph
**Stage:** 2 · **Paradigm:** Tier-1 deterministic (zero model calls; $0/mo — parse→SHA-256→hash-equality lookup→count-threshold; `@effort("deterministic")` on writer; a model call here = paradigm-bypass) · **Tracks:** @data-engineer SINGLE-TRACK (whole graph + bridge + tests; no backend-developer track — no core-module API seam warrants it for M1; `identity` core-module stays `export {}` until a consumer needs it).
**Bindings RESOLVED D-1..D-7 + C-1 stub-crypto + C-2 migration:**
- **D-2 salt (CRITICAL):** per-brand 32B salt at brand-creation → `brand.identity_salt_ciphertext`; `SaltProvider` EXTENDS existing `apps/core/.../SecretsProvider` (Single-Primitive — do NOT invent 2nd secrets abstraction); fetch/decrypt at bridge startup; **HARD CRASH on failure** (`salt.length!==32 → throw`, never empty/default/global = cross-brand correlation = ONE-invariant breach). `LocalSecretsProvider` dev stand-in (already throws on empty). CI: 2 brands → different hashes.
- **D-1 phone-guard (CRITICAL):** `brand.phone_guard_threshold INT DEFAULT 10` + `suppression_window_days DEFAULT 30` (brand-configurable, NOT hardcoded); `shared_utility_identifier` + `suppressed_until`; windowed distinct-brain_id count > threshold → suppress from merge-key set. Re-eval = EXISTING Argo-job type (D-7). False-merge (ghost high-LTV in ledger) > over-suppression (recoverable) → N=10 leans guard-strong. Windowed (not lifetime) for cool-down. N=10 boundary test.
- **C-1:** `stubSha256` (MurmurHash, `identity-core/src/index.ts:90`) DELETED → `node:crypto` real SHA-256 (replay-correctness heart; non-crypto hash = 2nd brain_id per rebuild). Known-vector + cross-brand-differs CI.
- **D-4:** deterministic `merge_id=sha256(brand_id‖canonical‖merged‖rule_version)`; ON CONFLICT DO NOTHING on `identity_merge_event` PK + 2 UNIQUE PARTIALs (alias one-live-pointer, identity_link strong-id); at-least-once + idempotent writer; replay 3× → 1 merge row.
- **D-3:** `contact_pii` elevated RLS = `brand_id` AND `app.role='send_service'` (BOTH two-arg fail-closed); dev `pii_value text` plaintext stand-in (prod KMS = `0018` follow-up, not M1); brain_app w/o send_service → 0 rows.
- **D-5:** deterministic-only `rule_version='v1-deterministic'`; **`merge_rule`/`merge_candidate`/`pii_vault_reference` DEFERRED** (CTO §6 — constant rule version suffices; no mart consumer yet → smaller/more-reversible migration).
- **D-6:** E.164 phone norm (`regionCode` param, `+91` default — NOT hardcoded in pure fn); `09876543210`≡`+919876543210`; un-normalizable → low-confidence, no crash.
- **D-7:** bridge inside EXISTING stream-worker process (added to `main.ts`); no new deployable/Argo-job-type/container.
**C-2 migration `0017_identity_graph.sql`:** +brand cols (ALTER ADD IF NOT EXISTS — brand 0004 has NEITHER salt NOR threshold col yet); all identity tables ENABLE+FORCE RLS, two-arg `current_setting('app.current_brand_id',TRUE)`, REVOKE+minimal GRANT (append-only INSERT+SELECT; shared_utility_identifier+UPDATE for re-eval; brain_id_alias+UPDATE for Phase-2 unmerge), copy NN-1 DO-block from `0004_brand.sql:61`. Additive (I-E02); down=DROP (identity = derived projection, rebuildable from Bronze, not immutable SoR).
**C-3 identity-bridge** (`apps/stream-worker/src/identity-bridge/` was EMPTY dir): `IdentityBridgeConsumer.ts` copies `CollectorEventConsumer.ts:29` (autoCommit:false, offset-after-write, DLQ@5); `IdentityRepository.ts` copies `BronzeRepository.ts:53` (set_config-then-write-one-txn, brain_app); `ResolveIdentityUseCase`/`IdentityResolver`/`SharedUtilityPolicy`/`SaltProvider`.
**Single-Primitive:** CLEAN — ONE identity primitive (core `identity` ctx, written by bridge); salt reuses SecretsProvider; ONE hasher (identity-core); merge_rule deferred = fewer primitives. Reuses tenant-context, NN-1 pattern, stream-worker layering.
**Isolation:** ALL RLS tests under `SET ROLE brain_app` (dev superuser `brain` MASKS RLS — MEMORY note `dev-db-superuser-masks-rls.md`).
**Slices (smallest-first, COMMIT PER SLICE — prior runs lost time to infra timeouts):** 1 mig 0017 + real-crypto + SaltProvider → 2 deterministic resolver + idempotent bridge writer → 3 phone-guard suppression + contact_pii gate + re-eval job → 4 deploy (affected-only, EXISTING stream-worker ArgoCD app, health-probe rollback; NO canary — Phase-4 deferred ADR-010). Branch `feat/identity-graph` off master.
**ADR:** none new — within ADR-008 (identity/salt), ADR-007 (KMS/secrets), ADR-001 (RLS), ADR-010 (deploy, honors Phase-4 canary deferral). Recorded: 0017 = first identity migration (none 0001-0016), additive, no layer change.
**Artifact:** 03-architecture-plan.md. **Next:** @data-engineer — Stage 3 (single track).

## 2026-06-16T19:05:00Z — Architect — feat-realized-revenue-ledger
**Stage:** 2 · **Paradigm:** Tier-0 deterministic (zero model calls; $0/mo, 0 tokens/day — DB txn / CHECK+UNIQUE / named-fn SUM / time-horizon compare; a model call here = paradigm-bypass) · **Tracks:** @data-engineer SINGLE-TRACK (whole ledger + recognition engine + finalization job + tests + affected-only deploy; intelligence-engineer = NONE — metric engine/parity oracle that READ the ledger are the NEXT slice; realized_gmv_as_of is the clean read seam).
**Bindings RESOLVED D-1..D-7 (D-5 = Data-Engineer Sprint-0, non-blocking) + no-float-SQL + named-as-of-fn + dedup + immutability-by-grant:**
- **D-2 immutability (CRITICAL):** STRUCTURAL by GRANT — brain_app SELECT+INSERT only (NO UPDATE/DELETE), proven by a migration-time grant assertion (copy 0017 NN-1 DO-block style). billing_posted_period CHAR(7) 'YYYY-MM'. M1 has NO billing_run table (billing meter = non-goal) → all periods open, no closure possible. Late reversal posts NEW row, billing_posted_period = to_char(occurred_at,'YYYY-MM') (current period), never original sale's. Tightened doc-08 §7.1's loose `date` type → CHAR(7) (operational period, not instant); journaled, no Canon amendment (doc is descriptive of rule not storage type).
- **D-4 dedup (CRITICAL):** UNIQUE (brand_id, order_id, event_type, (occurred_at::date)) — occurred_at::date admits legit split-shipment double-finalization, kills same-day replay. PK (brand_id, ledger_event_id) deterministic sha256 backstop. ON CONFLICT DO NOTHING + ledger_replay_suppressed_total metric.
- **D-3 no-double-count:** NAMED fn realized_gmv_as_of(brand_id,as_of) RETURNS BIGINT, STABLE SECURITY INVOKER; SUM WHERE economic_effective_at::date<=as_of AND event_type<>'provisional_recognition'. SOLE as-of path; ad-hoc SUM forbidden. Map: finalization(+), rto_reversal/refund/chargeback/cancellation/concession(−), settlement_fee_reversal(−), marketplace/payment_adjustment(±), provisional EXCLUDED.
- **D-1 horizon:** brand cols cod_recognition_horizon_days INT DEFAULT 25, prepaid_recognition_horizon_days INT DEFAULT 7. Finalization job = EXISTING Argo-job type (sibling phone-guard-reeval.ts) in apps/stream-worker/src/jobs/revenue-finalization.ts; emits finalization for provisionals past horizon WITH RTO/cancel/existing-finalization pre-check (M-3 race: pre-check + signed-sum net + dedup UNIQUE = 3 guards). No new deployable.
- **D-6 single-currency:** brand.currency_code CHAR(3) DEFAULT 'INR'; BEFORE INSERT trigger ledger_currency_matches_brand() (structural, not app-only — high-stakes SoR). assertSameCurrency (money:102) covers TS path, trigger covers raw-SQL path; both required.
- **D-7 rounding:** banker's round-half-to-even; NEW helper roundToMinorBankers in packages/money (only pkg addition; grep-confirmed no divide/round exists); rounding_adjustment_minor BIGINT col; no amount_minor division in M1 (no allocation = attribution ledger non-goal).
- **no-float-SQL (M-2):** lint covers .ts ONLY (eslint.config.mjs:112); all *_minor cols BIGINT; migration DO-block asserts information_schema.columns data_type='bigint' for %_minor.
**Migration 0018_realized_revenue_ledger.sql:** brand col adds (ALTER ADD IF NOT EXISTS) + ledger table per doc-08 §7.1 (:336) exact columns; ENABLE+FORCE RLS two-arg current_setting('app.current_brand_id',TRUE) copied from 0017:91; REVOKE ALL + GRANT SELECT,INSERT (append-only-by-grant, copy 0016:55); dedup UNIQUE; partial as-of index WHERE event_type<>'provisional_recognition'; currency trigger; 3 migration-time DO-block assertions (NN-1 two-arg, append-only-grant, _minor-is-BIGINT); realized_gmv_as_of fn. Additive (I-E02); down=DROP (rebuildable from Bronze, not Iceberg SoR — same as 0016:21). 0018 = first ledger migration (none 0001-0017); renumber on collision.
**Recognition engine** apps/core/src/modules/measurement/internal/ (was export{} stub): DDD ctx; LedgerEntry uses @brain/money Money (never raw number); PgLedgerRepository copies BronzeRepository set_config-then-INSERT-one-txn under brain_app; OrderEventConsumer offset-after-write idempotent; billing_posted_period=to_char(occurred_at,'YYYY-MM').
**Single-Primitive:** CLEAN — ONE ledger (event_type discriminator, not per-type tables — doc-08 §0.4#1), ONE @brain/money (+1 minimal banker's helper), ONE RLS pattern (0017), ONE named as-of fn, ONE currency trigger. No new service/table-family/queue/deployable/Argo-job-type/ADR.
**Isolation:** ALL RLS tests under NON-superuser app role (dev brain MASKS RLS — MEMORY dev-db-superuser-masks-rls.md); pattern from tools/isolation-fuzz/src/pg.test.ts:1-90.
**Slices (smallest-first, COMMIT PER SLICE):** S1 mig 0018+brand cols+realized_gmv_as_of → S2 recognition engine + roundToMinorBankers → S3 finalization job + reversal paths + replay metric → S4 tests (9 cases) → DEPLOY affected-only core+stream-worker + ArgoCD sync + finalization job manifest, NO canary (Phase-4 deferred ADR-010). Branch feat/realized-revenue-ledger off master.
**ADR:** none new — within ADR-001(RLS), ADR-010(deploy, honors Phase-4 canary deferral), ADR-007(secrets), I-S07(money), I-E02(additive). Recorded: 0018 first ledger migration, additive, no layer change.
**D-5 flag:** reconciliation tolerance (±2-3% W4, >±5% stop-fix) = Data-Engineer Sprint-0 freeze, non-blocking; freeze before any external-reconciliation test (not in this slice — no live Shopify).
**Artifact:** 03-architecture-plan.md. **Next:** @data-engineer — Stage 3 (single track).

## 2026-06-17T01:05:00Z — Architect — feat-metric-engine-parity
**Stage:** 2 · **Paradigm:** Tier-0 deterministic (zero model calls; $0/mo, 0 tokens/day — typed registry lookup / named-fn SUM / bigint integer equality; model call here = paradigm-bypass, METRICS.md tier-0 "the only tier that produces numbers") · **Tracks:** @intelligence-engineer SINGLE-TRACK (registry + engine + provisional fn 0020 + parity oracle + CI dep edge + tests; other = NONE — metric parity is theirs).
**Bindings RESOLVED D-1..D-7 + bigint-fixtures:**
- **D-2 non-tautological parity reference (CRITICAL):** oracle's INDEPENDENT reference SQL = `SELECT currency_code, SUM(amount_minor) FROM realized_revenue_ledger WHERE brand_id=$1 AND economic_effective_at::date<=$2::date AND recognition_label='finalized' GROUP BY currency_code` — STRUCTURALLY != realized_gmv_as_of (which filters event_type<>'provisional_recognition' + returns scalar BIGINT). Two load-bearing diffs: (a) recognition_label='finalized' vs event_type<>'provisional_recognition' → a provisional leak in path A yields non-zero delta; (b) GROUP BY currency_code vs blended scalar → a currency blend yields a delta. Helper getIndependentReferenceRevenue(brandId,asOf,client)→Map<string,bigint> in tools/parity-oracle/src/reference.ts; FORBIDDEN to import the engine or call either named fn (doc-comment + import fence). Both paths read the SAME seeded snapshot under brain_app. tolerance 0 → 1-minor-unit delta fails vitest → CI test:parity exits non-zero → blocked.
- **D-1 registry:** packages/metric-engine/src/registry.ts METRIC_REGISTRY keyed (metric_id,version) `as const`, resolveMetric throws on unknown, version bump = NEW KEY (immutable). readSeam field encodes the named DB path; unit test asserts recognitionLabels↔readSeam consistency.
- **D-3 CI dep edge:** add @brain/metric-engine workspace:* dep to tools/parity-oracle/package.json + package-level tools/parity-oracle/turbo.json {extends:["//"], tasks:{test:parity:{dependsOn:["@brain/metric-engine#build","^build"]}}}. Turbo 2.9.18 supports package configs (verified). pr.yml:32-33 test:parity --affected already BLOCKING (no continue-on-error). apps/core ALREADY deps @brain/metric-engine (confirms in-process binding, D-7) — only the oracle edge missing. Proof: touch only the engine → --dry-run lists oracle affected.
- **D-4 provisional fn:** migration 0020_provisional_gmv_as_of.sql additive — provisional_gmv_as_of(brand_id uuid, as_of date) RETURNS TABLE(currency_code,provisional_minor) SECURITY INVOKER, WHERE recognition_label IN ('provisional','settling') GROUP BY currency_code. Per-currency from day one (not scalar like realized — new code, correct shape). Migration-time assertion: exists + prosecdef=false. Down=DROP FUNCTION. No ad-hoc SUM in app.
- **D-5 per-currency:** computeRealizedRevenue/computeProvisionalRevenue → Map<CurrencyCode,bigint>. M1 realized = 1 entry (wraps scalar fn, keyed by brand currency); provisional reads TABLE direct. Map keys ARE currencies → no blend path. 2-brand/2-currency fixture (A=INR, B=AED) proves no cross-brand/cross-currency blend.
- **D-6 sole-emitter:** eslint.config.mjs:83-88 fence is MIS-SCOPED (disallows metric-engine from ALL core-modules, contradicting its own comment "analytics+measurement only"). FIX to allow-list (measurement+analytics allowed, others denied) — bug-fix, decision-log noted. No app-code ledger SUM; engine is sole consumer of the seams.
- **D-7 no new deployable + F-SEC-02:** packages/metric-engine = in-process lib (core already deps it); tools/parity-oracle = CI-only. Engine reads via withBrandTxn(brandId,fn) = explicit BEGIN;set_config(...,true);fn;COMMIT → GUC genuinely txn-scoped (F-SEC-02 carry-in tighten; does NOT fix GetRealizedGmvAsOf itself — pre-existing, must-fix-before-Phase-2). Two-arg current_setting fail-closed already → worst case 0 rows. No PII (UUID-keyed).
- **bigint-fixtures (M-2):** GoldenFixture money fields number→bigint (no-float-money TSPropertySignature fires on number for *_minor/*Value*); checkParity delta rewritten bigint (Math.abs rejects bigint); assertNotTautology typeof→'bigint'; Sprint-0 fixtures bigint literals.
**Single-Primitive:** CLEAN extend-only — ONE engine (fill export{} stub), ONE @brain/money, ONE no-float rule, ONE import fence (fixed), ONE parity harness (reuse checkParity/runGoldenFixtures), ONE CI step. NEW only: 0020 fn (additive), registry.ts (TS const), engine/oracle fill-in. No new service/table/queue/deployable/ADR.
**Isolation:** parity + GUC tests under brain_app pool (BRAIN_APP_DATABASE_URL) — dev superuser brain MASKS RLS (MEMORY). Reuse ledger live-test dual-pool harness (realized-revenue-ledger.live.test.ts:41-90).
**Slices (smallest-first, COMMIT PER SLICE):** S1 registry+realized engine+per-currency type+fence fix → S2 provisional fn 0020 + provisional metric → S3 oracle independent reference + golden fixtures(5) + CI dep edge + bigint fix → S4 tests(9) + gate proof (RED on 1-minor delta, GREEN on agreement). Branch feat/metric-engine-parity off master @ f29e61d.
**ADR:** none new — within STACK locked-choice 4 (sole emitter), parity-oracle, I-S07 (money), I-S01 (RLS), I-E02 (additive). Decision-log note: eslint metric-engine fence corrected (was over-blocking) to match documented intent.
**Artifact:** 03-architecture-plan.md. **Next:** @intelligence-engineer — Stage 3 (single track).

## 2026-06-17T02:40:00Z — Architect — feat-analytics-api-dashboard
**Stage:** 2 · **Paradigm:** Tier-0 deterministic (engine read → EXISTS(finalized) → bigint→string → Intl format; zero model calls, $0/mo, 0 tokens/day; model call here = paradigm-bypass) · **Tracks:** @backend-developer (analytics svc + BFF route + engine==BFF/honest-empty/isolation tests) ∥ @frontend-web-developer (card + client adapter + formatMoneyDisplay + e2e).
**Bindings turned into build plan D-1..D-12 (CTO bindings were precise — NOT re-derived):**
- **D-1 contract LOCKED:** GET /api/v1/dashboard/realized-revenue?as_of=YYYY-MM-DD (BFF pattern, NOT /api/v1/metrics — one read surface, ADR-002) → {request_id, data:{state:'no_data'|'has_data', as_of, realized:Record<ccy,string>|null, provisional:Record<ccy,string>|{}|null}}. bigint→STRING (JSON no bigint, pg-string convention). realized/provisional SIBLINGS, never summed. Frozen first = parallel-track coordination point.
- **D-2 honest-empty (HEART):** state driven by EXISTS(SELECT 1 FROM realized_revenue_ledger WHERE recognition_label='finalized') — NOT by value. Neutralizes the realized-revenue.ts:71 `??'0'` landmine (NULL→'0'→0n indistinguishable from real net-zero). no_data ⇒ null fields. UI "No data yet" on state==='no_data' regardless of value.
- **D-3 sole-read-path (HEART):** numbers ONLY from computeRealizedRevenue/computeProvisionalRevenue; ONLY ad-hoc SQL = the EXISTS existence check (existence, not SUM — D-2 requires it). engine==BFF exact-bigint test = proof.
- **POOL NUANCE (load-bearing):** registerBffRoutes gets DbPool (context-wrapped, GUC-injecting client.query(ctx,sql,params)) at bff.routes.ts:60 — but engine withBrandTxn needs a RAW pg.Pool (own BEGIN/set_config/COMMIT, deps.ts:44). rawPgPool ALREADY exists at main.ts:277. Bind: thread rawPgPool into registerBffRoutes as new optional param → EngineDeps.pool. NO DbPool wrap (double-GUC), NO 3rd pool.
- **D-4 provisional separate:** sibling fields, distinct card labels ("Realized Revenue" / "Provisional / Settling — not yet confirmed"); empty provisional → {} → "No provisional data"; never blended.
- **D-5 no-9th-envelope (HEART):** client uses const {data}=await bffFetch<BffEnvelope<RawRevenue>>(...) mirroring getBrandSummary client.ts:666; raw+mapped types SEPARATE. 8 prior flat-shape bugs.
- **D-6 isolation (HEART):** negative-control under brain_app (BRAIN_APP_DATABASE_URL, NOBYPASSRLS) — dev superuser brain MASKS RLS (MEMORY dev-db-superuser-masks-rls.md). Reuse dual-pool harness realized-revenue-ledger.live.test.ts:41-90 (superPool seed / appPool assert / setBrandGuc). Brand A data, query as Brand B → no_data. assert current_user='brain_app'.
- **D-7 money:** new formatMoneyDisplay(minor:bigint,ccy) wraps @brain/money money() VO + Intl.NumberFormat (existing formatMoney at money/index.ts:123 is log-only "NOT for rendering"). No parseFloat/inline /100. no-float-money lint covers.
- **D-8 boundary:** analytics/index.ts (export{} :7) → export {getRevenueMetrics}; no cross-boundary ./internal import.
- **D-9 validation:** Fastify schema as_of pattern ^\d{4}-\d{2}-\d{2}$ optional → 400 INVALID_DATE; omitted → server new Date().
- **D-10/D-11:** no new deployable (analytics in apps/core monolith, card in apps/web); ZERO migrations (read-only). Affected-only deploy, ArgoCD sync, Phase-4 canary deferral (ADR-010). 
- **D-12 testids:** realized-revenue-card / -value / provisional-revenue-value / realized-revenue-no-data.
**Single-Primitive:** CLEAN extend-only — ONE engine (consume), ONE withBrandTxn, ONE @brain/money (+1 UI display wrapper, NOT a 2nd money model), ONE BffEnvelope, ONE card shell template (data-status-card), ONE DASHBOARD_QUERY_KEY (brand-switcher already invalidates), ONE brain_app harness. New: 1 svc file + 1 route block + 1 card + 1 client adapter + 1 formatter + 3 tests. No new service/table/queue/deployable/ADR.
**Slices (smallest-first, COMMIT PER SLICE):** S1 analytics svc + BFF route + rawPgPool thread (backend) → S2 card + client + formatMoneyDisplay + hook + page mount (frontend) → S3 engine==BFF/honest-empty/isolation-brain_app/provisional-separate live tests + dashboard.spec e2e (both). Both tracks parallel from §4 frozen contract. Branch feat/analytics-api-dashboard (base metric-engine HEAD).
**ADR:** none new — ADR-002 (this IS sole-read-path realization), ADR-001 (RLS), ADR-010 (deploy), I-S07/I-E02. Decision-log note: registerBffRoutes +optional rawPool param (additive, no break).
**Artifact:** 03-architecture-plan.md. **Next:** @backend-developer + @frontend-web-developer — Stage 3 (two parallel tracks).
