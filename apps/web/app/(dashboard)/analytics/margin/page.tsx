/**
 * Margin & Costs page — server shell. Renders CM1/CM2 + the cost-input form (feat-cm2-cost-inputs).
 */
import { MarginContent } from './margin-content';

export const metadata = { title: 'Margin & Costs — Brain' };

export default function MarginPage() {
  return <MarginContent />;
}
