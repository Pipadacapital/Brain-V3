-- SPEC:D.2 — compiled metric view: net_revenue (v1) @ grain=month · entity=semantic_order
-- GENERATED by @brain/semantic-metrics compiler — DO NOT EDIT. Change the YAML + recompile.
-- Tenancy: the BRAND_PREDICATE sentinel (below) is replaced with `brand_id = ?` by the serving seam.
CREATE OR REPLACE VIEW iceberg.brain_serving.mv_metric_net_revenue_month_slow AS
WITH base AS (
  SELECT
    brand_id,
    date_trunc('month', conversion_at) AS period,
    currency_code,
    channel,
    SUM(net_revenue_minor) AS net_revenue_minor
  FROM iceberg.brain_serving.semantic_order
  WHERE conversion_at IS NOT NULL
    AND ${BRAND_PREDICATE}
  GROUP BY 1, 2, 3, 4
)
SELECT
  brand_id,
  period,
  currency_code,
  channel,
  net_revenue_minor,
  (net_revenue_minor) AS net_revenue
FROM base;
