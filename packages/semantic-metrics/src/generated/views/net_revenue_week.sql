-- SPEC:D.2 / §1.11.1 — compiled INTERACTIVE metric view: net_revenue (v1) @ grain=week.
-- Reads the Spark pre-agg iceberg.brain_serving.preagg_net_revenue_week (covered dims). Long-tail/uncovered dims → the
-- base-entity fallback view iceberg.brain_serving.mv_metric_net_revenue_week_slow. Tenancy: BRAND_PREDICATE sentinel → `brand_id = ?`.
-- GENERATED by @brain/semantic-metrics compiler — DO NOT EDIT. Change the YAML + recompile.
CREATE OR REPLACE VIEW iceberg.brain_serving.mv_metric_net_revenue_week AS
SELECT
  brand_id,
  period,
  currency_code,
  channel,
  net_revenue_minor,
  (net_revenue_minor) AS net_revenue
FROM iceberg.brain_serving.preagg_net_revenue_week
WHERE ${BRAND_PREDICATE};
