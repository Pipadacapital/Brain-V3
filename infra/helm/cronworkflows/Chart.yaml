apiVersion: v2
name: cronworkflows
description: Brain scheduled batch jobs — Argo CronWorkflows running the core/stream-worker image job entrypoints (decision-engine detectors, attribution reconcile, revenue finalization).
type: application
version: 0.1.0
appVersion: "0.0.0"
