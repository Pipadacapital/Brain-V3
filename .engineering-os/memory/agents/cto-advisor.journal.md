# Engineering Advisor — Journal

> Append-only. See /Users/rishabhporwal/.claude/plugins/cache/engineering-os/engineering-os/2.3.1/docs/role-empowerment-model.md for entry shape.

## 2026-06-15T07:19:27Z — system — bootstrap
**Action:** Journal initialized by /eos-init on 2026-06-15T07:19:27Z.

## 2026-06-15T12:00:00Z — Engineering Advisor (cto-advisor) — context-sync/doc-08-v1.5
**Stage:** context-sync · **Action:** Foundation-amendment assessment (doc-03 §5 + doc-08 §36/§37) · **Decision:** PARTIALLY WARRANTED
**Rationale:** Absorb §37 field-complete dict + 5 reserved domains + connector-registry ext as context (no Canon change). Two targeted notes warranted: METRICS.md money-rule += reporting_currency_value_minor approved FX-normalization pattern (doc 08 §36 Delta 1); TRIGGER-SURFACES.md compliance row += tax_regime enum/region are live Phase-1 model fields requiring the compliance surface lane on any change. All other Canon files (STACK, HLD, INVARIANTS, COMPLIANCE) are NO-CHANGE. GCC GTM stays Phase 5; Iceberg+StarRocks ADR-002 confirmed. · **Next:** Stakeholder approves or rejects the two AMEND recommendations; if approved, a Foundation amendment edits METRICS.md + TRIGGER-SURFACES.md.

## 2026-06-17T02:35:00Z — Engineering Advisor (cto-advisor) — feat-analytics-api-dashboard
**Stage:** 1 (compressed, personas folded) · **Action:** Intake + adversarial stress pass · **Personas:** none spawned (folded) · **Decision:** ADVANCE
**Rationale:** All blockers shipped (feat-metric-engine-parity facacfe + full ledger spine). 4 HIGH findings: (1) `realized-revenue.ts:71` `?? '0'` creates a `0n` that is indistinguishable from "no finalized rows" — D-2 requires explicit `state:'no_data'` discriminant; (2) `analytics/index.ts` stub + BFF direct-query pattern risks ad-hoc SUM regression — D-3 sole-read-path must be architecturally enforced and tested; (3) 8 prior BFF envelope mismatches — D-1/D-5 binds exact `{request_id,data}` envelope and `const {data}=` unwrap in client; (4) dev superuser masks RLS — D-6 isolation test must use `brain_app` NOBYPASSRLS (already wired in CI from parity gate). 2 MEDIUM: provisional-separate (D-4), money formatting utility (D-7). 12 architect bindings declared (D-1..D-12). Tier-0 deterministic throughout. No new deployable. · **Next:** Architect (Stage 2) — bindings D-1..D-12 are the contract

## 2026-06-16T00:00:00Z — Engineering Advisor (cto-advisor) — feat-access-onboarding-flow
**Stage:** 1 · **Action:** Intake review + Canon-conflict reconciliation · **Personas:** Identity & Session Abuse Red-teamer:sonnet, Scope & Product-Realism Skeptic:sonnet · **Decision:** ADVANCE (scoped)
**Rationale:** Spec names Authentik OIDC, Google one-tap, MFA, Redis sessions, rotating refresh tokens — all Canon-conflicting with D0.1 (app-native M1 sealed). Scoped 10 acceptance criteria that fix the Stakeholder's tested gaps within the app-native stack and no new infrastructure: rotating refresh tokens (Postgres-only AC-1), membership-remove/suspend revocation (AC-2), rate limiting via Redis CacheAdapter (AC-3), brand schema additions currency/timezone/revenue_definition (AC-4), onboarding progress persistence column on organization (AC-5), 4-step wizard Step 3+4 (AC-6), invited-email sign-up guard (AC-7), multi-org selector (AC-8), session context improvements (AC-9), audit coverage gaps (AC-10). 3 Stakeholder decisions required (SD-1: confirm Authentik/Google/MFA deferred; SD-2: rotating refresh now; SD-3: revocation-on-role-change policy). 5 items deferred as child requirements. Lane high_stakes confirmed + schema_changes added. · **Next:** Orchestrator spawns 2 sonnet personas; synthesis pass → Architect (Stage 2) on ADVANCE

## 2026-06-15T21:37:25Z — Engineering Advisor (cto-advisor) — feat-access-onboarding-flow
**Stage:** 1 (synthesis pass) · **Action:** Persona synthesis (02a identity-abuse + 02b scope-realism) → 02c-intake-synthesis.md · **Decision:** ADVANCE
**Personas synthesized:** Identity & Session Abuse Red-teamer:sonnet (9 concerns, 2 CRITICAL, 4 HIGH, 2 MED, 1 LOW) + Scope & Product-Realism Skeptic:sonnet (7 concerns, 3 HIGH, 3 MED, 1 LOW). No concern dropped. All CRITICAL/HIGH folded into ranked must-address list (MA-01 through MA-16). 10 finalized ACs with persona modifications incorporated. Canon amendment: conditional only — none required if `revenue_definition` CHECK constraint ships without `placed` in M1; METRICS.md amendment needed only if `placed` is included. Lane high_stakes CONFIRMED. Build tracks: backend-developer + frontend-web-developer.
**Rationale:** Both CRITICALs are implementation-absent gaps (refresh endpoint does not exist; revocation not wired into service layer). 6 HIGHs cover harden-the-CRITICAL-path concerns plus design-level issues the Architect must resolve before migration schema is committed (MA-09 onboarding_status placement, MA-12 revenue_definition enum). 5 MEDs are correctness/maintainability issues within same sprint.
**Next:** Architect (Stage 2) — resolve MA-09 binding option decision + MA-12 before schema migration; then build tracks backend + frontend in parallel.

## 2026-06-15T11:09:57Z — Engineering Advisor (cto-advisor) — chore-platform-foundations-sprint0
**Stage:** 1 · **Action:** Intake review · **Personas:** Sprint-0 Over-Engineering Skeptic:sonnet, Isolation + Secrets Hardness Skeptic:sonnet · **Decision:** ADVANCE
**Rationale:** Requirement is sound — problem/user/success metric/constraints all Canon-aligned; no INVARIANT violated; no frozen ADR re-opened. Lane confirmed high_stakes with 6 trigger surfaces (scan's multi_tenancy + 5 added: schema_changes, system_of_record_audit, secrets_auth_iam, iac, shared_contract_parity). Five challenge findings raised as Architect directional input: Sprint-0 2-week cap risk (C1), StarRocks row-policy gap (C2), dbt over-scope (C3), DQ framework scope ambiguity (C4), pixel scope in CI (C5). Four "make it less dumb" findings: Authentik on EKS deferral candidate, LiteLLM gateway deferral candidate, staging/prod full-apply scope ambiguity, output-format framing clarification. No escalation triggers met.
**Next:** Persona synthesis (2 sonnet personas spawn in parallel via orchestrator), then ADVANCE to Architect.

## 2026-06-16T00:27:32Z — Engineering Advisor (final-reviewer) — feat-access-onboarding-flow
**Stage:** 6 · **Verdict:** PASS (GO) · **Paradigm audit:** clean (Tier-1, $0 model spend)
**Gates re-run (captured on this machine):** full suite 75/75 turbo + core 74/74 (family-wipe.live 3/3 NON-skipped, 53ms); validity_check exit 0; **family-wipe under brain_app: set_config=3 rows / no-GUC=0 rows (real negative control — the regressed path)**; Playwright 3/3 live (4-step + ghost-404 + resume); BFF rate-limit single-count 1-5=401/6=429 (Redis counter=6); brand CHECK rejects placed+USD; tenant-isolation no-GUC=0/wrong-WS=0/brain=16.
**Acceptance:** 11 MET / 0 scaffold / 0 gap · open CRITICAL/HIGH = 0 · Stakeholder complaint MET (4-step + currency/timezone + resume).
**Scope/Canon:** all 5 deferred items OUT; +1 sanctioned dep (ioredis); no new table/service; no hard-rule deviation. Honesty spot-check: r2 false-done /onboarding/advance now genuinely registered (bff.routes:363).
**Auto-rule:** first-occurrence root cause (inert test masked live-PG RLS defect) — below ≥3 threshold; watch-item only, no rule-proposal written.
**Next:** Stakeholder gate (Stage 7).

## 2026-06-16T04:38:00Z — Engineering Advisor (cto-advisor) — feat-multi-brand
**Stage:** 1 · **Action:** Intake review (design reconciliation + RLS gap analysis + scope definition) · **Personas:** Brand-Switch-Abuse Isolation Red-teamer:sonnet, Scope-and-Product-Realism Skeptic:sonnet · **Decision:** ADVANCE
**Rationale:** Two prerequisites shipped and verified. Three key design decisions resolved: (1) set-brand is a NEW BFF endpoint — not an extension of set-org — because the membership check is the 3-arg brand-level form and the audit event is distinct; (2) no schema change needed — the 3-GUC session already carries brand_id/workspace_id/role and re-mint is a read+JWT-sign operation; (3) brand_self_read RLS policy is MISSING (P0 blocker — brand_isolation only allows reads when GUC=brand_id, so list-my-brands returns zero rows under brain_app without it). Migration 0013_brand_self_read.sql is required in this slice. 7 findings surfaced; F-1 (missing RLS) and F-3 (missing endpoint) are P0. No Canon amendment. 2 Stakeholder decisions required (route URL shape, dashboard summary focus).
**Next:** Orchestrator spawns 2 sonnet personas in parallel; synthesis pass → Architect (Stage 2) on ADVANCE

## 2026-06-16T05:20:00Z — Engineering Advisor (cto-advisor) — feat-multi-brand
**Stage:** 1 (synthesis pass) · **Action:** Persona synthesis (02a brand-switch-abuse 10 concerns + 02b scope-realism 7 concerns) → 02c-intake-synthesis.md · **Decision:** ADVANCE
**Personas synthesized:** Brand-Switch-Abuse Isolation Red-teamer:sonnet (10 concerns: 2 CRITICAL, 4 HIGH, 3 MED, 1 LOW) + Scope-and-Product-Realism Skeptic:sonnet (7 concerns: 3 HIGH, 2 MED, 2 LOW). No concern dropped. All CRITICAL/HIGH folded into ranked must-address list (MA-01 through MA-08). Full list MA-01 through MA-15 (2 CRITICAL, 6 HIGH, 4 MED, 3 LOW).
**Rationale:** Both CRITICALs address the same root defect from different attack angles: (C1-abuse + C3-realism) both confirm that `refreshSession`/`resolveActiveContext` cannot be used for set-brand because there is no `preferredBrandId` parameter and the fallback `findActiveByUser` selects the wrong brand. Consolidated into MA-01: use `mintSessionToken` directly with explicitly resolved `ActiveContext`. (C2-abuse) cross-org workspace spoofing via body → MA-02: workspaceId from JWT only. 6 HIGHs address role carry-over (MA-03), RLS policy workspace scoping + soft-delete trap + NN-1 (MA-04), sessionPreHandler mandate (MA-05), brand-summary cache invalidation + adapter fix + BFF scoping (MA-06), M1 org-level invariant documentation (MA-07), create-brand routing isolation (MA-08). 7 finalized ACs with all persona modifications incorporated. Canon: no amendment. Lane high_stakes confirmed. Build tracks: backend-developer + frontend-web-developer in parallel after migration 0013 deploys.
**Next:** Architect (Stage 2) — resolve implementation order: 0013 migration first; then AC-1 set-brand endpoint; then AC-3/AC-4/AC-5 frontend tracks in parallel.

## 2026-06-16T10:56:00Z — Engineering Advisor (final-reviewer) — feat-multi-brand
**Stage:** 6 · **Verdict:** PASS · **Recommendation:** APPROVE · **Paradigm audit:** clean (Tier-0 deterministic, 0 model calls; Single-Primitive reuse; no over-engineering)
**ONE invariant (I-S01):** HOLDS — independently re-ran isolation-fuzz (11/11 PASS, NOSUPERUSER NOBYPASSRLS, cross-brand connector read=0, negative-control policy_off=1/policy_on=0 non-inert). SEC-MB-1 body-spoof closed (organizationId from JWT). 0013 live in pg_policies.
**Gates re-run (≥3, captured):** isolation-fuzz pg.test.ts 11/11 · switch-brand.live.test.ts 4/4 (incl 2 negative controls) · critical-paths.test.ts 22/22. Typecheck core+web EXIT 0.
**Both VETO gates:** FAIL→bcfee81→DELTA PASS legitimate, blocking:false on both; negative_control[] present + non-inert.
**Deferred:** QA-3 (audit correlation_id column) ship-as-techdebt; SEC-MB-4 (audit-after-mint, LOW) ship-as-techdebt. Neither touches isolation/auth.
**Scope creep:** none (no new migration/endpoint/secret beyond plan; STACK/ADR unchanged). **No AC unmet.**
**Commit-hygiene finding (action for gate):** spine UNCOMMITTED in working tree — 0013_brand_self_read.sql (untracked) + bff.routes.ts + auth.service.ts + brand.service.ts (modified). Mechanical commit command provided (11-final-review.md §7); not a code defect.
**Auto-candidate rule:** NOT fired — "tenant/workspace id from body not JWT" root-cause class recurs in 2 distinct runs (this + feat-access-onboarding-flow set-org); below ≥3 threshold; recorded as watch-item, no rule-proposal written (human runs /adopt-rule).
**Next:** Stakeholder gate (/approve). Intended state: stage 6 complete, awaiting-stakeholder, owner=stakeholder. Did NOT advance to Stage 7/deploy.

## 2026-06-16T20:40:00Z — Engineering Advisor (final-reviewer) — feat-members-team-management
**Stage:** 6 · **Verdict:** PASS · **Recommendation:** APPROVE · **Paradigm audit:** clean (Tier-0 deterministic, 0 model calls; Single-Primitive reuse — no second suspend mechanism; no over-engineering)
**Requirement:** DELIVERED — lifecycle Invited→Active→Suspended→Removed operable end-to-end (invite/accept/role/suspend/reactivate/remove + pending resend/revoke), hierarchy-enforced, e2e 4/4 standalone.
**Security spine (spot-verified by READING code, not trusting reports):** createInvite D-6 bound (invite.service.ts:97-100); suspendUser atomic single rawPgPool BEGIN/COMMIT + actor/target authority from DB + owner block + D-9 org assertion + post-COMMIT audit brand_id correct (auth.service.ts:766-868); reactivateUser distinct, no session revoke (875-953). LIVE brand_admin→brand_admin escalation CLOSED (403, wire-tested WIRE-2).
**Gates re-run (captured on this machine):** vitest workspace-access 69/69 PASS (6 files; 3 wire tests executed not skipped) — reproduced QA delta exactly. core typecheck EXIT 0; web typecheck EXIT 0. 43ea557 is-ancestor → true.
**Both VETO gates legitimate:** Security PASS (0 blocking; 5 CRITICAL + 3 HIGH closed). QA FAIL→fcbc221(test-only, 2 files)→DELTA PASS; WIRE-1 read at :366 confirmed non-inert (live session, 200-before control, real suspendUser, 401 SESSION_REVOKED after). Real fix, not a wave-through.
**Deferred dispositions:** SEC-V1 ship-as-techdebt (functional claim effectively resolved by WIRE-1; cross-org isolation independently proven NC-1/2/3 + WIRE-3 under brain_app — residual is two assertion queries' hygiene, not a runtime risk); SEC-V2 LOW ship-as-techdebt (dead path); F-QA-4 ship-as-techdebt (audit correlation_id column, pre-existing schema, needs migration); F-QA-5 ship-as-techdebt (rate-limiter test-infra, pre-existing). None touch the security spine.
**Scope/Canon:** no scope creep (diff confined to workspace-access + apps/web members surface + contracts + 0014 + run artifacts); no new ADR/stack/secret/dep; migration 0014 additive (I-E02); no Single-Primitive violation; no hard-rule deviation. No AC unmet.
**Auto-candidate rule:** "wire-level assertion required for auth/isolation claims (not DB-row/service-throw proof)" — root cause of r1 bounce; below ≥3-distinct-prior-runs threshold; watch-item only, NO rule-proposal auto-written (human runs /adopt-rule). Dev-superuser-masks-RLS durable rule honored end-to-end.
**Next:** Stakeholder gate (Stage 7). Intended state: stage 6, awaiting-stakeholder, owner=stakeholder. Did NOT advance to deploy / did NOT write state/active.json.

## 2026-06-16T21:20:00Z — Engineering Advisor (final-reviewer) — feat-data-plane-ingest-spine
**Stage:** 6 · **Verdict:** PASS · **Recommendation:** APPROVE · **Paradigm audit:** clean (deterministic-only, 0 model spend; Single-Primitive — one spool/one dedup/one Bronze table, brand_id at every layer; no over-engineering; M1 Zod-local validate is the sanctioned simplification 02 §133)
**Requirement:** DELIVERED — doc 05 §14 exit met: hello-world event flows collector→Redpanda→Bronze behind RLS, contracts regenerated. Full-wire e2e (pipeline-wire.e2e.test.ts, dcf2d55) proves it end-to-end (real subprocess, real TCP, real Redpanda, in-proc consumer, brain_app asserted) — non-inert.
**Spine spot-verified by READING code (not trusting reports):** accept-before-validate structural (collect.route + accept-event.usecase import zero kafka; ACK=spool INSERT commit pre-200); offset-commit-after-write (CollectorEventConsumer autoCommit:false, commit only on written/dedup/DLQ); RLS write path BEGIN→set_config(...,true)→INSERT ON CONFLICT→COMMIT as brain_app; dedup Redis NX + PK backstop.
**Gates re-run (captured on this machine):** stream-worker lint EXIT 0 (SR-01 closed); stream-worker typecheck EXIT 0; live RLS shape (bronze_events rls=t force=t, two-arg policy, INSERT+SELECT only); LIVE fail-closed under SET ROLE brain_app reproduced by me — no-GUC=0, wrong-brand=0, correct-brand=1, superuser=1 (false-pass trap proven); iceberg diff empty + 0015/0016 status A.
**Both VETO gates legitimate:** Security FULL BOUNCE SR-01 (real ESLint NN-7 CI failure, test-only) → 3567196 → DELTA PASS, migrations untouched. QA FULL FAIL F-QA-01 (real missing full-wire e2e) → dcf2d55 (additive) → DELTA PASS. Both bounces = gates working; neither fix weakened isolation/auth.
**D-4 amendment SOUND:** Postgres bronze_events is the pre-authorised D-4 fallback (02 §119), amendment on ADR-003, NOT a new stack layer/ADR. Iceberg target untouched. Header-marked dev/M1 staging mirror.
**Deferred dispositions:** SR-02 ship-as-techdebt (null-GUC test precision; production fail-closed re-proven live by me — test hygiene, not runtime risk); SR-03 ship-as-techdebt-CONDITIONAL (spool unvalidated body — acceptable ONLY under M1-internal no-external-traffic; must-fix before external exposure); SR-04/F-QA-05 rate-limit ship-as-techdebt-CONDITIONAL (same M1-internal caveat); F-QA-04 cosmetic. None touch the live-proven RLS spine.
**Scope/Canon:** no new ADR/STACK/docs; only 2 existing deployables touched; no hardcoded secret; migrations additive (I-E02); no Single-Primitive violation; no hard-rule deviation; no AC unmet.
**Auto-candidate rule:** none written — no root cause repeating ≥3 distinct prior runs (SR-01 lint-key + F-QA-01 wire-gap are run-local). Dev-superuser-masks-RLS durable rule honored end-to-end (every isolation probe under brain_app, superuser-trap explicitly proven).
**Next:** Stakeholder gate (Stage 7). Intended state: stage 6, awaiting-stakeholder, owner=stakeholder. Did NOT advance to deploy / did NOT write state/active.json.

## 2026-06-16T22:40:00Z — Engineering Advisor (final-reviewer) — feat-identity-graph
**Stage:** 6 · **Verdict:** PASS · **Recommendation:** APPROVE → Stakeholder gate · **Paradigm audit:** clean (Tier-1 deterministic, zero model calls, @effort present)
**Gates re-run (independently captured):** identity e2e 26/26 PASS (79ms); identity-core + stream-worker typecheck EXIT 0; RLS no-GUC identity_link=0 rows under SET ROLE brain_app; re-eval brand visibility superuser=171 vs brain_app=0 (SR-01 root cause confirmed at DB).
**Spine verified (code-read):** per-brand 32B salt hard-fail no-default (SaltProvider:88-123); no raw PII in identity_link (hash-only, raw→contact_pii); contact_pii dual-GUC gate (0017:239-244); RLS FORCE all 8 tables + NN-1 block; idempotent merge (deterministic merge_id + ON CONFLICT DO NOTHING). Negative controls real/non-tautological (superuser-sees vs brain_app-doesn't reproduced).
**Over-engineering/hard-rule:** CLEAN — only new dep @types/node; no new deployable/ADR/stack/argo; migration additive; merge_rule/merge_candidate/pii_vault_reference deferred. No hard-rule deviation; no Stakeholder-stop.
**Re-eval-gap call (SR-01/QA-04 MED):** phone-guard-reeval runs as brain_app and enumerates 0 brands under FORCE RLS → suppressions never expire, over-suppression accumulates. Fail-closed (no false-merge/leak), recoverable via fix+replay. ACCEPT as M1 techdebt; P1 must-fix before prod scale. Misleading comment (reeval.ts:41-42) claims an unwired superuser pool.
**Retro:** root cause = cross-tenant system job written against tenant-scoped brain_app under FORCE RLS (related to dev-db-superuser-masks-rls.md note, opposite direction). First occurrence in series — NOT ≥3-run recurrence → no rule-proposal; logged for watch.
**Next:** Stakeholder gate (Stage 7) — weighs the reeval-suppressions-never-expire gap as the sole residual risk.

## 2026-06-16T19:56:00Z — Engineering Advisor (final-reviewer) — feat-realized-revenue-ledger
**Stage:** 6 · **Verdict:** PASS · **Recommendation:** APPROVE → Stakeholder gate · **Paradigm audit:** clean (tier-0 deterministic, zero model calls — correct for a money substrate)
**Gates re-run (independently captured):** ledger live suite 30/30 PASS (306ms); append-only-by-GRANT — UPDATE+DELETE as brain_app (non-superuser, is_super=f) → permission denied, grants=SELECT,INSERT only; closed-sum NON-TAUTOLOGY live — realized_gmv_as_of=100000 vs naive SUM=200000 (DIFFER → fn load-bearing); closed-sum nets — +finalization/−refund → 0; single-currency trigger — AED into INR brand RAISEs. brand+ledger both FORCE RLS confirmed.
**Heart verified (code+live):** 4 load-bearing walls real — append-only by GRANT (not convention), no-double-count named fn excludes provisional, dual-date late-reversal posts new current-period row (June untouched), no-float BIGINT (Assertion-3 + grep clean). Negative controls non-tautological/non-inert (5 probes captured RED). Dedup index correctly hardened to timezone('UTC',occurred_at)::date (IMMUTABLE) vs plan's STABLE occurred_at::date.
**Over-engineering/scope/hard-rule:** CLEAN — additive 0018; no new deployable/ADR/stack/queue/Argo-type; only packages/money addition is roundToMinorBankers (1 fn); non-goals billing_run/fx_rate honored; doc-08 §7.1 date→CHAR(7) is an operational tightening (journaled, NOT a Canon amendment). No hard-rule deviation.
**F-SEC-01 call (HIGH, deferred):** revenue-finalization no-ops under brain_app FORCE-RLS brand enumeration → no provisional ever finalizes. FAIL-CLOSED (understate never overstate, no leak/double-count), recoverable (fix+replay; rebuildable from Bronze), no M1 consumer (metric engine is next slice). ACCEPT as M1 techdebt; P1 must-fix before prod (SECURITY DEFINER enumeration fn / superuser-scoped pool). Stakeholder owns the conscious accept.
**Retro / recurrence:** root cause = cross-tenant system job enumerating FORCE-RLS `brand` under brain_app with no GUC → 0 rows → silent no-op. This is the SAME pattern as identity phone-guard-reeval (run c9a1a0, SR-01/QA-04) — now **2nd distinct occurrence**. The identity final-review explicitly logged "first occurrence, no rule-proposal, logged for watch." Now at 2 of the ≥3 auto-write threshold → I do NOT adopt a rule; I RECOMMEND the Stakeholder file one and added it to pending-stakeholder-attention.md. A 3rd occurrence crosses the auto-candidate bar.
**Next:** Stakeholder gate (Stage 7) — weighs F-SEC-01 conscious-accept + the recommended rule-proposal; deploy is affected-only core+stream-worker + finalization manifest, NO canary.

## 2026-06-17T01:48Z — Engineering Advisor (final-reviewer) — feat-metric-engine-parity
**Stage:** 6 · **Verdict:** PASS (APPROVE) · **Paradigm audit:** clean (tier-0, zero model calls verified)
**Gates re-run:** typecheck (engine+oracle EXIT 0); test:parity 16/16 GREEN + live RED PROOF captured (TS=50001 vs 50000 delta=1 FAIL@tol0 -> revert PASS); test:unit 9/9; turbo --affected --dry-run (both pkgs in scope); validity_check clean (9 files). All replicated by reviewer.
**Heart spot-verified at source:** reference.ts imports ONLY `pg` type (the 2 metric-engine hits are comments), raw SQL `recognition_label='finalized' GROUP BY currency_code` — genuinely non-tautological. RED proof = hard assertions (toBe(false)/toBe(1n)). CI pr.yml now provisions postgres:16 + brain_app NOBYPASSRLS + migrate:up BEFORE test:parity (line 61<69), no continue-on-error — SEC-001 bounce was a REAL fix not a wave-through. Engine reads ONLY via realized_gmv_as_of inside withBrandTxn (correct-by-construction).
**Deferred:** F-SEC-02 (old GetRealizedGmvAsOf autocommit-GUC gap) — re-confirmed still unwrapped at source; NOT regressed (new path uses withBrandTxn); ship as carried tech-debt, must-fix-before-Phase-2. SEC-003/QA-F1/QA-F2 LOW -> ship, M2.
**Recurrence / auto-candidate:** root cause this run = none (clean money/parity path; the prior FORCE-RLS-enumeration pattern at 2/3 does NOT recur here — engine reads per-brand under GUC, no cross-tenant enumeration). No new rule-proposal from this run.
**Hard-rule check:** no dependency/Single-Primitive/compliance/paradigm/gate-skip deviation. Scope honored (only realized+provisional; no new deployable; 0020 additive; eslint fence = bug-fix).
**Next:** Stakeholder gate (Stage 7) — weighs F-SEC-02+SEC-003 carried tech-debt + QA M2 deferrals + the merge of feat/metric-engine-parity.
