-- SPEC:D.2 / §1.11.1 — compiled INTERACTIVE metric view: cm3 (v1) @ grain=month.
-- Reads the pre-agg iceberg.brain_serving.preagg_cm3_month (covered dims). Long-tail/uncovered dims → the
-- base-entity fallback view brain_serving.mv_metric_cm3_month_slow. Tenancy: BRAND_PREDICATE sentinel → `brand_id = ?`.
-- GENERATED by @brain/semantic-metrics compiler — DO NOT EDIT. Change the YAML + recompile.
CREATE OR REPLACE VIEW brain_serving.mv_metric_cm3_month AS
SELECT
  brand_id,
  period,
  currency_code,
  channel,
  cm3_minor,
  (cm3_minor) AS cm3
FROM iceberg.brain_serving.preagg_cm3_month
WHERE ${BRAND_PREDICATE};
