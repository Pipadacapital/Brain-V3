/**
 * Revenue Analytics page — server component shell.
 */
import { RevenueContent } from './revenue-content';

export const metadata = { title: 'Revenue — Brain Analytics' };

export default function RevenuePage() {
  return <RevenueContent />;
}
