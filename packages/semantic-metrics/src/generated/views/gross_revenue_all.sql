-- SPEC:D.2 — compiled metric view: gross_revenue (v1) @ grain=all · entity=semantic_order
-- GENERATED by @brain/semantic-metrics compiler — DO NOT EDIT. Change the YAML + recompile.
-- Tenancy: the BRAND_PREDICATE sentinel (below) is replaced with `brand_id = ?` by the serving seam.
CREATE OR REPLACE VIEW brain_serving.mv_metric_gross_revenue_all AS
WITH base AS (
  SELECT
    brand_id,
    currency_code,
    channel,
    SUM(order_value_minor) AS gross_revenue_minor
  FROM brain_serving.semantic_order
  WHERE ${BRAND_PREDICATE}
  GROUP BY 1, 2, 3
)
SELECT
  brand_id,
  currency_code,
  channel,
  gross_revenue_minor,
  (gross_revenue_minor) AS gross_revenue
FROM base;
