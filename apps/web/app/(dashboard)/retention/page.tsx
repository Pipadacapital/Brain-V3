/**
 * Retention — "Do customers come back?" (server shell).
 * Top-level tab #7. Cohort retention + repeat-purchase rate.
 */
import { RetentionContent } from './retention-content';

export const metadata = { title: 'Retention — Brain' };

export default function RetentionPage() {
  return <RetentionContent />;
}
