/**
 * Data Health page — server component shell.
 */
import { DataHealthContent } from './data-health-content';

export const metadata = { title: 'Data Health — Brain' };

export default function DataHealthPage() {
  return <DataHealthContent />;
}
