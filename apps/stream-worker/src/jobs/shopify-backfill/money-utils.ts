/**
 * money-utils.ts — Re-exports from @brain/shopify-mapper (A0 move, D-12).
 *
 * decimalStringToMinor has been MOVED to packages/shopify-mapper. Re-exported
 * here for backward compatibility with existing imports.
 */

export { decimalStringToMinor } from '@brain/shopify-mapper';
