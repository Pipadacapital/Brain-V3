image:
  repository: REPLACE_WITH_ECR_REGISTRY/brain-core-prod
  tag: ""              # B3: no mutable :latest — CI populates image.digest at deploy (fail-closed in the template)
  digest: ""

streamWorkerImage:
  repository: REPLACE_WITH_ECR_REGISTRY/brain-stream-worker-prod
  tag: ""              # B3: no mutable :latest — CI populates image.digest at deploy (fail-closed in the template)
  digest: ""

# dbt-runner image — runs the scheduled mart rebuilds. CI promotes the staging digest (build-once).
dbtRunnerImage:
  repository: REPLACE_WITH_ECR_REGISTRY/brain-dbt-runner-prod
  tag: ""
  digest: ""

# spark-bronze image — Iceberg Bronze sink + maintenance crons.
sparkBronze:
  image:
    repository: REPLACE_WITH_ECR_REGISTRY/brain-spark-bronze-prod
    tag: ""
    digest: ""

envSecretName: core-env

serviceAccount:
  name: brain-jobs
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::ACCOUNT_ID:role/brain-jobs-prod
