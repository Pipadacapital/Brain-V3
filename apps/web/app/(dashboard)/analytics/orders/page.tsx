/**
 * Orders Analytics page — server component shell.
 */
import { OrdersContent } from './orders-content';

export const metadata = { title: 'Orders — Brain Analytics' };

export default function OrdersPage() {
  return <OrdersContent />;
}
