# Architect — Journal

> Append-only. See /Users/rishabhporwal/.claude/plugins/cache/engineering-os/engineering-os/2.3.1/docs/role-empowerment-model.md for entry shape.

## 2026-06-15T07:19:27Z — system — bootstrap
**Action:** Journal initialized by /eos-init on 2026-06-15T07:19:27Z.

## 2026-06-16T20:10:00Z — Architect — feat-data-plane-ingest-spine
**Stage:** 2 · **Paradigm:** deterministic-only (zero model calls; HTTP edge + pg spool + KafkaJS + Zod + Redis SETNX + SQL INSERT) · **Tracks:** @backend-developer (collector accept-before-validate edge + spool + drainer) ∥ @data-engineer (stream-worker consume→dedup→Bronze sink + DLQ)
**D-4 RESOLVED → Postgres `bronze_events` staging-table fallback (M1 only).** Decisive: no production-grade TS Iceberg writer exists (repo grep clean: zero iceberg/parquet/pyiceberg deps); Nessie is catalog-metadata-only (does not accept row writes); mature writers = JVM/PyIceberg = Phase-3-deferred (STACK.md:46/60, locked-choice #7). The fallback is the one D-4 pre-authorised (02 §119). NO new stack layer (Postgres = PersistenceAdapter). ADR-002/003 amendment NOTE only, not a new ADR; target (Bronze=Iceberg) unchanged; Phase-3 flip = mechanical column-map. **bronze_table.sql/bronze_spec.json untouched** (Phase-3 target spec).
**Isolation RE-BOUND for Postgres sink:** D-8 = Postgres RLS (two-arg current_setting, FORCE RLS, brain_app INSERT+SELECT-only, NN-1 assertion inherited) instead of Iceberg partition predicate. All isolation tests under SET ROLE brain_app (superuser brain = false pass; MEMORY note).
**Contract fixes (Slice 1, before any consumer/Apicurio):** F-6/D-5 ingest_at→ingested_at (Zod); F-1/D-6 envelope occurred_at/ingested_at number→string ISO-8601; codegen regen .avsc committed same commit (I-E01).
**Single-Primitive:** clean — ONE spool, ONE dedup (Redis + PK backstop), ONE bronze_events (event_type as column), brand_id at every layer. Extends @brain/events + tenant-context + 0001 RLS pattern.
**Slices:** 1 spike-confirm+contract+migrations(0015 spool,0016 bronze) → 2 collector edge → 3 stream-worker sink → 4 e2e+durability+dedup+isolation tests → 5 deploy track. COMMIT PER SLICE (prior runs lost ~61min to infra timeout). **Artifact:** 03-architecture-plan.md. **Next:** @backend-developer + @data-engineer — Stage 3.

## 2026-06-15T11:34:04Z — Architect — chore-platform-foundations-sprint0
**Stage:** 2 · **Paradigm:** deterministic/infrastructure (zero model calls; managed-vs-self-host already bound in STACK.md) · **Tracks:** A(backend-developer) B(platform-devops) C(platform-devops) D(data-engineer) E(data-engineer+backend-developer)
**Single-Primitive:** clean — repo sealed with correct topology (apps/packages/tools/db/infra all stubbed); builders fill stubs, no new deployable/db/ledger/package. **Encoded:** NN-1..NN-7 + 9 scope rulings + EC10 operational definition (dev full-apply / staging structural-scaffold-compute-zero / prod plan-only) as per-track pass-1 acceptance. Critical path = Track C (Terraform); D/E develop code legs against local docker-compose day 1, gate live legs on C. Track B carries the deploy pipeline (affected-only build + ArgoCD staging-auto/prod-promote + auto-rollback + flag-off).
**Artifact:** 03-architecture-plan.md (run 2026-06-15T11-06-27Z__09ba6e). **Next:** A,B,C,D,E builders — Stage 3 (Group 1 day-1 parallel; A→D contracts-skeleton handoff EOD day 1).

## 2026-06-15T15:00:00Z — Architect — context-sync:datamodel-v1.5 (doc 08 §36/§37 + doc 03)
**Stage:** context-sync (no spawn) · **Paradigm:** deterministic/data-modeling (zero model calls) · **Frozen-architecture:** INTACT — no new primitive (5 reserved domains = Silver tables modeled-not-built; connector-registry + region cols = additive on existing Postgres/StarRocks tiers; only 2 ledgers remain). **Region seam:** additive-columns-now, RegionAdapter posture UNCHANGED (Phase-5 GTM trigger holds). **M1 delta:** field-complete §37 dict + new Silver dims (ad_account/campaign/set/creative, product_variant, inventory_level, order_line_item, order_status_history, refund, shipment_tracking_event) + region/tax cols (region, tax_regime, reporting_currency_value_minor). **§37.11 promote-via-contracts** = consistent with I-E01. **Artifact:** architect-assessment.md. **Next:** data-engineer — M1 canonical migration (contract-first, additive-only).

## 2026-06-16T01:50:00Z — Architect — feat-access-onboarding-flow
**Stage:** 2 · **Paradigm:** Tier-1 deterministic (zero model calls; $0/mo — every item is a DB txn / Redis counter / enum→URL table / CHECK constraint; a model call here = paradigm-bypass) · **Tracks:** B-MIG,B-1..B-8,DEPLOY(backend-developer) + F-1..F-4(frontend-web-developer)
**Single-Primitive:** CLEAN (extend-only) — extends user_session(rotation lineage), UserSessionRepository(+2 bulk-revoke), BrandRepository(+3 cols), organization(+onboarding cols), one RateLimiter, one AuditWriter; CSRF nets DOWN to one (removed BFF duplicate). No new service/table/queue/primitive.
**Decisions (binding):** MA-09 → onboarding_status on `organization`, Option A, first-brand-only (no brand_id exists at Step 1; Canon "brand=tenant" is isolation not UX; enum pending→org_created→brand_created→integration_selected→complete maps 1:1 to resume routes). MA-12 → revenue_definition CHECK=('realized','delivered'), 'placed' EXCLUDED, NO Canon amendment (grep-confirmed METRICS.md has no placed_revenue; MER is "never placed/gross"). CRITICAL 2/2 closed in design (B-1 rotation+FOR UPDATE+family-wipe; B-2 same-txn revoke-on-all).
**Migrations:** 0010_brand_locale, 0011_onboarding_state, 0012_session_rotation_lineage (each up+down, NN-1 negative-control note; next free int after 0009; renumber on collision). Branch from latest feat/onboarding-session-context (rebase risk flagged).
**New endpoints:** POST /auth/token/refresh, POST /bff/session/set-org, POST /bff/session/onboarding/advance. BFF contract: needs_onboarding boolean → onboarding_status enum across login/refresh/set-org.
**Artifact:** 03-architecture-plan.md (run 2026-06-15T21-18-00Z__a7a965). **Next:** backend-developer + frontend-web-developer — Stage 3 parallel against §6 contract.

## 2026-06-16T22:15:00Z — Architect — feat-identity-graph
**Stage:** 2 · **Paradigm:** Tier-1 deterministic (zero model calls; $0/mo — parse→SHA-256→hash-equality lookup→count-threshold; `@effort("deterministic")` on writer; a model call here = paradigm-bypass) · **Tracks:** @data-engineer SINGLE-TRACK (whole graph + bridge + tests; no backend-developer track — no core-module API seam warrants it for M1; `identity` core-module stays `export {}` until a consumer needs it).
**Bindings RESOLVED D-1..D-7 + C-1 stub-crypto + C-2 migration:**
- **D-2 salt (CRITICAL):** per-brand 32B salt at brand-creation → `brand.identity_salt_ciphertext`; `SaltProvider` EXTENDS existing `apps/core/.../SecretsProvider` (Single-Primitive — do NOT invent 2nd secrets abstraction); fetch/decrypt at bridge startup; **HARD CRASH on failure** (`salt.length!==32 → throw`, never empty/default/global = cross-brand correlation = ONE-invariant breach). `LocalSecretsProvider` dev stand-in (already throws on empty). CI: 2 brands → different hashes.
- **D-1 phone-guard (CRITICAL):** `brand.phone_guard_threshold INT DEFAULT 10` + `suppression_window_days DEFAULT 30` (brand-configurable, NOT hardcoded); `shared_utility_identifier` + `suppressed_until`; windowed distinct-brain_id count > threshold → suppress from merge-key set. Re-eval = EXISTING Argo-job type (D-7). False-merge (ghost high-LTV in ledger) > over-suppression (recoverable) → N=10 leans guard-strong. Windowed (not lifetime) for cool-down. N=10 boundary test.
- **C-1:** `stubSha256` (MurmurHash, `identity-core/src/index.ts:90`) DELETED → `node:crypto` real SHA-256 (replay-correctness heart; non-crypto hash = 2nd brain_id per rebuild). Known-vector + cross-brand-differs CI.
- **D-4:** deterministic `merge_id=sha256(brand_id‖canonical‖merged‖rule_version)`; ON CONFLICT DO NOTHING on `identity_merge_event` PK + 2 UNIQUE PARTIALs (alias one-live-pointer, identity_link strong-id); at-least-once + idempotent writer; replay 3× → 1 merge row.
- **D-3:** `contact_pii` elevated RLS = `brand_id` AND `app.role='send_service'` (BOTH two-arg fail-closed); dev `pii_value text` plaintext stand-in (prod KMS = `0018` follow-up, not M1); brain_app w/o send_service → 0 rows.
- **D-5:** deterministic-only `rule_version='v1-deterministic'`; **`merge_rule`/`merge_candidate`/`pii_vault_reference` DEFERRED** (CTO §6 — constant rule version suffices; no mart consumer yet → smaller/more-reversible migration).
- **D-6:** E.164 phone norm (`regionCode` param, `+91` default — NOT hardcoded in pure fn); `09876543210`≡`+919876543210`; un-normalizable → low-confidence, no crash.
- **D-7:** bridge inside EXISTING stream-worker process (added to `main.ts`); no new deployable/Argo-job-type/container.
**C-2 migration `0017_identity_graph.sql`:** +brand cols (ALTER ADD IF NOT EXISTS — brand 0004 has NEITHER salt NOR threshold col yet); all identity tables ENABLE+FORCE RLS, two-arg `current_setting('app.current_brand_id',TRUE)`, REVOKE+minimal GRANT (append-only INSERT+SELECT; shared_utility_identifier+UPDATE for re-eval; brain_id_alias+UPDATE for Phase-2 unmerge), copy NN-1 DO-block from `0004_brand.sql:61`. Additive (I-E02); down=DROP (identity = derived projection, rebuildable from Bronze, not immutable SoR).
**C-3 identity-bridge** (`apps/stream-worker/src/identity-bridge/` was EMPTY dir): `IdentityBridgeConsumer.ts` copies `CollectorEventConsumer.ts:29` (autoCommit:false, offset-after-write, DLQ@5); `IdentityRepository.ts` copies `BronzeRepository.ts:53` (set_config-then-write-one-txn, brain_app); `ResolveIdentityUseCase`/`IdentityResolver`/`SharedUtilityPolicy`/`SaltProvider`.
**Single-Primitive:** CLEAN — ONE identity primitive (core `identity` ctx, written by bridge); salt reuses SecretsProvider; ONE hasher (identity-core); merge_rule deferred = fewer primitives. Reuses tenant-context, NN-1 pattern, stream-worker layering.
**Isolation:** ALL RLS tests under `SET ROLE brain_app` (dev superuser `brain` MASKS RLS — MEMORY note `dev-db-superuser-masks-rls.md`).
**Slices (smallest-first, COMMIT PER SLICE — prior runs lost time to infra timeouts):** 1 mig 0017 + real-crypto + SaltProvider → 2 deterministic resolver + idempotent bridge writer → 3 phone-guard suppression + contact_pii gate + re-eval job → 4 deploy (affected-only, EXISTING stream-worker ArgoCD app, health-probe rollback; NO canary — Phase-4 deferred ADR-010). Branch `feat/identity-graph` off master.
**ADR:** none new — within ADR-008 (identity/salt), ADR-007 (KMS/secrets), ADR-001 (RLS), ADR-010 (deploy, honors Phase-4 canary deferral). Recorded: 0017 = first identity migration (none 0001-0016), additive, no layer change.
**Artifact:** 03-architecture-plan.md. **Next:** @data-engineer — Stage 3 (single track).
