{
  "name": "@brain/events",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "test:unit": "vitest run --exclude '**/*.live.test.ts' --passWithNoTests",
    "test:live": "vitest run .live"
  },
  "devDependencies": {
    "@types/node": "25.9.3",
    "vitest": "^2.1.0"
  }
}
