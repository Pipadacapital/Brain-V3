-- SPEC:D.2 — compiled metric view: orders (v1) @ grain=all · entity=semantic_order
-- GENERATED by @brain/semantic-metrics compiler — DO NOT EDIT. Change the YAML + recompile.
-- Tenancy: the BRAND_PREDICATE sentinel (below) is replaced with `brand_id = ?` by the serving seam.
CREATE OR REPLACE VIEW iceberg.brain_serving.mv_metric_orders_all AS
WITH base AS (
  SELECT
    brand_id,
    channel,
    COUNT(DISTINCT order_id) AS order_count
  FROM iceberg.brain_serving.semantic_order
  WHERE ${BRAND_PREDICATE}
  GROUP BY 1, 2
)
SELECT
  brand_id,
  channel,
  order_count,
  (order_count) AS orders
FROM base;
