-- SPEC:D.2 / §1.11.1 — compiled INTERACTIVE metric view: cm3 (v1) @ grain=day.
-- Reads the Spark pre-agg iceberg.brain_serving.preagg_cm3_day (covered dims). Long-tail/uncovered dims → the
-- base-entity fallback view iceberg.brain_serving.mv_metric_cm3_day_slow. Tenancy: BRAND_PREDICATE sentinel → `brand_id = ?`.
-- GENERATED by @brain/semantic-metrics compiler — DO NOT EDIT. Change the YAML + recompile.
CREATE OR REPLACE VIEW iceberg.brain_serving.mv_metric_cm3_day AS
SELECT
  brand_id,
  period,
  currency_code,
  channel,
  cm3_minor,
  (cm3_minor) AS cm3
FROM iceberg.brain_serving.preagg_cm3_day
WHERE ${BRAND_PREDICATE};
