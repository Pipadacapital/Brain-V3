# 01 — Delta Plan (Phase 0)

Verdict per requirement, Waves A–D + cross-cutting (§0.5, §1.10, §1.11, §1.7, §1.3) + E–I scaffold baselines.
Evidence = code file:line or live metadata (see 00-foundation-synthesis.md for full context). DONE = verified with real evidence; the "remaining work" cell for DONE items is the verification test to write per §0.3.4.

**Verdict totals: DONE 4 · PARTIAL 28 · MISSING 21 (53 rows).**

---

## Wave A — Identity

| Spec § | Verdict | Evidence | Remaining work / test-to-write |
|---|---|---|---|
| A.1.1 pixel identify event | PARTIAL | `brain.identify` exists in served asset (pixel-asset.route.ts:284–294, :315; plain sha256 email → `identify {hashed_customer_email}`); form-submit auto-bridge :394–412; consumed as pre_hashed_email strong tier (extract-identifiers.ts:91,199). No pixel.identify.v1 in live Apicurio (1 artifact only); pixel-sdk has NO identify (capture.ts:77–102). | (1) pixel.identify.v1 envelope {identifiers:{email_sha256?,phone_sha256?}, source, consent_state} registered in Apicurio; (2) phone support; (3) source field ('form_autodetect'\|'explicit_api'\|'login_hook'); (4) replace submit-listener with MutationObserver + blur on input[type=email\|tel]/autocomplete targets, gated by brand flag `pixel.autodetect.enabled`, NEVER near input[type=password] (current code violates this); (5) sessionStorage hash-dedupe (today per-page in-memory); (6) add identify to pixel-sdk OR unify the build first (pre-Wave-A item, see WA-03). |
| A.1.2 consent model | PARTIAL | Client consent(): __brainConsent → Shopify customerPrivacy → PIXEL_CONSENT_DEFAULT=granted env (pixel-asset.route.ts:142–164, :552); Silver R3 quarantines ABSENT consent_flags → silver_consent_rejected (silver_collector_event.py:224–243; _silver_technical.py:567–613). | (1) Per-brand config `{identity_capture: off\|explicit_only\|autodetect, consent_source}` on tenancy.brand (columns don't exist) — migration must seed current behavior or existing installs zero out (AMD-04); (2) IAB TCF __tcfapi reader; (3) Silver denied-VALUE drop for identifies (today presence-only: analytics:false passes) + test; (4) consent_state field on identify; (5) client-side consent-gate identity capture. |
| A.1.3 normalization before hashing | PARTIAL | TS identity-core normalizeIdentifier (:180; email trim+lowercase :186 no NFC; phone custom regex, REGION_PREFIX = IN only :107–110); hashIdentifier salted :219–227; Python twin _raw_normalize.py:120–161; cross-language golden-vector tests _p4_golden/test_*_golden.py (fixture, not property-based). Gmail dots/plus untouched (matches ADR). | (1) Resolve salt-model amendment FIRST (AMD-01); (2) create packages/identity-normalization (TS) + mirrored Python module extracted from identity-core/_raw_normalize; (3) add NFC email normalization both languages; (4) libphonenumber E.164 for IN/AE/SA/QA/BH/KW/OM (unparseable → no identifier — fix Python fallback that returns cleaned digits); (5) property-based cross-language tests, 10k generated identifiers, 0 mismatches (A.5.2). |
| A.1.4 Shopify identity fields | PARTIAL | shopify-mapper/index.ts:412–446 + resources.ts:200–221 (salted hashes + storefront_customer_id); Spark twin silver_shopify_order_normalize.py:74–123; reaches Silver in payload JSON (silver_order_state.py:186). | Field-name + salt-policy decisions (AMD-01/AMD-02), then rewire onto shared normalization module; ratify payload-properties (+ promoted nullable Iceberg columns) as "the Silver canonical envelope" mechanism (AMD-05 — it is not Avro). |
| A.1.4 WooCommerce | PARTIAL | woocommerce-mapper/index.ts:313–389; Spark twin silver_woocommerce_normalize.py:164–166. | Same as Shopify (naming/salt/envelope); no extraction gap. |
| A.1.4 Shopflo | PARTIAL | shopflo-mapper/index.ts:255–288 (legacy names customer_email_hash/phone_hash), 393–401/503–505/715 (standard names + storefront_customer_id). | Same as Shopify + unify checkout_abandoned's legacy field names onto one convention (or the canonical pre_hashed_identifiers slot — currently populated by ZERO mappers). |
| A.1.4 GoKwik | PARTIAL | gokwik-mapper/index.ts:145–155, 466–468, 536–538 (hashes + storefront_customer_id; phone-first). | Same as Shopify. |
| A.1.4 checkout_session_id | PARTIAL | Shopflo ONLY: shopflo-mapper/index.ts:417–429 emits it. GoKwik CHECKOUT_ID_KEYS (:89) feeds only event_id/order_id — no property (verified :440–545). Pixel checkout events carry none (capture.ts:183–186). Not an identity key (extract-identifiers.ts = 0 hits). | Add checkout_session_id to gokwik order+checkout outputs (source keys already probed); capture on pixel checkout events; add to extract-identifiers as identifier type + stitch key set; optionally promote into silver_checkout_signal/silver_collector_event. |
| A.1.4 hash backfill job | MISSING | No rederive/rehash job in db/iceberg/spark (searched). Nearest: 7 shadow *_normalize jobs (TARGET_TABLE default shadow, cutover not done — silver_shopify_order_normalize.py:49–51), widen-backfill MERGE precedent (:213–218), FULL_REFRESH=1 mechanism. erasure_raw_delete.py empties raw for shredded subjects (implicit skip, not encoded). | Write the one-off Spark backfill (bronze raw → hash fields for historical silver rows, MERGE additive, explicit shredded-subject skip) — OR ratify shadow-normalize cutover + FULL_REFRESH as the mechanism. Blocked on AMD-01 (shadows are salted-hash). |
| A.1.5 bridge ingestion / graph model | PARTIAL | Live upsert: IdentityBridgeConsumer (:53) → ResolveIdentityUseCase → Neo4jIdentityRepository.writeOutcome (:291–380); graph 15,105 Identifier / 3,787 Customer / 15,128 IDENTIFIES. Edge = IDENTIFIES {tier,is_active,created_at} on (:Customer) — no OBSERVED_WITH/first_seen/last_seen/count/source; consumes Bronze topic, not Silver fields. | AMD-06: ratify Identifier→IDENTIFIES→Customer as the spec model; add first_seen/last_seen/source/count props (ON MATCH SET last_seen, count+=1) — prerequisite for the 90-day shared-device rule; decide Bronze-topic vs Silver-envelope ingestion. |
| A.1.5 ordered per-brand identity priority | MISSING | IdentityResolver.ts:137–230 = hard-coded TIER gating, identical all brands; grep identity_priority/priority_config = 0. Only per-brand knob = phone-guard (:78–81). Never-silent-overwrite honored (:225–229). | Versioned per-brand priority config (default [platform_customer_id, email_sha256, phone_sha256, anonymous_id]) threaded into IdentityResolver; highest-priority winner semantics; config-version stamp on outcomes. |
| A.1.5 / §1.5 bi-temporal identity_map appends | PARTIAL | Live schema valid-time only (effective_from/to, is_current, replaced_by_brain_id, merge_event_id, updated_at — Trino DESCRIBE; 14,902 rows). Batch projection of Neo4j (silver_identity_map.py, MERGE on brand+hash+brain_id+effective_from), not append-per-mutation; as-of(T_system) impossible. | AMD-07: add system_from/system_to columns + append semantics, OR ratify Iceberg snapshot time-travel as the system axis (note 7-day TTL conflict — see AMD-10). Then make map mutations bi-axial appends. |
| A.2.2 sanctioned views + CI guard | MISSING | identity_current_v / identity_asof = 0 hits (only private _je_* temp views, gold_journey_events.py:146–182); mv_silver_identity_map.sql = all-intervals, 0 callers; tools/lint has no raw-read guard. | Create identity_current_v (WHERE is_current) + identity_asof view/function (Trino + Spark-side seam; reuse _snap_as_of.py pattern); lint script failing direct silver_identity_map reads outside views, wired into pr.yml; repoint existing readers. |
| A.2.3 multi-key session stitch | PARTIAL | Stitch is single-key Node cron (journey-stitch-from-identity.ts:106–155, anon-only, unambiguous-only, ORDER grain → PG connector_journey_stitch_map). Resolver upstream IS multi-key (ResolveIdentityUseCase.ts:250–360) but never projected to sessions. silver_session_identity / silver_stitch_conflicts = 0 hits. | Build Spark session-grain stitch v2: silver_session_identity {brand_id, session_id, brain_id, matched_via[], stitch_version, stitched_at} + silver_stitch_conflicts; identifier set {anon, checkout_session_id, email, phone}; resolve via identity_current_v; dual-write ops.silver_identity_link flag-controlled; ambiguous → conflict row never guess (A.5.3). |
| A.2.3(4) shared-device 90d rule | MISSING | Exactly-one half exists twice (IdentityResolver.ts:214–229; stitch job :143–154); no last_seen stored on edges (grep=0), no 90d/per-brand window anywhere. | Depends on A.1.5 last_seen; then per-brand recency window in resolver medium-adoption + stitch. |
| A.2.3(5) re-stitch on identity change | MISSING | identity.map.changed = 0 hits; live lane {dev,prod}.identity.{minted,linked,merged,suppressed,review_queued}.v1; IdentityChangeRecomputeConsumer does mart-scoped recompute, not identifier-keyed re-stitch; de-facto re-stitch = accidental full-scan every run. | AMD-08: extend existing identity.* lane vs new topic. Emit on every map mutation keyed brand_id+identifier_hash; stitch consumes, re-runs steps 1–3 for sessions containing that identifier within attribution lookback (carries most of the >40% target; A.5.5). |
| A.2.4 merge/unmerge audited | PARTIAL | Merge: MergeEvent + ALIAS_OF intervals (Neo4jIdentityRepository.ts:354–380) + reversible audit.identity_audit (IdentityAuditDecisionLog.ts) + identity.merged.v1. Unmerge exists e2e: identity.routes.ts POST → merge-admin.ts:82 → neo4j-identity-reader.ts:298 (ALIAS_OF close, lifecycle 'split') + UI button. Re-versioning exists (gold_journey_events_reversion.py, merge-only). Survivor = LOWEST UUID (IdentityResolver.ts:286–288), not older; no actor; unmerge emits no event and no journey restoration. | (1) AMD-09 survivor rule (lowest-UUID vs older — changing breaks merge_id replay determinism); (2) unmerge → identity event + restoration path in reversion job; (3) actor on MergeEvent/audit; (4) A.5.7 merge→re-version→unmerge→restoration round-trip test. |
| A.2.5 stitch performance | MISSING | Stitch is not Spark: Node cron, per-brand FULL scans (:110), no watermark/partitioning/baseline. | Net-new with stitch v2: incremental (silver_job_watermark pattern from #300), partition brand_id+event_date, broadcast/sort-merge policy, 5M-event <20min baseline recorded in gate, 4GB local profile. |
| A.3 probabilistic quarantine (Splink) | PARTIAL | Rule-based review-gated matcher live (ProbabilisticMatcher.ts, 'probabilistic-fellegi-sunter', weak signals, score capped, never auto-merges → MergeReview, 20 nodes). Quarantine holds by construction. Splink/silver_probabilistic_stitch/customer_sessions_extended_v/model docs/estimated:true = 0 hits. | Net-new Splink Python Spark job + silver_probabilistic_stitch (≥0.95) + union view + knowledge-base/models/splink-<v>.md (holdout precision ≥0.98) + gateway estimated:true + the zero-probabilistic-rows-in-attribution DATA test (A.5.6; today structural only). AMD-12: replaces vs sits beside rule-based matcher (matcher-registry supports both). |
| A.4(1) realtime Neo4j upsert + ≤50ms p99 | PARTIAL | Upsert LIVE (stream-worker running; IdentityBridgeConsumer wired main.ts:343; 15,105 identifiers). No latency benchmark (grep p99/50ms = unrelated hit only). | Write 1k-event replay benchmark asserting ≤50ms p99 added; record in GATE-A. |
| A.4(2) Redis touchpoint zset cache | MISSING | redis-cli SCAN: only serving-cache + ratelimit keys; zero tp: keys; no consumer code (grep=0). | New consumer group: zset `{brand_id}:tp:{brain_id}` last-200, score=ts, TTL 30d sliding, deterministic-only; merge handler on identity.merged.v1 (union→survivor, delete absorbed); AnalyticsCacheInvalidateConsumer is the wiring template. Must use sanctioned key builder. |

## Wave B — Journey

| Spec § | Verdict | Evidence | Remaining work |
|---|---|---|---|
| B.1 canonical journey generation | PARTIAL | gold_journey_events.py builds versioned journey_events from silver_touchpoint (live: 24 cols, 6,743 rows; data_version/is_current/sequence_number/brain_id_asof); anonymous_ placeholder :217; deterministic touchpoint_id; idempotent MERGE :96; monotone sequence :268–271. NO matched_via/identity_basis (live schema); silver_session_identity absent; versioning per-touchpoint not per-journey; tiebreak touch_seq not (session_id, event_seq). | Add matched_via + identity_basis='deterministic' columns (additive ALTER; matched_via requires Wave A stitch provenance or derivation from silver_identity_map identifier_type). AMD-11: keep (touchpoint_id, data_version) key, expose derived journey-level version — retrofitting journey_version violates §0.5 non-breaking. Input = silver_touchpoint until silver_session_identity ships (AMD-13). |
| B.2 cross-device re-versioning | PARTIAL | gold_journey_events_reversion.py: merge detection via silver_identity_map (:174–178), chain resolve (:72), flip-then-copy N+1 (:224–239), crash-safe watermark (:166–171). Batch poll, not Kafka; identity.map.changed.v1 nonexistent; journey_version_log = 0 hits; merge-only causes. | (1) journey_version_log {brand_id, brain_id, from_version, to_version, cause, at} written from the reversion job (all fields in hand at step 5); (2) event-driven trigger off identity.merged.v1 / AMD-08 lane (today latency = refresh-loop cadence); (3) unmerge/restitch cause handling. |
| B.3 journey APIs | PARTIAL | Events analog: GET /api/v1/analytics/journey/events (analytics-journey.routes.ts:930; keyset cursor, contract analytics.api.v1.ts:253). Trace analog: /journey/timeline (:864, via connector_journey_stitch_map). Compare = 0 hits. NO Redis cache on journey routes (serving-ttl 'journey' tier unused); tenant-from-session done. | Extend EXISTING routes (AMD-14, don't parallel-build): X-Journey-Version header (max(data_version) or version-log read), matched_via/session_id/url_path fields (blocked on B.1), trace lookback window + identity_evidence [{identifier_type, first_seen, source}] from silver_identity_map, new compare endpoint w/ t_minus_conversion_ms, wrap all three in ServingCacheReader ('journey' tier, {brand_id}: keys), contract + p95 latency tests (<300ms warm/<2s cold). |
| B.4 replay & explainability (?as_of=) | MISSING | No as_of/replayed/FOR TIMESTAMP AS OF anywhere in serving (grep). Blocks exist: brain_id_asof pair (gold_journey_events.py:243–258), bi-temporal(valid) identity map, snap_identity_link + _snap_as_of (test-proven), full version history retained. | Build as_of path batch-only with replayed:true — via AMD-10: reconstruct from version history + identity-map intervals (NOT Iceberg time-travel; 7-day snapshot TTL makes it unusable). Pre-identification replay answerable today from brain_id_asof NULL/anonymous_ rows. |
| B.5.4 attribution-consumes-journey flag seam | MISSING | Attribution reads silver_touchpoint directly (gold_attribution_credit.py:120 + reconcile-attribution.ts twin) — never journey_events. | Flagged read-path switch (silver_touchpoint → journey_events current view) in BOTH drivers + parity on golden with identity held constant; then document Stitch-v2-on delta. |

## Wave C — Measurement

| Spec § | Verdict | Evidence | Remaining work / test-to-write |
|---|---|---|---|
| C.1 verify ledger recognition + reconciliation | DONE | Code-verified gold_revenue_ledger.py:284–315 (provisional/finalization-prepaid-horizon/cod_delivery_confirmed/cod_rto_clawback/cancellation/refund; deterministic ledger_event_id; MERGE idempotent); attribution basis :73. Tolerances recorded: parity toleranceMinor=0n (parity.test.ts:250,258–263); attribution residual exact; DQ delta=100 warn/2h freshness error. 3-way ₹1,746,754,034 exact — one-off (BRAIN-AUDIT-REPORT.md:480–482). | TEST: codify the 3-way reconcile (Bronze ⇄ silver_order_state ⇄ ledger provisional, tolerance 0 per currency) as a repeatable CI oracle — audit demands it before Wave C touches the ledger seam. Also AMD-15: spec "prepaid at capture" → provisional/finalized two-stage language. |
| C.2.1 refunds fact | PARTIAL | silver_refund.py:60–73 exists; fed by refund.recorded.v1 (resources.ts:288). LIVE 0 rows — 100% quarantined (84 rows refund_before_order) via mapper epoch-0 occurred_at BUG. | (1) FIX shopify refund mapper timestamp (prerequisite); (2) add order_line_id?/reason_code taxonomy (RTO first-class)/refund_method/initiated_at/settled_at/lineage; (3) gold fact (extend vs rename per AMD-16) + shred-manifest entry; (4) Woo/GoKwik refund emission. |
| C.2.2 settlements fact | PARTIAL | silver_settlement.py:53–75 (amount/fee/tax/settlement_id/at); razorpay lane real (manifest + mapper, 2y backfill); gold_settlement_summary.py registry-enabled. LIVE: silver 0 rows; gold table ABSENT (entity-incremental never runs ensure_gold_table on empty source). | Per-order/batch grain fact + settlement_batch_id; reconciliation job vs ledger (C.5.4 diff=0 on golden); fix ensure-table-on-empty; needs a live razorpay sync to validate. |
| C.2.3 fees fact | PARTIAL | Fees only inside razorpay settlements (fee_minor/tax_minor, silver_settlement.py:99–100) + pct-config cost_input types. No per-order fee fact; other connectors capture none. | Per-order fee fact from silver_settlement payment rows; extend connectors where fees exposed; gold_measurement_fees net-new. |
| C.2.4 costs fact | MISSING | Zero cost facts: shiprocket-mapper no freight fields; silver_shipment no money cols; product.upsert no cost field (resources.ts:85–101); no gold_product_costs/CSV ingest. Only seam: billing.cost_input (0055) + POST /api/v1/costs, 0 live rows. | Everything: shiprocket freight capture, reverse-logistics cost on returns, packaging default, COGS source (mapper extension where platform exposes, else CSV cost-sheet ingest + validation), gold_product_costs {brand_id, sku, cost_minor, currency, valid_from, valid_to} (cost_input sku-scope is the ancestor), gold_measurement_costs. |
| C.2.5 spend fact | DONE | silver_marketing_spend delivers canonical day×channel×campaign grain: PK (brand_id, spend_event_id), stat_date/platform/campaign_id/spend_minor/currency (silver_marketing_spend.py:1–80); LIVE 30,482 rows; served + consumed by CAC/campaign/CM marts. | TEST: idempotent + currency-partitioned aggregation assertion + lineage-column mapping (spend_event_id=source_event_id, platform=source_system). AMD-16 decides alias-as-gold_measurement_spend vs ratify silver table as the fact. |
| C.2.6 inventory fact | PARTIAL | silver_inventory_level.py:63–71 (point-in-time levels from product.upsert.v1), scheduled. LIVE 0 rows despite 7 live product.upsert.v1 events, no quarantine — unexplained. | (1) Investigate the 0-row gap (silver_product_variant also 0); (2) derive MOVEMENT from level deltas or new source; (3) flag gating + gold mart. |
| C.3 gold_order_economics + gold_product_economics | PARTIAL | gold_contribution_margin.py LIVE (14 rows incl. 3-decimal currencies) but brand×currency LIFETIME totals from pct_bps config; TS twin contribution-margin.ts is the served path; silver_order_state lifecycle exists (:283–345) but no economics_state/settlement link; cost_input empty → all cogs=0. | Per-order economics net-new: fact-based CM (measured refunds/fees/costs), cm3_allocation_basis (deterministic-attributed vs day×channel pro-rata), economics_state provisional\|settled\|reversed mapped onto recognition states + settlement facts, RTO reverse-cost, product rollup. AMD-17: CM1/CM2/CM3 naming collision (live CM1 = spec CM2) must be resolved first. Also pick ONE CM authority (Spark mart vs TS compute) or add parity gate. |
| C.4 marts-migration flag + parity | MISSING | No 'marts_migration' flag (grep clean); marts read Silver directly (gold_cac.py:1–40, gold_campaign_*, gold_executive_metrics; metric-engine ROAS on mv_silver_marketing_spend + mv_gold_revenue_ledger). | Flag (needs platform-flags), read-path indirection in 4–5 consuming marts + metric-engine, line-by-line parity harness (copy parity-oracle pattern; deltas = new fees/costs, never revenue). |
| C.5.1 lineage endpoint | MISSING | No /v1/metrics/{metric}/lineage route in the 17 frontend-api route files (recon route inventory). | GET /v1/metrics/{metric}/lineage?date= returning fact tables + row counts + job versions; registry readSeam field is the seed. |
| C.5.5 is_new_customer per order | MISSING | No per-order flag in silver_order_state/ledger/executive marts. Customer-grain ingredients: silver_customer.py:78–79 first_seen_at/first_identified_at (Neo4j-sourced); monthly gold_cac.new_customers; cohort marts. | Window over silver_order_state per brain_id (first recognized order) or join vs first_identified_at; define NULL-brain_id semantics; thread into CAC/ROAS/executive (aMER/cac_new). |
| §1.2 3-decimal money | DONE | money/src/index.ts:33–42 BHD/KWD/OMR=3 (+JPY 0, CLF 4); banker's rounding returns adjustment_minor; tests lock KWD=3/'KWD 12.500'; live CM rows carry BHD/KWD/OMR minor units unblended. | TEST (C.5.3): e2e KWD golden order Bronze→order_state→ledger→CM, zero rounding loss, no hardcoded /100 outside the package. |

## Wave D — Semantic

| Spec § | Verdict | Evidence | Remaining work |
|---|---|---|---|
| D.1 semantic entities + deprecation map | MISSING | No semantic_* views/tables (grep db/trino/views + metric-engine); knowledge-base/semantic/ EMPTY. Proto-entities live: mv_gold_customer_360, mv_journey_events_current/journey_timeline, mv_gold_product_detail, mv_gold_campaign_performance/attribution, mv_silver_order_state/order_line. | Author semantic_customer/order/product/campaign/journey as thin compositions over listed mv_* (customer_360 nearly 1:1; semantic_order needs Wave C economics; semantic_campaign needs spend+attribution join); deprecation-map.md; lint blocking NEW consumers of deprecated marts (extend v4-naming-guard pattern). |
| D.2 metric registry (packages/semantic-metrics) | MISSING | No package/YAML/compiler//v1/semantic/metrics route (grep=0). Seed: TS METRIC_REGISTRY, 22 certified metrics w/ readSeam/toleranceMinor (registry.ts:116); METRIC_ID_ENUM auto-derived; ${BRAND_PREDICATE} runtime tenancy (trino-deps.ts) not compiled current_brand(). | Create package: YAML per metric, compiler → Trino views + Spark pre-aggs (interactive:true) + JSON catalog GET /v1/semantic/metrics + TS types + compiled-SQL snapshot tests; migrate the REAL 22 (not spec's "16"); tenancy via BRAND_PREDICATE discipline or session-property equivalent (Trino REST has no row-policy, ADR-0007 D3). mer/amer/cm*/refund_amount/return_rate/cac_new/identified_purchase_rate blocked on Wave C facts. |
| D.3 consumer migration + BAI convergence | PARTIAL | Convergence partially holds by construction: BAI (ask.routes.ts → askBrain → metric-engine) and dashboards share one chokepoint for the 22 metrics (I-ST01 sole emitter). No compiled views to migrate to, no per-brand flags, no per-endpoint parity harness. | After D.2: semantic.serving flag (needs platform-flags), per-endpoint old-vs-compiled parity tests, route-by-route migration (ServingCacheReader.enabled pass-through is the safe-OFF pattern). |
| D.4 acceptance (same-number, catalog, cross-tenant, deterministic_only, deprecation lint) | MISSING | All depend on D.1/D.2 artifacts that don't exist. | Same-number test ×10 golden scenarios to the minor unit; MCP-shaped tool def per metric; cross-tenant test on compiled views; deterministic_only exclusion proof; deprecation lint. |

## Cross-cutting (§0.5, §1.10, §1.11, §1.7, §1.3)

| Spec § | Verdict | Evidence | Remaining work |
|---|---|---|---|
| §0.5 platform-flags | MISSING | No flag system: grep isEnabled/getFlag/featureFlag = 0; only env booleans (config/core.ts:39,:77,:87; stream-worker.ts:111). Prior layer deliberately removed (#295). | Create packages/platform-flags: Redis-backed per-tenant, `{brand_id}:flag:{name}` via sanctioned key builder (no-raw-redis-key rule), default OFF, hexagonal port + ioredis adapter + static-env fallback; DECIDE how Python Spark jobs read flags (PG tenancy column or job env — no Python Redis convention exists). Precondition for every wave's gates. |
| §0.5 ESLint hexagonal boundary rule | PARTIAL | eslint.config.mjs rich (app-to-app ban, reach-around ban, metric-engine fence, no-raw-redis-key, no-float-money, no-pci-card-fields) — module fences, NOT ports-and-adapters. **lint:boundaries FAILS: 16 errors/3 warnings** (14 metric-engine fence violations: ml, notification, recommendation×3, workspace-access…; 2 missing-rule-definition: EraseSubjectUseCase.ts:90, Backfill.ts:263). | (1) Pay the 16-error debt (widen fence deliberately or reroute through analytics); (2) fix/strip the 2 stale disable-directives; (3) ADD the hexagonal rule (ban ioredis/kafkajs/neo4j-driver/trino imports outside */infrastructure\|adapters/); (4) green before it can gate Wave A. |
| §1.10 golden dataset | MISSING | No packages/testing-golden/generator/snapshot harness. Primitives: pixel-fixture send-event/seed-touchpoints, tools/seed, k6 load-test, parity-oracle comparison pattern. | Build packages/testing-golden: seeded PRNG, ~50k events, 3 brands, scenario matrix (anon-only, anon→known mid-session, multi-device, shared-device, COD, refunds, GCC 3-decimal, consent-off) through pixel-fixture/Kafka lanes; snapshot Silver/Gold/mv outputs; flags-OFF byte-compare wired into gates. REQUIRED before any Wave A behavior change. |
| §1.11.1 pre-aggs + interactive/slow flags | PARTIAL | Gold marts are Spark Iceberg pre-aggs, mv_* projection-only (ADR-0007 D5); known-metric routing can't reach ad-hoc (query-route.ts). No interactive:/slow: flags in any registry; pre-aggs not registry-derived. | Add flags to D.2 registry, bind metric×grain → pre-agg mart; p95<10s ad-hoc budget test. |
| §1.11.2 Redis result caching | PARTIAL | Live superset: `${brandId}:${metricId}:${paramsHash}:${servingVersion}` keys, 5 TTL tiers, stampede locks, brand-scoped invalidation (analytics-cache.ts/serving-ttl.ts; ADR-0007 D2). Gaps: no realtime/no-cache class; **crypto-shred never invalidates cache** (erasure path publishes no cache.invalidate — TTL is sole backstop, max 60m). | Wire ErasureOrchestratorConsumer → cache.invalidate.v1 scope.all (reuse CacheInvalidatePublisher, one injection); AMD-18 ratify live key shape; add no-cache tier. |
| §1.11.3 per-brand Trino concurrency gate | MISSING | No semaphore/queue in trino-adapter.ts/trino-deps.ts (grep=0); only per-key stampede guard + bounded JVM. | Per-brand admission gate at the single trino-adapter chokepoint: `{brand_id}`-keyed counter, max-concurrent + FIFO queue + timeout, config, load test. |
| §1.7 schema governance | PARTIAL | Live Apicurio: 1 artifact (collector.event.v1, AVRO), NO compatibility rule (/admin/rules=[] despite compose env FULL_TRANSITIVE); validateSchemaCompatibility hits nonexistent endpoint, 404→"compatible" (events/index.ts:130–136) — silent no-op; ~48 other topics JSON+zod only; naming `{env}.{domain}.{name}.v{N}` (+unversioned `{env}.brain.{entity}` exception). | (a) AMD-03 BACKWARD-vs-FULL_TRANSITIVE + Avro-everywhere-vs-ratified-JSON+zod; (b) idempotent boot step CREATING the global rule via REST (env var provably insufficient, registry in-mem); (c) fix the 404-as-pass client bug; (d) register schemas for new program topics; (e) record which naming pattern new topics follow. |
| §1.3 crypto-shred completeness | PARTIAL | pii-vault per-brand+per-subject DEK + 6-step EraseSubjectUseCase (shred_subject_keyring 0115, erase_contact_pii 0100, tombstone, scoped re-projection, CAPI delete, FORCE-RLS log; idempotent, DLQ). Gaps: step 4 Iceberg snapshot compaction REGISTERED-DISABLED (resurrectable from old snapshots); no cache invalidation on erase; knowledge-base/privacy/ empty (no shred-manifest); lake tiers hash-only (indirect satisfaction, undocumented). | Write shred-manifest.md (envelope columns + hash-only rationale); implement or formally amend snapshot-expiry compaction; erasure→cache.invalidate publish; register every new subject-linked table (Wave A/C additions). |

## Waves E–I scaffold baselines (contracts only)

| Spec § | Verdict | Evidence | Remaining work |
|---|---|---|---|
| E AI feature layer | PARTIAL | gold_ai_features.py + mv sql + ai-features.ts read seam exist in CODE but table/view ABSENT live, not in refresh loop; shape WIDE current-state not EAV PIT. No Redis feat: hashes, no /v1/features, no packages/ai-features, no shred manifest. | AMD-19 first (feature-store DDL vs CI-enforced "features are RUNTIME" invariant + v4-naming-guard); then PIT EAV DDL, `{brand}:feat:*` contract, 501 stub behind flag, YAML registry, shred registration. |
| F AI platform infra | PARTIAL | litellm.config.yaml ≈ F.1 complete but service commented out (compose :194–223); client seam live; ai_provenance exists (no tokens/cost); MCP 11 read-only tools + writeToolCount===0 CI; no execution_mode (grep=0), recommend-only hardcoded; resolver-prompt.ts git-backed, no prompts/ dir. | Re-enable litellm (`ai` profile); ops_llm_calls table + success-callback logger; AMD-20 restate MCP no-Trino contract as seam-only (transitive dep is false-by-construction); execution_mode enum default 'suggest'; prompts/ + loader. |
| G recommendations scaffold | MISSING | gold_recommendations = 0 hits; but a WORKING rule-based surface ships (decisions.routes.ts, detectors, confidence-gate, ledger 0082, UI). | gold_recommendations DDL (evidence/explainability enforced); AMD-21: grandfather existing surface, scope 501 stub to the NEW schema-backed endpoint only (verbatim spec would regress shipped feature). |
| H decision engine scaffold | MISSING | gold_decisions / decision-policies = 0 hits. Proto: append-only decision_log, model_registry 0083, reversible identity decision-log — none carries candidates/EV/policy_version. | gold_decisions DDL (candidates + per-candidate EV + constraints) + packages/decision-policies YAML compiler skeleton, flags OFF. |
| I action platform scaffold | MISSING | action.requested/execution_mode/holdout = 0 hits; no executor port. Fail-closed house pattern established (NotImplementedYet precedents). | 5 Avro envelopes registered (BACKWARD per AMD-03) + TS ExecutorPort + 4 NotImplemented adapters behind flags; contract tests only. |
| MCP baseline map (CONTRACT-F input) | DONE | 11 tools verified: mcp-tools.ts:76–191 SoT, dispatch-wiring.ts mount, brand from McpPrincipal, zod I/O, CI writeToolCount===0 (isolation-fuzz/mcp.test.ts); Trino reached only transitively via certified seams. | TEST: dependency-graph test asserting MCP tools reach Trino ONLY through certified metric-engine read seams (the real invariant), pending AMD-20. |

---

## CONTRADICTIONS & AMENDMENT CANDIDATES

Each per §0.4: conflicting spec text vs ground truth, two candidate resolutions, recommendation. File as `knowledge-base/amendments/AMD-<n>-<slug>.md` before the dependent work item starts.

**AMD-01 — salt model (A.1.3/§1.3) [BLOCKS: identity-normalization pkg, connector rewiring, backfill, stitch keys]**
Spec: uniform plain SHA-256. Ground truth: THREE conventions — per-brand SALTED sha256(salt||'||'||normalized) in all 4 mappers + Spark twins (identity-core:219, _raw_normalize.py:146; cross-brand-uncorrelatability a tested invariant); plain UNSALTED client-side in pixel (pixel-asset.route.ts:270–295); hash_salted_bytes for AWB/UTR. **Live bug:** pixel's unsalted hash can never match the connectors' salted `hashed_customer_email` — the anon→known bridge is broken today. (R1) Ratify dual-convention: plain sha256 = interop space (pixel + platform-hashed), salted = internal space; resolver keeps them as distinct tiers; ADD connector-side dual-write of the unsalted interop hash so pixel↔connector joins work. (R2) Migrate everything to plain sha256 — orphans every existing salted stitch key, destroys cross-brand-uncorrelatability, impossible to salt client-side anyway. **Recommend R1.**

**AMD-02 — identity field names (A.1.4)**
Spec: email_sha256/phone_sha256/platform_customer_id. Ground truth: hashed_customer_email/hashed_customer_phone/storefront_customer_id live in resolver + Silver jobs (+ Shopflo legacy names; unused canonical pre_hashed_identifiers slot). (R1) Amend spec to adopt existing names; move mappers onto the canonical slot as the single mechanism. (R2) Introduce spec names verbatim → second parallel namespace in identity links + migration. **Recommend R1.**

**AMD-03 — schema governance posture (§1.7)**
Spec: Avro everywhere, BACKWARD. Ground truth: 1 registered artifact; NO live compat rule (env var doesn't materialize one); repo doctrine FULL_TRANSITIVE; ~48 topics JSON+zod; compat client 404→pass no-op bug. (R1) FULL_TRANSITIVE for Avro artifacts + ratify JSON+zod for existing non-collector lanes; new program envelopes (pixel.identify.v1, action.*) Avro-registered; fix rule-creation (idempotent boot step) + client URL bug. (R2) Wholesale Avro+BACKWARD migration of 48 topics — huge, out-of-scope churn. **Recommend R1.**

**AMD-04 — consent default (A.1.2/§1.3)**
Spec: per-brand identity_capture default OFF. Ground truth: PIXEL_CONSENT_DEFAULT=granted collector-wide env, load-bearing for CMP-less stores; enforcement is presence-quarantine not value-drop. (R1) Per-brand config with migration seeding each existing brand's current effective behavior; default OFF for NEW brands only; add denied-VALUE drop for identifies. (R2) Flip default OFF globally → zeroes pixel identity data for live installs. **Recommend R1.**

**AMD-05 — "Silver canonical envelope Avro fields" (A.1.4)**
Ground truth: silver_collector_event is an Iceberg table (payload JSON varchar), not Avro; additive mechanism = payload properties + promoted nullable columns (anonymous_id/device_id precedent with widen-backfill MERGE). (R1) Amend wording to that mechanism. (R2) Introduce real Avro envelope for Silver — re-architecture, violates additive rule. **Recommend R1.**

**AMD-06 — Neo4j graph model (A.1.5)**
Spec: (:BrainId) + OBSERVED_WITH{first_seen,last_seen,source,count}. Ground truth: (:Identifier)-[:IDENTIFIES{tier,is_active,created_at}]->(:Customer) with 15k live nodes. (R1) Ratify existing model; add first_seen/last_seen/source/count to IDENTIFIES. (R2) Parallel edge type — duplicates 15k-edge graph. **Recommend R1.**

**AMD-07 — bi-temporality of silver_identity_map (§1.5)**
Spec: valid+system time appends. Ground truth: valid-time-only batch projection of Neo4j. (R1) Add system_from/system_to columns + append-per-mutation semantics (additive). (R2) Declare Iceberg snapshots the system axis — conflicts with 7-day snapshot TTL (AMD-10). **Recommend R1** (R2 only with a per-table retention exemption).

**AMD-08 — identity.map.changed.v1 (A.2.3(5)/B.2)**
Spec assumes the topic. Ground truth: 5-topic lane {env}.identity.{minted,linked,merged,suppressed,review_queued}.v1 + recompute consumer already live. (R1) Extend existing lane: emit map-mutation events keyed brand_id+identifier_hash on the existing topics (or one new sibling following the discovered convention), consumers unified. (R2) Add the 6th overlapping topic verbatim. **Recommend R1.**

**AMD-09 — merge survivor rule (A.2.4)**
Spec: survivor = older brain_id. Ground truth: LOWEST-UUID canonical (IdentityResolver.ts:286–288); deterministic merge_id + replay idempotency depend on it; 3 MergeEvents + 11 ALIAS_OF live. (R1) Keep lowest-UUID, amend spec. (R2) Switch to older-id → invalidates replay determinism for existing MergeEvents. **Recommend R1.**

**AMD-10 — replay via Iceberg time-travel (B.4)**
Spec: ?as_of= time-travel. Ground truth: SNAPSHOT_TTL_MS=7d swept daily; live journey_events snapshots span <1h. (R1) Reconstruct as-of from retained version history + bi-temporal identity intervals + snap_identity_link seam (all fully retained). (R2) Per-table snapshot-retention exemption for journey_events — storage growth, still bounded. **Recommend R1.**

**AMD-11 — journey_version grain (B.1/B.2)**
Spec: per-journey journey_version, MERGE on (brand_id, brain_id, journey_version, event_id). Ground truth: per-touchpoint data_version+is_current, PK (brand_id, touchpoint_id, data_version) — same event-sourced intent, incompatible key; PK change violates §0.5. (R1) Keep data_version; expose derived journey-level version (max(data_version)) for X-Journey-Version + journey_version_log. (R2) New PK → breaking. **Recommend R1.**

**AMD-12 — Splink vs rule-based matcher (A.3)**
Ground truth: review-gated Fellegi–Sunter rule matcher live (never auto-merges). (R1) Splink sits beside it (matcher-registry supports multiple matcher_ids); rule matcher keeps feeding MergeReview; Splink owns silver_probabilistic_stitch. (R2) Replace — loses working review pipeline. **Recommend R1.**

**AMD-13 — stitch input table (B.1)**
Spec input silver_session_identity (v2) doesn't exist; live input is sessionized silver_touchpoint. (R1) journey builder keeps silver_touchpoint until stitch v2 lands, then flag-switch. (R2) Block Wave B on Wave A entirely. **Recommend R1** (matches wave ordering anyway).

**AMD-14 — journey API paths (B.3) + "Analytics Gateway"**
Spec paths /v1/customers/{brain_id}/journey etc. on a standalone gateway. Ground truth: /api/v1/analytics/journey/{events,timeline} live on the core BFF; no standalone gateway exists (ADR-0007 seam; analytics-gateway removed #295). (R1) Extend/alias existing routes on the BFF seam. (R2) Parallel-build spec paths — duplicates two endpoints. **Recommend R1.**

**AMD-15 — recognition language (C.1)**
Spec "prepaid at capture". Ground truth: two-stage provisional_recognition-at-booking → finalization-after-horizon. (R1) Adopt provisional/finalized language. (R2) Add a capture trigger — changes a live ledger. **Recommend R1.**

**AMD-16 — gold_measurement_* namespace (C.2)**
Ground truth: silver_refund/settlement/marketing_spend/inventory_level + gold_settlement_summary/contribution_margin already exist. (R1) Extend/repoint existing tables; alias or view-map into the measurement namespace where the spec name is load-bearing. (R2) Parallel greenfield namespace — duplicate facts, parity burden. **Recommend R1.**

**AMD-17 — CM naming collision (C.3)**
Live CM1 = spec CM2; live CM2 = spec CM3, in a SERVED metric (metric-engine twin). (R1) Adopt spec numbering in NEW gold_order_economics; map/rename live outputs with an explicit re-labelling release note + contract version bump. (R2) Keep live numbering, amend spec. Either is viable; **recommend R1** (spec numbering is the industry convention) — but it must be an explicit, tested rename, never silent.

**AMD-18 — cache key/TTL shape (§1.11.2)**
Live implementation is a superset (metricId + servingVersion segments, 5 tiers, version-bump invalidation). (R1) Ratify live shape as satisfying §1.11.2; add no-cache tier. (R2) Adopt spec verbatim — regression. **Recommend R1.**

**AMD-19 — feature store vs "features are RUNTIME" invariant (E)**
CI guard (v4-naming-guard) forbids feature precompute; CLAUDE.md invariant; RETIRED_feature_customer_daily.md. (R1) Sanctioned exception for the E offline store with PIT semantics, named allowlist in the guard. (R2) Training reads = as-of over Silver/Gold, no table. **Recommend decide at E-scaffold time; contracts can be written either way.**

**AMD-20 — MCP "no Trino client dependency" (CONTRACT-F)**
False-by-construction: ai-gateway-client's sole dep IS metric-engine (contains trino-adapter). Real tested invariant: no SQL emission, certified seams only. (R1) Restate contract as seam-only + dependency-graph test. (R2) Re-home MCP over compiled semantic views post-D.2. **Recommend R1 now, R2 later.**

**AMD-21 — G's 501 stub vs shipped recommendations**
A working recommend-only surface ships (decisions.routes.ts + ledger + UI). (R1) Grandfather it; 501 stub scoped to the NEW gold_recommendations-backed endpoint. (R2) Verbatim spec — regresses shipped feature. **Recommend R1.**

**Non-amendment defects surfaced (fix, don't amend):**
- Gate command `pnpm turbo build lint test` doesn't match turbo task names (test:unit etc.) — fix the gate definition.
- validateSchemaCompatibility 404→compatible no-op (events/index.ts:130–136) — bug.
- Shopify refund mapper occurred_at=1970 epoch fallback → 100% refund quarantine — bug, C.2.1 prerequisite.
- lint:boundaries 16 pre-existing errors — debt to pay before it gates.
- silver_inventory_level 0 rows despite live product.upsert events — investigate.
- infra/kafka/topics.yml stale bridge-consumer docs — doc fix.
- Repo-vs-live drift: 46 view files vs 39 live views; gold_settlement_summary/gold_ai_features registered-but-absent — "file exists ≠ served live" discipline in all gates.

---

## WAVE A WORK BREAKDOWN (ordered, deduplicated)

Scope: S (<1d), M (1–3d), L (>3d). Deps reference item ids. Amendments (WA-00) are S each and unblock everything downstream.

**Stage 0 — decisions & debt (before any behavior change)**
- **WA-00** File amendments AMD-01…AMD-09, AMD-12 (§0.4). S each, ~1d total. Deps: none. **CRITICAL PATH HEAD.**
- **WA-01** packages/platform-flags: Redis per-brand flags, default OFF, sanctioned key builder, port+adapter; decide Spark-side flag read (PG column vs env). [§0.5] M. Deps: none.
- **WA-02** ESLint: pay 16-error boundary debt + fix 2 stale directives + ADD hexagonal infra-import rule; green in CI. [§0.5] M. Deps: none.
- **WA-03** Pixel build unification: make served /pixel.js a literal build artifact of packages/pixel-sdk (or ratified single-source generator) — pre-Wave-A item the spec doesn't budget. [A.1.1] M. Deps: none.
- **WA-04** packages/testing-golden: seeded generator ~50k events / 3 brands / full scenario matrix; snapshot today's pipeline outputs; flags-OFF byte-compare harness. [§1.10] L. Deps: none (must COMPLETE before any flag-on behavior ships).
- **WA-05** Apicurio governance fixes: idempotent compat-rule boot step, fix validateSchemaCompatibility URL/404 bug, per AMD-03. [§1.7] S. Deps: WA-00(AMD-03).

**Stage 1 — normalization & signal capture**
- **WA-06** packages/identity-normalization (TS + Python twin): NFC email, libphonenumber E.164 (IN/AE/SA/QA/BH/KW/OM), extraction from identity-core/_raw_normalize; property-based 10k cross-language tests. [A.1.3, A.5.2] L. Deps: WA-00(AMD-01, AMD-02).
- **WA-07** pixel.identify.v1: Apicurio envelope, phone support, source field, consent_state; sessionStorage dedupe; MutationObserver+blur autodetect gated on `pixel.autodetect.enabled` flag; remove password-form reads; implement in unified pixel. [A.1.1] L. Deps: WA-01, WA-03, WA-05, WA-06.
- **WA-08** Per-brand consent config: tenancy.brand columns {identity_capture, consent_source} + migration seeding current behavior; TCF __tcfapi reader; Silver denied-VALUE drop for identifies + test. [A.1.2, A.5.4] M. Deps: WA-00(AMD-04); pixel half deps WA-07.
- **WA-09** Connector identity-field standardization: 4 mappers onto shared normalization + canonical slot (interop-hash dual-write per AMD-01); unify Shopflo legacy names; checkout_session_id on GoKwik order+checkout, pixel checkout events, and resolver identifier types. [A.1.4] M. Deps: WA-06.
- **WA-10** Historical hash backfill Spark job (bronze → silver, additive MERGE, shredded-subject skip) or ratified shadow-cutover+FULL_REFRESH. [A.1.4] M. Deps: WA-06, WA-09.

**Stage 2 — identity core**
- **WA-11** IDENTIFIES edge enrichment: first_seen/last_seen/source/count (ON MATCH SET). [A.1.5] S. Deps: WA-00(AMD-06).
- **WA-12** Per-brand ordered identity priority: versioned config, threaded into IdentityResolver, config-version stamped on outcomes. [A.1.5] M. Deps: WA-01, WA-11.
- **WA-13** silver_identity_map system-time: system_from/system_to columns + append-per-mutation writes per AMD-07. [§1.5] M. Deps: WA-00(AMD-07).
- **WA-14** Sanctioned views identity_current_v + identity_asof (Trino + Spark seam) + CI raw-read guard in pr.yml + repoint existing readers. [A.2.2] M. Deps: WA-13 (asof system axis), current_v can start immediately.
- **WA-15** Map-mutation event lane per AMD-08 (emit on every mutation, keyed brand_id+identifier_hash). [A.2.3(5)] M. Deps: WA-00(AMD-08), WA-05.

**Stage 3 — stitch v2**
- **WA-16** Spark incremental multi-key stitch: silver_session_identity + silver_stitch_conflicts; identifier set {anon, checkout_session_id, email, phone}; resolve via identity_current_v; dual-write ops.silver_identity_link behind flag; watermark + brand_id/event_date partitioning + join policy + 4GB local profile + 5M/20min gate baseline. [A.2.1/A.2.3/A.2.5] **L — critical path.** Deps: WA-09, WA-12, WA-14, WA-01.
- **WA-17** Shared-device 90d per-brand recency rule (resolver medium-adoption + stitch). [A.2.3(4)] S. Deps: WA-11, WA-16.
- **WA-18** Re-stitch consumer: consume WA-15 events, re-run stitch for sessions containing the identifier within attribution lookback. [A.2.3(5), A.5.5] M. Deps: WA-15, WA-16.
- **WA-19** Merge/unmerge completion: actor on MergeEvent/audit; unmerge emits identity event + reversion-job restoration path; A.5.7 round-trip test. [A.2.4] M. Deps: WA-15 (event), reversion work shared with Wave B.

**Stage 4 — probabilistic + realtime**
- **WA-20** Splink quarantined layer: Python Spark job, silver_probabilistic_stitch (≥0.95), customer_sessions_extended_v, knowledge-base/models/splink-<v>.md (holdout precision ≥0.98), gateway estimated:true, zero-probabilistic-in-attribution DATA test. [A.3, A.5.6] L. Deps: WA-16 (deterministic labels), WA-00(AMD-12).
- **WA-21** Redis touchpoint cache consumer: `{brand}:tp:{brain}` zset last-200/TTL 30d, deterministic-only, merge union handler on identity.merged.v1. [A.4(2)] M. Deps: WA-01, sanctioned key builder; independent of stitch.
- **WA-22** ≤50ms p99 identity-upsert benchmark (1k-event replay) recorded in gate. [A.4(1), §1.8] S. Deps: none (baseline first, re-run after WA-11/12).

**Stage 5 — gate**
- **WA-23** GATE-A: acceptance A.5.1–8 on golden (≥40%/≥55% identification, hash equivalence 0/10k, ambiguity conflict-row, consent drop, day-7 re-stitch, probabilistic quarantine, merge/unmerge round-trip, flags-OFF byte-identical), invariant checklist §1.9, rollback verification, evidence file. Deps: ALL above + WA-04.

**Critical path:** WA-00 → WA-06 → WA-09 → (WA-12/WA-14) → WA-16 → WA-18 → WA-23, with WA-04 (golden) and WA-01 (flags) as mandatory parallel rails that must land before any flag-on behavior; WA-03 (pixel unification) gates WA-07.
