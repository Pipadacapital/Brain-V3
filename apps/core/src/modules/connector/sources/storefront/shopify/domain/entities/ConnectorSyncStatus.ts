/**
 * BACKWARD-COMPAT RE-EXPORT SHIM — neutral entity moved to @brain/connector-core.
 * New code should import from '@brain/connector-core' directly.
 */
export { ConnectorSyncStatus } from '@brain/connector-core';
export type { ConnectorSyncStatusProps, SyncState } from '@brain/connector-core';
