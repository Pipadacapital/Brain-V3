-- SPEC:D.2 / §1.11.1 — compiled INTERACTIVE metric view: cm1 (v1) @ grain=day.
-- Reads the pre-agg iceberg.brain_serving.preagg_cm1_day (covered dims). Long-tail/uncovered dims → the
-- base-entity fallback view brain_serving.mv_metric_cm1_day_slow. Tenancy: BRAND_PREDICATE sentinel → `brand_id = ?`.
-- GENERATED by @brain/semantic-metrics compiler — DO NOT EDIT. Change the YAML + recompile.
CREATE OR REPLACE VIEW brain_serving.mv_metric_cm1_day AS
SELECT
  brand_id,
  period,
  currency_code,
  channel,
  cm1_minor,
  (cm1_minor) AS cm1
FROM iceberg.brain_serving.preagg_cm1_day
WHERE ${BRAND_PREDICATE};
