-- SPEC:D.2 (AMD-25) — compiled CROSS-ENTITY metric view: mer (v1) @ grain=all
-- primary=semantic_order × cross=semantic_campaign — blended whole-period, joined on (brand_id, currency_code).
-- GENERATED by @brain/semantic-metrics compiler — DO NOT EDIT. Change the YAML + recompile.
CREATE OR REPLACE VIEW brain_serving.mv_metric_mer_all AS
WITH unioned AS (
  SELECT
    brand_id,
    currency_code,
    'base' AS __src,
    net_revenue_minor AS net_revenue_minor,
    NULL AS spend_minor
  FROM brain_serving.semantic_order
  UNION ALL
  SELECT
    brand_id,
    currency_code,
    'cross' AS __src,
    NULL AS net_revenue_minor,
    spend_minor AS spend_minor
  FROM brain_serving.semantic_campaign
),
agg AS (
  SELECT
    brand_id,
    currency_code,
    SUM(net_revenue_minor) FILTER (WHERE __src = 'base') AS net_revenue_minor,
    SUM(spend_minor) FILTER (WHERE __src = 'cross') AS spend_minor
  FROM unioned
  WHERE ${BRAND_PREDICATE}
  GROUP BY 1, 2
)
SELECT
  brand_id,
  currency_code,
  net_revenue_minor,
  spend_minor,
  (CAST(net_revenue_minor AS double) / NULLIF(spend_minor, 0)) AS mer
FROM agg;
