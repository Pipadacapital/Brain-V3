# integration.yml — real-engine integration suite (C5 / R-48).
#
# The PR workflow (pr.yml) runs unit/contract/isolation/parity against a Postgres service container
# ONLY — the `.live.test.ts` suites (real Trino-over-Iceberg serving, real Kafka) and the Playwright
# e2e are NOT exercised in CI, so a cross-tenant serving leak or a broken live pipeline could merge green.
# This workflow closes that: it stands up the REAL engines (the repo's docker-compose stack) and runs
# the live + e2e suites against them.
#
# BRAIN V4: StarRocks is REMOVED. Serving is Trino-over-Iceberg (fronted by Redis). This workflow brings
# up Trino (not StarRocks) and provisions the brain_serving Trino VIEWS via db/trino/views/run-trino-views.sh.
#
# ⚠️ INTRODUCED NON-BLOCKING ON PURPOSE: it runs on a schedule, on master pushes, and on demand — NOT
# on pull_request — so it can stabilize (engine cold-start timing, Playwright full-stack wiring)
# WITHOUT blocking PR merges while it's being proven. PROMOTE TO A PR GATE once it's reliably green:
# add `pull_request:` to the triggers below and mark it a required check.

name: integration
on:
  workflow_dispatch: {}
  schedule:
    - cron: '0 2 * * *' # nightly 02:00 UTC
  push:
    branches: [master]
  # PR gate (promoted now the dbt Silver step is proven green — verified locally + on master push).
  # Path-filtered to the data/connector/medallion/infra surfaces so doc-only PRs don't pay the
  # real-engine cost. To make it BLOCKING, mark "integration" a required status check in branch
  # protection (a repo-admin setting, not expressible in this file).
  pull_request:
    paths:
      - 'apps/**'
      - 'packages/**'
      - 'db/**'
      - 'infra/**'
      - '.github/workflows/integration.yml'

permissions:
  contents: read

env:
  DATABASE_URL: postgres://brain:brain@localhost:5432/brain
  BRAIN_APP_DATABASE_URL: postgres://brain_app:brain_app@localhost:5432/brain
  REDIS_URL: redis://localhost:6379
  KAFKA_BROKERS: localhost:19092
  KAFKA_BROKERS: localhost:19092
  # Brain V4 serving engine: Trino-over-Iceberg (StarRocks removed). The metric-engine/core read the
  # brain_serving Trino views; createTrinoPool targets TRINO_URL (host:port = TRINO_HOST:TRINO_PORT).
  TRINO_HOST: localhost
  TRINO_PORT: '8090'
  TRINO_URL: http://localhost:8090

jobs:
  # ───────────────────────────────────────────────────────────────────────────
  # Real-engine isolation + parity + live suites (Trino-over-Iceberg serving, Kafka).
  # ───────────────────────────────────────────────────────────────────────────
  real-engine-tests:
    name: Live suites on real engines (Trino + Kafka)
    runs-on: ubuntu-latest
    timeout-minutes: 40
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: pnpm/action-setup@v6
      - uses: actions/setup-node@v6
        with:
          node-version-file: .nvmrc
          cache: pnpm
      - run: pnpm install --frozen-lockfile

      - name: Bring up real engines (Postgres + Trino + Kafka + Redis + MinIO + Iceberg REST + Spark)
        run: |
          # --wait blocks until every service's compose healthcheck is healthy (Trino + the iceberg-rest
          # fixture both have a cold start; their healthchecks + start_period account for it).
          # lakehouse (minio + iceberg-rest + Trino + Spark Bronze sink) is needed for the V4 Spark
          # Silver/Gold refresh that REPLACED dbt — the live suites read the brain_serving Trino views it feeds.
          docker compose --profile core --profile ingest --profile lakehouse up -d --wait

      - name: Provision brain_app login role + apply migrations
        run: |
          PGPASSWORD=brain psql -U brain -h localhost -d brain -c "
            DO \$\$
            BEGIN
              IF NOT EXISTS (SELECT 1 FROM pg_roles WHERE rolname = 'brain_app') THEN
                CREATE ROLE brain_app LOGIN NOSUPERUSER NOCREATEDB NOCREATEROLE NOBYPASSRLS PASSWORD 'brain_app';
              ELSE
                ALTER ROLE brain_app LOGIN PASSWORD 'brain_app';
              END IF;
            END
            \$\$;
          "
          pnpm migrate:up

      # ── V4 transform: Spark Silver → Spark Gold → Iceberg, served by Trino views (REPLACES dbt+StarRocks) ──
      # Brain V4 removed dbt (Phase 6b) AND StarRocks (serving → Trino). The analytics transform is
      # Spark-on-Iceberg: the Spark Silver/Gold jobs materialize the EXTERNAL Iceberg catalogs from raw
      # Bronze. This step runs the V4 oneshot refresh (so the Iceberg Gold/Silver marts exist), then
      # provisions the brain_serving Trino VIEWS over them — thin projections, no async refresh. The
      # *.live suites below read the same Trino serving tier the dashboards read.
      - name: V4 refresh — Spark Silver/Gold materialize Iceberg (oneshot)
        run: |
          # ONESHOT runs every Spark Silver job, then every Spark Gold job → deterministic Iceberg
          # Gold/Silver for the live suites. The Spark run scripts join kafka's netns for service DNS
          # (iceberg-rest/minio/trino), same as the parity oracle. No StarRocks MV SYNC — Trino views are live.
          ONESHOT=1 bash tools/dev/v4-refresh-loop.sh

      - name: Provision Trino serving views (iceberg.brain_serving.mv_*)
        env:
          TRINO_URL: http://localhost:8090
        run: |
          # Create iceberg.brain_serving + every brain_serving.mv_* Trino VIEW over the Iceberg Gold/Silver
          # marts Spark just materialized. CREATE OR REPLACE VIEW — idempotent; no async refresh to wait on.
          bash db/trino/views/run-trino-views.sh

      # Isolation fuzz + parity oracle — now against the REAL Trino serving tier (the PR run has no Silver
      # tier, so the cross-tenant negative-control on the ${BRAND_PREDICATE} seam only truly fires here).
      - name: Isolation fuzz (real engines)
        run: pnpm turbo run test:isolation
      - name: Parity oracle (real engines)
        run: pnpm turbo run test:parity

      # The *.live.test.ts suites (credit writer, reconcile, analytics, dq) read the real Trino serving tier.
      - name: Core live suites (Trino-over-Iceberg serving)
        run: pnpm --filter @brain/core exec vitest run --no-file-parallelism live
      - name: Stream-worker e2e (Kafka + Bronze)
        run: pnpm --filter @brain/stream-worker test:e2e

      - name: Dump engine logs on failure
        if: failure()
        run: docker compose --profile core --profile ingest --profile lakehouse logs --tail 200
      - name: Tear down
        if: always()
        run: docker compose --profile core --profile ingest --profile lakehouse down -v

  # ───────────────────────────────────────────────────────────────────────────
  # Playwright e2e against the full running stack.
  # ───────────────────────────────────────────────────────────────────────────
  playwright-e2e:
    name: Playwright e2e (full stack)
    runs-on: ubuntu-latest
    timeout-minutes: 40
    env:
      BASE_URL: http://localhost:3000
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: pnpm/action-setup@v6
      - uses: actions/setup-node@v6
        with:
          node-version-file: .nvmrc
          cache: pnpm
      - run: pnpm install --frozen-lockfile

      - name: Bring up engines
        run: docker compose --profile core --profile ingest up -d --wait

      - name: Provision brain_app + migrate
        run: |
          PGPASSWORD=brain psql -U brain -h localhost -d brain -c "
            DO \$\$ BEGIN
              IF NOT EXISTS (SELECT 1 FROM pg_roles WHERE rolname = 'brain_app') THEN
                CREATE ROLE brain_app LOGIN NOSUPERUSER NOCREATEDB NOCREATEROLE NOBYPASSRLS PASSWORD 'brain_app';
              ELSE ALTER ROLE brain_app LOGIN PASSWORD 'brain_app'; END IF;
            END \$\$;"
          pnpm migrate:up

      - name: Install Playwright browser
        run: pnpm --filter @brain/web exec playwright install --with-deps chromium

      - name: Materialize .env for the app stack (the dev scripts read --env-file=../../.env)
        run: |
          cat > .env <<'ENV'
          NODE_ENV=development
          DATABASE_URL=postgres://brain:brain@localhost:5432/brain
          BRAIN_APP_DATABASE_URL=postgres://brain_app:brain_app@localhost:5432/brain
          REDIS_URL=redis://localhost:6379
          KAFKA_BROKERS=localhost:19092
          KAFKA_BROKERS=localhost:19092
          TRINO_HOST=localhost
          TRINO_PORT=8090
          TRINO_URL=http://localhost:8090
          ENV

      - name: Start the app stack (core + web + collector + stream-worker)
        run: |
          pnpm turbo run dev \
            --filter=@brain/core --filter=@brain/web \
            --filter=@brain/collector --filter=@brain/stream-worker > /tmp/stack.log 2>&1 &
          echo "stack started (pid $!)"

      - name: Wait for web to be ready
        run: npx wait-on http://localhost:3000 --timeout 120000

      - name: Playwright e2e
        run: pnpm --filter @brain/web test:e2e

      - name: Upload Playwright report + stack log
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: |
            apps/web/playwright-report
            /tmp/stack.log
          if-no-files-found: ignore

      - name: Tear down
        if: always()
        run: docker compose --profile core --profile ingest down -v
