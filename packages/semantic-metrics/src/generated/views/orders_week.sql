-- SPEC:D.2 / §1.11.1 — compiled INTERACTIVE metric view: orders (v1) @ grain=week.
-- Reads the pre-agg iceberg.brain_serving.preagg_orders_week (covered dims). Long-tail/uncovered dims → the
-- base-entity fallback view brain_serving.mv_metric_orders_week_slow. Tenancy: BRAND_PREDICATE sentinel → `brand_id = ?`.
-- GENERATED by @brain/semantic-metrics compiler — DO NOT EDIT. Change the YAML + recompile.
CREATE OR REPLACE VIEW brain_serving.mv_metric_orders_week AS
SELECT
  brand_id,
  period,
  channel,
  order_count,
  (order_count) AS orders
FROM iceberg.brain_serving.preagg_orders_week
WHERE ${BRAND_PREDICATE};
